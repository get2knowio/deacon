{
	"name": "ParityUpDotfilesNonExecutableInstallCommand",
	"image": "mcr.microsoft.com/devcontainers/base:bookworm",
	"workspaceFolder": "/workspace",
	"workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
	"remoteUser": "vscode",
	"postCreateCommand": "mkdir -p /tmp/dotsrc && cd /tmp/dotsrc && printf '#!/bin/sh\\npwd > /workspace/dotfiles-custom-log.txt\\n' > .run-my-dotfiles-script && chmod 644 .run-my-dotfiles-script && git init -q -b main . && git -c user.email=parity@example.com -c user.name=parity add -A && git -c user.email=parity@example.com -c user.name=parity commit -q -m fixture"
}
