# Does deacon behave like the DevContainers CLI?

Compared against **`@devcontainers/cli` 0.87.0** and the [containers.dev spec]
(https://github.com/devcontainers/spec) at commit `113500f4`.

This is the durable record of what we know about deacon's behavior relative to the
reference. It is **hand-maintained**: when a scenario in `parity/cases/` teaches us
something new, the row changes here in the same commit.

## How to read a row

| Status | Meaning |
|---|---|
| **Conformant and matching** | The spec defines it, deacon follows it, and the reference agrees. Nothing owed. |
| **deacon follows the spec where the CLI does not** | An observable difference in which deacon is the conformant side. The reference's deviation, not work we owe. |
| **Documented choice** | The spec is silent and the two tools differ deliberately. Each is allowlisted with its reasoning in `parity/ALLOWLIST.json`. |
| **deacon extension** | Capability the reference has no equivalent for. Nothing to compare against. |
| **Open nonconformance** | deacon is the wrong side and we know it. Always carries an issue link. |

A status is a claim backed by a scenario or an allowlist entry — never by argument
alone. Where a row has no scenario yet, it says so.

## Summary

Of **129 recorded behaviors**:

- **0** — open nonconformance
- **9** — deacon follows the spec where the CLI does not
- **11** — documented choice
- **20** — deacon extension
- **89** — conformant and matching

**Batch 2 of the mining pass reopened the open-nonconformance column with three rows, all
about lockfiles, all measured against the pinned oracle. All three have since closed, and
the column is empty again.** The batch
([#480](https://github.com/get2knowio/deacon/issues/480), the lockfile family) triaged
twelve upstream configs, landed five scenarios and deferred seven; the two defects it found
are [#556](https://github.com/get2knowio/deacon/issues/556) — `build` parsed
`--no-lockfile` and `--frozen-lockfile` and consulted neither, so both flags were inert
while `up` implemented them, **CLOSED 2026-08-11** — and
[#557](https://github.com/get2knowio/deacon/issues/557) —
`up --frozen-lockfile` compared the lockfile byte-for-byte, so it rejected every lockfile the
reference CLI writes even though the two documents said exactly the same thing, **CLOSED
2026-08-11** and its case green. Each red case asserted the FIXED behaviour, so all three
turned green on their fix rather than on a case edit, and no assertion moved. #556's fix is
the one the two green control rows predicted: routing `build` through the SAME decision `up`
makes (`commands/shared/lockfile`) rather than giving it a second implementation, which is
what "a missing wire, not a missing capability" is worth in code.

**#557's second half — which key ORDER deacon EMITS — was ruled and landed with it.** The
spec prescribes the three properties and says nothing normative about their order, so this
was a spec-silent output surface of exactly the class
[#486](https://github.com/get2knowio/deacon/issues/486) and
[#490](https://github.com/get2knowio/deacon/issues/490) were: filed for a maintainer ruling
rather than asserted. Ruled MATCH on 2026-08-11 — deacon writes `version, resolved,
integrity`, the reference's order and the order every example in `devcontainer-lockfile.md`
uses. It shipped in the SAME change as the tolerant comparison on purpose: the order change
rewrites every existing deacon-written lockfile once, and a byte comparison still in place
would have failed `--frozen-lockfile` across that churn. No tolerance was authored, because
after the change there is nothing to tolerate — the files deacon writes are byte-identical
to the reference's.

**The batch-1 mining pass is fully dispositioned.** That pass ([#480](https://github.com/get2knowio/deacon/issues/480)) ran 22 scenarios harvested from the reference CLI's own e2e
fixtures at `v0.87.0`. Fourteen agreed on arrival and are rows below. Eight cases were red on
seven distinct deacon-side differences, every one measured against the pinned oracle and
filed. ALL FIVE clear-cut defects closed on 2026-08-05 —
[#487](https://github.com/get2knowio/deacon/issues/487) and [#488](https://github.com/get2knowio/deacon/issues/488) (Feature-level legacy VS Code properties; local Features
outside `.devcontainer/`), [#489](https://github.com/get2knowio/deacon/issues/489) (local-Feature identity per option set), then
[#491](https://github.com/get2knowio/deacon/issues/491) (bare v1 Feature ids) and [#492](https://github.com/get2knowio/deacon/issues/492) (`build.options` never reaching the
builder).

The remaining two sat on SPEC-SILENT output surfaces where matching the reference might or
might not be worth it, so they were filed for a ruling rather than as defects and carried no
row until one existed — asserting a status before the ruling is the failure this document
exists to avoid. **The maintainer ruled FIX on both on 2026-08-05**, and each is now an
ordinary "matches the reference" row in the `read-configuration` section:

- [#486](https://github.com/get2knowio/deacon/issues/486) — the pre-`customizations` VS Code properties (`settings`, `extensions`,
  `devPort`) on the CONFIGURATION were echoed where authored; the reference migrates them
  into `customizations.vscode`. The ruling turned on the standing rule that a spec-silent
  output surface makes the reference the authority, PLUS internal consistency: the same
  properties on a FEATURE were already migrated (#487).
- [#490](https://github.com/get2knowio/deacon/issues/490) — `featuresConfiguration…sourceInformation.userFeatureId` was canonicalized
  (`…/git` reported as `…/git:latest`); the reference preserves the authored form. The ruling
  followed deacon's own precedents #407 and #491 — the reference reports the id the
  configuration wrote — rather than #490's own (absent) collision harm.

Both fixes reuse the mechanism their precedent introduced rather than adding a second: one
legacy-property migration shared by the configuration and Feature readers, one
canonical → authored carry shared with #491 and the lockfile. Every case mined by the batch
is now green with no tolerance, each verified live against the pinned oracle on its own
fixture, and none of the seven left an open-nonconformance row behind.

The open-nonconformance column emptied on 2026-08-03 when duplicate Features
([#430](https://github.com/get2knowio/deacon/issues/430)) left it, refilled the same day
with the two compose-path gaps that closing #448 surfaced
([#462](https://github.com/get2knowio/deacon/issues/462) uid remapping,
[#460](https://github.com/get2knowio/deacon/issues/460) lifecycle phase coverage), and both
left it on 2026-08-04. Fixing #460 then surfaced
[#467](https://github.com/get2knowio/deacon/issues/467), which held the column at one until
it too was measured and closed on 2026-08-04. Each was found and filed by the suite's own
cases before any user hit it, and each fix was verified the way it was found — live against
the pinned oracle, on the issue's own repro.

That chain is the system working rather than a treadmill: each measurement was
finer-grained than the one before it. #467 is a defect neither of its predecessors could
see, because a path that ran no lifecycle phase at all could not reveal how two SOURCES of
the same phase combine — and it was invisible to every image-metadata scenario that
existed, all of which declared the image's hook in a phase the configuration left alone.
An empty column is a statement about what has been MEASURED, not a claim of correctness,
and it has a short shelf life: the honest claim is that every difference we have measured is
either fixed, deacon-conformant, allowlisted with a ruling, or in this column with an issue
link.

It had a shelf life of hours. Closing #467 prompted an audit for other callers of the merge
it fixed, and [#477](https://github.com/get2knowio/deacon/issues/477) is what that audit
measured: the same collapse on the CONTAINER's stamped label, which `set-up` reads and #467's
fix did not reach. It refilled the column the day #467 emptied it, and left again the same
day when the fix was measured against the oracle on all three of its shapes. That is the
fourth link in the same chain and it was found the same way as the other three — by
measuring a surface against the pinned oracle rather than by reasoning about the code.

So the column reads zero again, with the same caveat it carried the last time it did: an
empty column is a statement about what has been MEASURED, and its shelf life is however
long it takes someone to look somewhere new. #477 is the standing evidence for that — it
existed the whole time #467 was open and became visible only when someone audited the other
callers of the merge #467 fixed. The audit that closed it went one step further and found
`exec`, the only other caller, clean for a reason rather than by luck: it reads the
last-wins scalars off that helper and runs no lifecycle phase at all. The next link, if
there is one, will come from measuring a surface nobody has measured yet.

The fifth link came from the same measurement that closed the fourth. Closing #477 read
`set-up --include-merged-configuration` on its way past and found the BLOCK wrong even
though the hooks it reported were now right:
[#483](https://github.com/get2knowio/deacon/issues/483) refilled the column on 2026-08-04
and left it on 2026-08-05. It is the clearest case yet for measuring a surface rather than
reasoning about it — the collection logic was correct and shared, and only the reporting
path around it was not, which no test of what set-up RAN could see. The fix routes that
block through the pipeline `read-configuration` already used, and the row below is now
backed by the first parity case ever to invoke `set-up`.

Two rows moved from "deacon follows the spec where the CLI does not" to "conformant and
matching" at [#439](https://github.com/get2knowio/deacon/issues/439) — not because deacon
changed status by argument, but because the classification was re-adjudicated against the
pinned spec and then the gap it was hiding was closed. See the
`--include-features-configuration` row.

One row moved out of "open nonconformance" at
[#440](https://github.com/get2knowio/deacon/issues/440): `build --push` pushing deacon's
internal tag ([#438](https://github.com/get2knowio/deacon/issues/438)). It was not fixed
for its own sake — stamping `devcontainer.metadata` on a pushed image requires deacon to
own the push, and owning it means choosing what to push. See the `build --push pushes the
image names the user supplied` row.

## Coverage this document does *not* claim

Combinatorial coverage of the scenario space is **no longer tracked**. The previous
model counted obligations across a six-dimension scenario grid and dispositioned each
one; it measured a hole rather than closing it, and cost more to maintain than the
measurement was worth. What remains is the scenario suite itself: if a behavior matters,
it gets a scenario.

Four areas of coverage were **consciously dropped** when the parity corpora were
retired, rather than ported:

- **Cross-CLI container handoff** — running the reference's `up` then deacon's `up` over
  one workspace and asserting the second provisions its own container.
- **Rendered compose-project state** — *partially recovered.* The primary service's
  resolved volumes, environment, mount graph and project name are now compared by
  `case-state-compose-feature-mounts` and `case-state-compose-sidecar-volume`. What stays
  dropped is the rendered project *document* — the merged Compose file each CLI hands to
  `docker compose` — which the two deliver differently (deacon on stdin, the reference via
  a temp file) and which no channel observes.
- **Runtime-versus-merged cross-check** — relating one CLI's
  `read-configuration --include-merged-configuration` to the container it actually made.
- **Intra-deacon comparison** — diffing two deacon runs (single-container versus compose)
  against each other, with no reference side.

Each needed machinery no declarative channel provides, and none had produced a finding.
They are recorded here so their absence is a decision on the record rather than an
oversight nobody noticed.

## Behaviors by area

### `build`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| A BuildKit-only build flag (`--cache-to`) is accepted and passed through, and the build still produces the configured image. | deacon extension | 1 scenario | deacon-only surface. Scoped deliberately to acceptance and pass-through, and exercised with `type=inline` — the one export type buildx's default `docker` driver supports across Docker versions, so the case measures deacon rather than the runner. It used `type=local`, which needs the `docker-container` driver on older Docker and has no Podman equivalent; there the build produced no image and the case failed for reasons unrelated to deacon. Local export was not the claim in any case: even where it succeeded it wrote nothing to the destination… |
| A Dockerfile instruction that exits non-zero fails the build, and the failure is reported at the build stage rather than as a later container-creation failure. | matches the reference | 3 scenarios | The exit code alone cannot distinguish a build failure from a container-creation failure; the declared failure phase is what makes the two different facts rather than… |
| `build` reports and tags the FEATURE-EXTENDED image, so a user-supplied `--image-name` resolves to the image with the Features installed rather than to the pre-Feature… | matches the reference | 4 scenarios | This project shipped the inverse once: the post-build Feature pass layered correctly and the user's tag still pointed at the base, which every outcome-only assertion… The count read `2` while three cases already carried the behavior and is corrected here alongside the fourth, `case-build-dockerfile-feature-multi-order-label` (#536), which reads the `devcontainer.metadata` label off the image the user's `--image-name` names — a tag left pointing at the pre-Feature base carries no Feature entries at all, so the ordering case pins this claim as a by-product of pinning its own. |
| `build` stamps `org.deacon.configHash` on the image it produces; the reference CLI sets no such label. | deacon extension | 2 scenarios | Measured against oracle 0.87.0 on fx-build-image-labels and fx-build-image-args, not inferred. A deacon-only bookkeeping label in deacon's own `org.deacon.*` namespace —… |
| The `devcontainer.metadata` label on a BUILT image records one entry per installed Feature plus the configuration pick, so a later consumer of the image can read what it… | matches the reference | 5 scenarios | CLOSED by #436 for the Dockerfile and image-reference shapes, and by #440 for the three that #436 left unstamped — `--push`, `--output` and Compose. `build` used to stamp a hardcoded `[{"configHash":…}]`, dropping every Feature entry. It now writes the value `up` writes, from the one shared construction (`up::merged_config::container_metadata_entries`), stamped on the final image once it exists. Re-measured against oracle 0.87.0: on fx-build-dockerfile-feature both sides record `{"id":"./features/marker"}`; on a config with `remoteUser`/`remoteEnv` and no Features both record the config pick; on a base image carrying its own label both prepend that entry. On fx-build-image-labels and fx-build-image-args — no Features, nothing in the config pick — neither side writes the label at all, which is what the two differential cases observe. The byte form still differs in JSON whitespace (`[{…}]` vs `[ {…} ]`); that is the separate documented choice recorded by `bhv-container-metadata-label-serialization` (its key-order half was a deacon defect and was closed by #394). #440 then closed the three shapes that were skipped rather than fixed: after #436 a `--push`, `--output` or Compose build carried NO label where it had previously carried a wrong one. deacon now builds into the local daemon, stamps, and publishes from there, so all four shapes go through the one construction. Measured on this branch: a Dockerfile config with `containerEnv`/`remoteUser` pushed to a throwaway `registry:2` and pulled back records `[{"containerEnv":{…},"remoteUser":"root"}]`, byte-identical to what the local shape stamps; the same config exported with `--output type=docker,dest=…` carries it in the exported tar's image config; and a Compose config with a local Feature records `[{"id":"./features/marker"},{"containerEnv":{…},"remoteUser":"root"}]`. The one shape still unstamped is a MULTI-platform `--push`/`--output`, which BuildKit will not load into the daemon — deacon warns rather than pretending. Fifth scenario added at [#536](https://github.com/get2knowio/deacon/issues/536), which closed a COVERAGE gap rather than a defect: the label's entries are ordered — one per installed Feature, in install order, then the configuration pick — and every `build` fixture declaring a Feature declared exactly ONE, so nothing would have reported the day `build`'s order stopped matching `up`'s (they share `resolve_features_ordered` and `apply_features_and_lockfile`). `case-build-dockerfile-feature-multi-order-label` builds `fx-build-feature-multi-order`, whose declared order (`late`, `early`) is inverted by `installsAfter` and which pulls in a third Feature (`pulled`) through `dependsOn` that the configuration never names. Measured at oracle 0.87.0, both CLIs, no divergence: `[{"id":"./features/early"},{"id":"./features/pulled"},{"id":"./features/late"},{"containerEnv":{…},"remoteUser":"root"}]`, differing only in the JSON whitespace this row already records, and both produced images report `early/pulled/late` from the marker file the three `install.sh` scripts append to — so the label records the order the installs actually ran in. The assertion pins the label's whole STRING rather than a subset of parsed entries because `jsonSubset` over an array is order-insensitive; and it is a spec-expectation rather than a differential because tolerating the whitespace on `chan-image.labels.devcontainer.metadata` would excuse an order difference on the same path. The cross-CLI half is carried by `case-up-feature-multi-dependency-order-differential`, which compares the marker sequence through the same resolver. |
| build produces a container image and reports the build outcome, matching the reference. | matches the reference | 3 scenarios | Backed by case-build-image-labels-differential and case-build-image-args-differential, which compare the produced IMAGE's whole configuration between the two CLIs. Since #436 closed the `devcontainer.metadata` difference, the only key on which the two sides differ is deacon's `org.deacon.configHash`, tolerated by one named allowlist entry. |
| The image a `build` produces carries what the configuration and Dockerfile authored into it — labels, environment, command and working directory — and `build.args` reach… | matches the reference | 3 scenarios | Measured against oracle 0.87.0 on fx-build-image-labels and fx-build-image-args: `Env`, `Cmd`, `Entrypoint`, `WorkingDir` and every authored label agree exactly on both… |
| `build --output type=docker,dest=<path>` exports the built image to that path and reports the export destination. | deacon extension | 2 scenarios | deacon-only surface: the reference CLI has no `--output` flag on `build`, so there is nothing to compare against and the case is a spec-expectation. The claim pinned is… Second scenario added at #440: the EXPORTED image carries `devcontainer.metadata`. It did not before — the export was handed to the primary BuildKit invocation, which left no local image to stamp. deacon now builds into the daemon, stamps, then exports; the export pass also loads, so the tag the build was given names the stamped image and the label is readable off it as well as present in the exported tar. |
| `build --platform <os/arch>` builds for the named platform and produces a real image carrying the configuration's authored labels. | deacon extension | 1 scenario | deacon-only surface (`--platform` is not a reference `build` flag). Previously covered by an assertion that tolerated either outcome and therefore asserted nothing on… |
| `build --push` to a registry that cannot be reached fails with a non-zero exit and a diagnostic naming the push as the failing step, rather than reporting success. | matches the reference | 1 scenario | Both CLIs fail non-zero and name the push. deacon's message used to name the WRONG registry — it pushed its internal `deacon-build:<hash>` tag, which resolves to Docker Hub, and that push failed first (#438). Closed by #440's restructure: the push is now deacon's own step over the image names the user supplied, so the diagnostic reads `failed to push 'registry.invalid/conformance-push:latest'`, and the case asserts that string rather than the bare phrase. |
| `build --push` pushes the image names the user supplied and nothing else. | matches the reference | 1 scenario | Was an open nonconformance (#438): deacon handed the push to BuildKit, which pushes every `-t` tag, so the internal `deacon-build:<hash>` bookkeeping tag went along — no registry component, therefore Docker Hub, therefore an authorization failure that made `build --push` fail against EVERY registry, reachable or not. Closed as a necessary consequence of #440 rather than as separate work: stamping `devcontainer.metadata` on a pushed image requires building locally and pushing afterwards, and a push deacon performs itself has to choose its targets. It pushes the `--image-name`s (and, only when none were given, the deterministic tag — the same thing BuildKit would have pushed). Measured on this branch: `--push --image-name localhost:<port>/…` against a throwaway `registry:2` succeeds and the pulled-back image carries the metadata label; `--push --image-name registry.invalid/conformance-push:latest` fails naming that target and not Docker Hub. |
| A Dockerfile whose first line is a `# syntax=` frontend directive reaches the builder with that directive still first, so the produced image is the one BuildKit's frontend built. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The directive is meaningful only when it is line 1, so a CLI that rewrites, prepends to, or re-emits the Dockerfile — to inject Feature layers, or to resolve an `ARG` consumed by `FROM` — can displace it, after which Docker reads it as an ordinary comment and silently falls back to the classic frontend. Measured at oracle 0.87.0 on `fx-upstream-dockerfile-syntax-directive`: the two images agree on everything except deacon's `org.deacon.configHash`, the extension every `chan-image` case already tolerates. The fixture carries one authored `LABEL` for a reason worth keeping: without it the reference's image has NO labels at all and the two sides differ at the `labels` NODE, which a tolerance scoped to one key inside that map cannot reach — it reported as a STALE tolerance instead of a tolerated difference. |
| `build.options` reaches the builder, so a `--label` the configuration asks to be passed through appears on the produced image. | matches the reference | 1 scenario | Was an open nonconformance ([#492](https://github.com/get2knowio/deacon/issues/492)): deacon accepted `build.options`, echoed it from `read-configuration`, and never passed it to the build — the silent-fallback shape constitution IV rules out, with no signal to the user because nothing failed. The cause was a SHAPE error, not a missing wire: `build.options` was read with `as_object()` and folded into the same map as `build.args`, so the spec's array (`devcontainerjson-reference.md`: "an array of Docker image build options … passed to the build command when building a Dockerfile") matched nothing and was dropped. `build.args` and `build.options` are now distinct fields, and a non-array `build.options` is a validation error rather than a silent drop. Pass-through semantics were measured off the reference rather than guessed — at 0.87.0 it appends the array VERBATIM as discrete argv elements, immediately after the `--build-arg` pairs and before the context, with no filtering — and deacon now does the same on BOTH build paths: `build` (`commands/build`) and `up`'s Dockerfile build (`commands/up/image_build.rs`), which the issue asked be checked. Verified on `fx-upstream-build-target-options`: `docker image inspect` reports `test_build_options=success` on the image from each path, `/var/test-marker` still reads `||test-content||` (so `build.target` is unchanged), and `case-build-upstream-target-and-options` is green against the pinned oracle with no tolerance beyond the already-adjudicated `org.deacon.configHash`. |
| `build --frozen-lockfile` refuses a workspace with no lockfile, before it builds anything. | matches the reference | 1 scenario | Fixes [#556](https://github.com/get2knowio/deacon/issues/556). The flag's contract is "resolution may not change the lockfile", and with no lockfile there is nothing for resolution to agree with. MEASURED at oracle 0.87.0 on `fx-upstream-lockfile-no-lockfile`, BEFORE the fix: the reference exited 1 with `{"outcome":"error","message":"Lockfile does not exist."}` and left the workspace clean; deacon exited 0, built the Feature-extended image, and WROTE `.devcontainer-lock.json` (327 bytes) — the inverse of what the flag asks for. The cause was admitted in the source: `commands/build/mod.rs`'s `BuildArgs` documented `no_lockfile`/`frozen_lockfile` as "not yet consumed by `build`'s lockfile-writing path", so both were parsed, threaded and dropped, and `apply_features_and_lockfile` wrote unconditionally. AFTER: deacon exits 1 with `Lockfile does not exist.` and writes nothing. The fix is a WIRE, not a second implementation — the skip / semantic-compare / write decision `up` already made now lives in `commands/shared/lockfile` and BOTH subcommands reach it, so a lockfile's bytes and its fate never depend on which subcommand resolved the Features (the seam #563 opened when it consolidated every writer onto `canonical_lockfile_json`). The refusal happens BEFORE the build, matching where the reference throws: its `writeLockfile` is reached from `generateFeaturesConfig`, which runs before the image build. That same call site is why the refusal is gated on the configuration DECLARING Features — `generateFeaturesConfig` returns early when it does not, so `build --frozen-lockfile` on `{"image": …}` with no lockfile exits 0 (measured, both CLIs); the gate also corrected `up`, which refused there and now agrees. `case-build-upstream-lockfile-frozen-missing` asserted the FIXED behavior (exit 1, lockfile absent) on both channels from the day it landed, so it turned green on the fix and NOT on a case edit. Mined from the reference CLI's own e2e fixtures at `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 2). |
| `build --no-lockfile` suppresses lockfile generation: the build succeeds and no `devcontainer-lock.json` is written. | matches the reference | 1 scenario | Fixes [#556](https://github.com/get2knowio/deacon/issues/556), the same missing wire as the row above seen through the other flag, and the half NO exit code can catch. MEASURED at oracle 0.87.0 on `fx-upstream-lockfile-no-lockfile`, BEFORE the fix: both CLIs exited 0 and both produced an image; the reference left `.devcontainer-lock.json` ABSENT, deacon wrote it (327 bytes, the resolved `git:1.3.2` entry). AFTER: deacon leaves it absent. The two flags are kept as separate rows and separate cases deliberately — their correct outcomes are opposite (refuse vs succeed-and-write-nothing), so a fix that wired only one would leave the other silently broken and a single row would go green while half the defect remained; the shared `LockfilePolicy` makes that impossible to get half-right, because one enum decides both. `--no-lockfile` also means "do not READ": measured at 0.87.0, a deliberately stale lockfile came back byte-identical from a `--no-lockfile` build, and deacon now leaves it alone too (deacon never pinned resolution from the lockfile, so the read half was already vacuous — the write half was the observable). `case-build-upstream-lockfile-no-lockfile-flag` asserted the fixed behavior from the day it landed and turned green without an assertion edit. |
| Passing `--no-lockfile` and `--frozen-lockfile` together is rejected as mutually exclusive, before any Docker, registry or filesystem work. | matches the reference | 1 scenario | The two flags ask for contradictory things — ignore the lockfile entirely, and require it present and unchanged — so naming both is a user error rather than a precedence question, and neither CLI picks a winner. MEASURED at oracle 0.87.0 on `fx-upstream-lockfile-no-lockfile`: both exit 1 with the same sentence (`--no-lockfile and --frozen-lockfile are mutually exclusive.`) and both leave the workspace byte-identical. `case-build-upstream-lockfile-flags-mutually-exclusive` needs no daemon and is `resourceGroup: fs-heavy` — the group that isolates a workspace WITHOUT touching Docker. `none` was the obvious choice and is the wrong one, and perturbing the case is what showed why: a `none` case runs against the COMMITTED fixture directory, so dropping `--no-lockfile` from the argv during the watched-to-fail run left a real build to run and a stray `.devcontainer-lock.json` in `parity/fixtures/fx-upstream-lockfile-no-lockfile/` — the untracked-copy failure #423 documented for `fx-upgrade-overlay-lockfile`, which would have silently changed what the other cases on that fixture measure. A `build`/`up` operation must never run un-isolated however certain today's argv looks; the `absent` filesystem claim is kept as the second line of defence. `chan-stderr` is deliberately not declared — the reference prepends its whole `yargs` usage block, so comparing the stream would report a difference about help-text formatting. Upstream drives this from three parameterised tests; the graduated pair is the one both CLIs share. Mined from the reference CLI's own e2e fixtures at `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 2). |

### `doctor`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| `doctor` reports host, platform, and runtime diagnostics in both a human rendering and `--json`, and does so regardless of whether the workspace carries a valid… | deacon extension | 9 scenarios | The pinned reference exposes no `doctor` command — see ext-doctor-diagnostics. Independence from the workspace configuration is the property worth pinning: `doctor` is… |

### `down`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| `down --remove` on a Compose configuration removes the project it created, identifying it by the same project name and workspace labels `up` derived. | deacon extension | 1 scenario | Same extension as bhv-down-removes-container. The risk this record exists for is a derivation that does not reproduce what `up` used — teardown then reports success and… |
| `down` over a workspace folder that has no devcontainer configuration reports that there was nothing to tear down and exits 0; it still resolves any container recorded… | deacon extension | 1 scenario | Recorded by ext-teardown-command: the spec defines no teardown command and the pinned reference exposes none, so there is no reference behavior to align with. Teardown… |
| `down --remove` stops and removes the workspace's container, so a subsequent command targeting that container fails. | deacon extension | 4 scenarios | The containers.dev spec defines no teardown command and the pinned reference exposes none, so `down` is a deacon surface with no reference analogue — see… |

### `exec`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| exec runs a command inside the target container and streams its stdout/stderr and propagates its exit code, matching the reference. | matches the reference | 8 scenarios | Backed by the declarative exec cases (case-exec-decl-tty, case-exec-decl-user, case-exec-decl-working-directory, case-exec-remote-env-propagation), which replaced the… |
| exec --container-id (no --workspace-folder/--config) recovers remoteUser and remoteEnv from the container's devcontainer.metadata label — which up stamps at create time… | matches the reference | 1 scenario | #322: up writes the merged devcontainer.metadata label (single-container, Dockerfile, and compose paths); exec/read-configuration/set-up read it back via the shared… |
| `exec` runs the command with the environment the user-env probe captured, with the PATH entries the image contributed via `ENV PATH` restored when the probe's login… | matches the reference | 1 scenario | Fixed under #370. Measured at oracle 0.87.0 against fx-exec-dockerfile-overlay: the image's `Config.Env` PATH is `/opt/conformance/bin:/usr/local/sbin:…`, and deacon's… |
| The relative order of a restored image `PATH` entry against one a Feature contributed through `/etc/profile.d`, when the probe's login shell dropped the image's. | documented choice | 1 scenario | The residual left by the #370 fix, and a deliberate consequence of how deacon fixes it. Measured at oracle 0.87.0 over fx-exec-feature-path-ordering (the image… |

### `host-ca`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| deacon can inject the host's CA certificates into the TLS trust store used for OCI registry and network access; the reference CLI does not model host-CA injection. | deacon extension | 1 scenario | Deacon-only capability (feature 016-host-ca-injection), enabled via {user_data_folder}/settings.json. Recorded by ext-host-ca-injection; backed by case-host-ca. |

### `observable-state`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| On the Compose path, `devcontainer.metadata` records mount sources as the author wrote them, with `${localWorkspaceFolder}` left unsubstituted. | matches the reference | 1 scenario | CLOSED — #437, a compose-path-only regression of #373. Both CLIs record the template on fx-state-compose-feature-mounts, verified at oracle 0.87.0. deacon built the label from the pre-substitution config all along; Docker Compose then interpolated `${localWorkspaceFolder}` in the generated override to the empty string, so `source=${localWorkspaceFolder}/sib` reached the container as `source=/sib`. The override now doubles every `$` in the values it emits, which is why the single-container path was never affected. |
| The set of Compose files a CLI composes with, and the Compose labels derived from that set (`com.docker.compose.project.config_files`, `com.docker.compose.config-hash`). | documented choice | 3 scenarios | Measured at oracle 0.87.0: both CLIs layer an override on top of the workspace's Compose file, and each delivers it differently — deacon passes it on stdin (recorded as… ADJUDICATED 2026-08-03 and KEPT: spec-check found `dockerComposeFile` defined as user input only; override delivery is unspecified and the labels are Compose's internal bookkeeping. All container observables measured equal. |
| deacon derives a valid, deacon-namespaced, human-readable compose project name (deacon_<folderStem>_<workspaceHash>_<configHash>, hash-only when the stem sanitizes to empty) that docker compose always accepts and that does not collide with… | documented choice | 4 scenarios | Robustness differentiator (issue #265; docs/DIFFERENTIATORS.md). The reference derives <folder>_devcontainer verbatim, so a folder like `-myproj` yields an invalid --project-name docker compose rejects; deacon's derived name is always valid and always outside the reference's convention, so `devcontainer up` can never adopt a deacon-owned project and then die looking for a `vsc-*` image deacon never builds. The FORM changed by maintainer ruling on [#564](https://github.com/get2knowio/deacon/issues/564) (2026-08-11): the workspace-folder stem now leads the two hashes. The frequency argument behind the hash-only form was overstated — there is no VS Code integration path for deacon (no extension; the Dev Containers extension bundles and drives the reference CLI, and *Attach to Running Container* does not provision), so the collision needs both tools PROVISIONING one workspace, which is a migration period rather than a steady state — while the cost was paid daily by the terminal-first audience deacon is actually for: `deacon_6fb1205c_532a7bdd-app-1` in `docker compose ps` identifies nothing without computing a hash. Both hashes stay and both are load-bearing (workspaceHash separates `~/work/api` from `~/tmp/api`; configHash is what makes an edited configuration a new generation, which [#371](https://github.com/get2knowio/deacon/issues/371) and [#551](https://github.com/get2knowio/deacon/issues/551) are built on). The stem is sanitized to Compose's `[a-z0-9][a-z0-9_-]*` — lowercased, disallowed characters replaced with `-`, runs collapsed, leading non-alphanumerics trimmed, truncated to 32 characters — because it is user-controlled input that reaches a `--project-name` argument; an empty result falls back to the hash-only form, which is the `-myproj` case this row was originally written for. Explicit declarations still win verbatim ahead of any derivation (`COMPOSE_PROJECT_NAME` in a sibling `.env`, a compose file's top-level `name:`). Measured live at oracle 0.87.0 on one compose workspace: reference `site_devcontainer` (volumes `site_devcontainer_app-data`, `site_devcontainer_db-data`), deacon before `deacon_742b6f14_b5e9edc0`, deacon after `deacon_site_742b6f14_b5e9edc0`. The four scenarios TOLERATE this name rather than asserting it, so the ruling moved their rationale text and nothing they check — the assertions that do exist are on the project-RELATIVE resource names, which the normalizer derives by stripping whatever project name it observed. Renaming orphans the previous project's named volumes (compose prefixes them), so `up` emits a one-time diagnostic when it finds this workspace's volumes under an older deacon project or a `<folder>_devcontainer` project: it names the project deacon will use, states that the old volumes are separate and intact, and gives the `docker compose -p <project> down -v` / volume-copy commands. Containers are already handled by `stop_superseded_containers`; volumes are deliberately never swept, which is why they need saying out loud. |
| When a Compose service's image is extended with Features, each CLI builds that image itself, so Compose's `com.docker.compose.image` label records a different content… | documented choice | 1 scenario | Measured against oracle 0.87.0 on fx-state-compose-feature-mounts: deacon `sha256:2badca10…`, reference `sha256:35612053…`. The label carries a DIGEST of an image each… |
| deacon stamps five identity/bookkeeping labels onto a created container that the reference CLI does not set at all (devcontainer.configHash, devcontainer.config_name,… | deacon extension | 10 scenarios | Measured against the pinned oracle 0.87.0 on fx-up-basic (024 Phase 5), not inferred. Newly RECORDABLE rather than newly true: the retired strip_intentional_labels rule… |
| Both CLIs override the container command with a shell keep-alive that holds the container open for exec/lifecycle work and exits cleanly on SIGTERM; the command STRINGS… | documented choice | 9 scenarios | Classified as intentional ONLY because the observable behavior was measured equal, not because the difference looks cosmetic. `docker stop`: deacon 245 ms… |
| The `devcontainer.metadata` label records the image metadata the configuration and each installed Feature contribute, with configuration values kept in the form the… | matches the reference | 7 scenarios | Surfaced by 024 US5, fixed under #373. Two differences were measured at oracle 0.87.0 and both are now closed: (1) the reference records a `{"id": "<feature>"}` entry… |
| The BYTE FORM of the `devcontainer.metadata` label value — JSON whitespace — as distinct from the entries it records. | documented choice | 3 scenarios | Split out of `bhv-container-metadata-label-content` when #373 closed the CONTENT difference and left the byte form as the only thing still differing. Two causes were recorded and one is now closed: KEY ORDER was a deacon defect, not a choice — `containerEnv`/`remoteEnv` were `HashMap`, so the label's bytes changed run to run and were never the authored order. #394 made both `IndexMap`; measured by `docker inspect` across repeated `up` runs over one configuration, the label is now byte-identical and in authored order. WHAT REMAINS IS WHITESPACE ALONE: the reference writes a spaced `[ {…}, {…} ]` only when the value reaches the container through an image build, and deacon, which always writes the container label directly, always writes compact `[{…}]`. The Compose case joined it when #437 closed the CONTENT difference there too: the Feature layer forces the reference to build the service image, and a value that reaches the container through a build is written spaced. ADJUDICATED 2026-08-03 and KEPT on that whitespace residual, which is now the record's whole claim. The ruling argues from the evidence already gathered rather than from a new measurement: the reference's byte form varies with its own build routing rather than with anything about the configuration, and once #373 closed the content and #394 closed the key order the two values parse to identical JSON with identical keys in identical order — so there is nothing here a consumer can observe, and matching it would mean varying deacon's label formatting by whether a build happened. |
| The observable container state after up (running status, labels, mounts, environment) matches the reference for the observed fixtures. | matches the reference | 1 scenario | Backed by the declarative chan-container-state cases — case-state-single-container, case-state-appport, case-state-mount-variety, case-state-dockerfile-nonroot and… |
| The normalized observable-state diff between deacon and the reference is empty for the observed fixtures. | matches the reference | 7 scenarios | Backed by the declarative chan-container-state cases, which compare the whole normalized container state between the two CLIs: case-state-single-container,… |

### `outdated`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| `outdated` resolves the full extends chain before reporting versions, so a Feature contributed by a parent link appears in the report. | deacon follows the spec, the CLI deviates | 5 scenarios | The pinned reference does not resolve `extends`, so it reports an EMPTY table and exits 0 for a workspace whose only Feature is declared one link up — a silent miss, not… ENFORCEMENT IS NOW DIFFERENTIAL AND SELF-INVALIDATING (2026-08-03). Until now the behavior was pinned on deacon's side alone — a `spec-expectation` case asserts what deacon prints and never runs the reference, and a one-sided pin cannot go stale because it never looks at the other side, so nothing would have noticed the day the reference started resolving `extends`. `case-outdated-extends-chain-features-differential` runs both, with one argv (`--output-format json`, the reference's spelling, which deacon accepts as a visible alias for #389 precisely so a differential can exist), and tolerates the one one-sided report key via `wvr-outdated-extends-chain-features`. Re-measured at oracle 0.87.0 on `fx-outdated-extends-feature`: deacon reports `git:1.3.2` at `current`/`wanted` `1.3.2`, the reference `{"features":{}}`; both exit 0. |
| `outdated` fails with a non-zero exit and a diagnostic naming the file when a `devcontainer-lock.json` is PRESENT but unreadable or invalid; an ABSENT lockfile remains… | documented choice | 2 scenarios | Fixes #406. deacon swallowed the read/validation error at `debug!` and continued as though no lockfile existed. That is not equivalent: the lockfile supplies `current`… Ruled KEEP 2026-08-02. ENFORCEMENT IS NOW DIFFERENTIAL AND SELF-INVALIDATING (2026-08-03): `case-outdated-malformed-lockfile-rejected-differential` runs both CLIs and consumes `wvr-outdated-malformed-lockfile-rejected` on `chan-exit-code.exitCode`, the one path where the difference is addressable. Before it, the record was enforced by nothing — the `spec-expectation` case pins deacon's exit 1 and never runs the reference, so a tolerance that could report stale did not exist (#423). Re-measured at oracle 0.87.0 on `fx-outdated-malformed-lockfile`: deacon exits 1 naming the file and the invalid `resolved`, the reference exits 0 with a full report. |
| `outdated` keys each report entry by the Feature reference the configuration DECLARED, tag included, rather than by the canonical untagged id. | matches the reference | 1 scenario | Fixes #407 divergence 1. deacon previously keyed the report by the CANONICAL untagged id. The concrete harm was a collision: two Features declared at different tags are… |
| `outdated` reports each configured Feature's current, wanted, and latest version, and reports an empty result for a configuration that declares no Features. | matches the reference | 11 scenarios | Assertions pin `current` and `wanted`, which the configuration's exact version reference fixes; `latest` is deliberately unpinned because it is whatever the registry… |

### `ports`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| deacon can auto-forward container ports to the host via a host-side daemon backed by a per-container registry and PID markers; the reference CLI does not model a… | deacon extension | 1 scenario | Deacon-only capability (feature 015-auto-forward-ports). Recorded by ext-auto-forward-ports; backed by case-auto-forward. Uses host-side forwarded_ports.json and… |

### `profiles`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| deacon applies a user-defined profile from settings.json (selected via the global --profile flag or DEACON_PROFILE) to layer default flags and configuration; the… | deacon extension | 1 scenario | Deacon-only capability (feature 017-user-profiles). Recorded by ext-user-profiles; backed by case-user-profiles. Reads {user_data_folder}/settings.json. |

### `read-configuration`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| When a link of an `extends` chain declares a Feature its base already declared at a different version, the later link's version REPLACES the base's entry rather than… | deacon extension | 1 scenario | Fixes #411. A Feature's map key carries its version, so a child bumping an inherited version writes a DIFFERENT key; under the previous key-wise merge both survived and… OPEN QUESTION, carried forward 2026-08-03 and NOT yet measured: whether this override rule is itself spec-supportable after #430. The rule is untouched by #430 — it fires only when one link overlays another — but it now sits on top of a document that may LEGALLY declare one Feature twice (`feature-dependencies.md` §Feature Equality, §Round Stable Sort), so a base declaring two versions and an overlay declaring a third replaces the FIRST match and silently keeps the second. `extends` is a deacon extension with no reference equivalent, so this cannot be settled by measuring the oracle; it needs its own argument from the spec's merge and equality text. Recorded here rather than as an issue because [#411](https://github.com/get2knowio/deacon/issues/411) is closed and this is a question about the rule it landed, not a defect in it. |
| A single configuration document whose `features` map declares one Feature at two versions installs BOTH, in oldest-tag-first order. | matches the reference | 3 scenarios + 1 Docker-backed install test | Closed #430 on 2026-08-03. Recorded until 2026-08-02 as `spec: unspecified` + `intentional-divergence` — deacon REJECTED the document — on the reasoning that one Feature at two versions has no coherent meaning and that nothing broke the tie. `feature-dependencies.md` breaks it in three passages: §Definition: Feature Equality makes OCI equality depend on the manifest DIGEST (two tags are two Features, not one declared twice), §Feature authorship states that "a single Feature may be installed more than once", and §Definition: Round Stable Sort supplies the tie-break, "Compare and sort each Feature from oldest to newest tag". MEASURED against oracle 0.87.0 on `fx-readconfig-duplicate-feature-one-document` (`git:1.3.2` declared before `git:1.3.1`): `read-configuration` output is now byte-identical, and both sides' `--include-features-configuration` resolve `git:1.3.1` then `git:1.3.2` with `consecutiveId` `git_0`/`git_1` — oldest first, against the document's order. The reference's `features resolve-dependencies` agrees, returning `git@sha256:6d953e8c…` (`:1.3.1`) before `git@sha256:63c96e8a…` (`:1.3.2`). Removing the parse-time rejection alone would have replaced a loud error with a SILENT DROP — Feature identity was version-independent, so the second entry overwrote the first and one `install.sh` ran with exit 0 — which is why the row's evidence includes `integration_up_duplicate_features`, a real `build` asserting both Features' users inside the produced image and their UID order. The retired `wvr-features-duplicate-in-one-document` is deliberately not replaced. |
| deacon's resolved-configuration output omits a property the author wrote as `null`, reporting it identically to one the author left out, while the reference echoes the… | documented choice | 2 scenarios | Surfaced by the 024 US5 de-suppression as a THREE-state collapse (authored null, authored empty, omitted); #398 closed the empty half and this record was renamed from… ADJUDICATED 2026-08-03: the residual is not owed — the reference's null-echo is whole-document verbatim leniency (every property, measured), the same axis as the rejection family; `wvr-readconfig-authored-empty-omitted` kept as a documented choice, superseding its 'fix deacon' ruling whose fixable half #398 closed. #398 closed with this ruling. |
| A document is a Compose configuration only when BOTH `dockerComposeFile` and `service` are present; one naming a Compose file without `service` resolves as single-container. | deacon follows the spec, the CLI deviates | no scenario yet — pinned by `test_uses_compose_is_presence_not_emptiness` and the measurement on #399 | Measured at oracle 0.87.0 across nine input shapes (#399, recorded in `config.rs`'s doc-comments by PR #447): with `service` present, `""`/`[]`/`[""]` produce byte-identical read-configuration output on both sides — emptiness is not the axis. The reference keys Compose-ness off `dockerComposeFile` key-presence alone (including an authored `null`) and then fails on what it accepted ('is a directory'; "Service 'undefined' not found"); the spec's own text (§'Docker Compose based': 'The required parameters are: dockerComposeFile … service') requires both, which is deacon's test. ADJUDICATED 2026-08-03: #399 closed as the reference's deviation, not work deacon owes. |
| An explicit --config pointing at a file that does not exist is rejected (no silent discovery fallback). | matches the reference | 2 scenarios | both-reject agreement case: both CLIs reject an explicit --config that does not exist. Asserted directly by case-errors-decl-bad-config-path. The migrated… |
| Reading a well-formed devcontainer.json exits 0 and emits the resolved configuration as a single JSON document. | matches the reference | 10 scenarios |  |
| Both the `configuration` and `mergedConfiguration` documents carry `configFilePath`, an absolute VS Code URI object (`$mid`, `fsPath`, `path`, `scheme`) naming the… | matches the reference | 4 scenarios | The field is a consumer-facing contract, not spec prose: a tool reading `read-configuration` output already knows how to unmarshal a VS Code URI and knows nothing about… |
| Configuration discovery searches exactly the three locations the spec names — `.devcontainer/devcontainer.json`, `.devcontainer.json`, and… | deacon follows the spec, the CLI deviates | 4 scenarios | The spec's devcontainer-reference lists all three locations in precedence order. deacon searches all three; the pinned reference v0.87.0 does NOT search the… |
| A JSON object with a duplicate top-level key is accepted with last-wins semantics, matching the reference after pruning. | matches the reference | 1 scenario | both-accept agreement case: both CLIs accept duplicate top-level JSON keys with last-wins semantics. Asserted directly by its declarative case. The migrated… |
| A cyclic extends chain (a -> b -> a) is detected and rejected during read-configuration; the reference CLI v0.87.0 does not resolve extends and accepts. | deacon extension | 1 scenario | extends is the in-flight proposal devcontainers/spec#22 (unspecified in the pinned spec). deacon resolves eagerly and detects the loop; the reference echoes extends… |
| read-configuration --include-merged-configuration resolves the full extends chain (base image, merged containerEnv and forwardPorts); the reference CLI v0.87.0 does not… | deacon extension | 7 scenarios | Ahead-of-spec deacon capability (devcontainers/spec#22, issue #297). Characterized by ext-extends-resolution and backed by case-tier1-extends-child. See… |
| An extends chain pointing at a nonexistent target file is rejected during read-configuration; the reference CLI v0.87.0 does not resolve extends and accepts. | deacon extension | 1 scenario | extends is the in-flight proposal devcontainers/spec#22 (unspecified in the pinned spec). deacon resolves eagerly and errors on the missing target; the reference echoes… |
| `--include-features-configuration` reports the resolved Feature set in install order, and a Feature supplied by `--additional-features` joins that resolution rather than… | matches the reference | 4 scenarios | Re-adjudicated at #439. This row read "deacon follows the spec, the CLI deviates", on the argument that the reference "additionally reports cache paths and consecutive ids that are process-local". The pinned spec (`spec/113500f4/`) mentions `featuresConfiguration` / `featureSets` / `dstFolder` **zero** times, so there was no spec shape to follow and the reference was the only authority on a surface users consume — which made the difference deacon's, not the reference's. Measured over all 11 Feature-bearing tier-1 workspaces against the pin: deacon omitted `dstFolder`, `internalVersion`, `computedDigest` and thirteen `features[]` keys (`version`, `name`, `description`, `documentationURL`, `installsAfter`, `capAdd`, `securityOpt`, `entrypoint`, `dependsOn`, `value`, `included`, `cachePath`, `consecutiveId`), emitted a deacon-only `source`, reported a local Feature's id as the canonical `local:<absolute path>`, and — the substantive defect — put the user's resolved option VALUES in `options`, where the reference puts the option SCHEMA and the values in `value`. All closed; the two documents now differ only in the staging directory each side would use, which `feature_staging_dir_token` rewrites to one token on both sides. |
| `featuresConfiguration` is reported only when Feature resolution produced at least one Feature set; a configuration declaring no Features carries no such key, under… | matches the reference | 2 scenarios | deacon reported `{"featureSets": []}`, which reads as "resolution ran and produced none" where the reference says nothing at all. The value is still computed internally… |
| A devcontainer.json with a hard JSONC syntax error is rejected at parse rather than silently recovered; the reference CLI's recovering parser drops the broken property… | documented choice | 7 scenarios | deacon fails fast (constitution IV, no silent fallbacks); the reference is lenient at read-configuration. Backed by case-errors-malformed-json and wvr-malformed-json. |
| deacon's `mergedConfiguration` omits the computed-empty properties the reference synthesizes there — `containerEnv`, `remoteEnv`, `portsAttributes` and `hostRequirements`. | documented choice | 1 scenario | Made VISIBLE by #398, not caused by it. `mergedConfiguration` is SYNTHESIZED rather than echoed, so unlike `configuration` an empty value there is a computed default and not authorship information, which is why `drop_absent_optional` still runs on BOTH sides of that one block. Re-measured against oracle 0.87.0 at #423 on `fx-readconfig-merged-computed-empties`: the reference emits `containerEnv: {}`, `remoteEnv: {}`, `portsAttributes: {}` and `hostRequirements: {cpus: null, memory: "-Infinity", storage: "-Infinity"}`; deacon emits none of the four. The difference still reproduces. WHAT IS ACTUALLY OBSERVABLE IS ONE PROPERTY, NOT FOUR, and the scenario's tolerance is scoped to it: the reference's three empty objects and deacon's three absences both normalize to "absent" under `drop_absent_optional`, so those three AGREE and a tolerance naming them would be stale the day it was written. Only `hostRequirements` survives the rule — its value is a populated object, the `Math.max()`-over-an-empty-set artifact — so `chan-structured-output.mergedConfiguration.hostRequirements` is the single diverging path. Until #423 this row read "allowlist only" while the record it cited did not exist: #435 folded the waiver files into `parity/ALLOWLIST.json`, whose loader rejects any record no case references, so the four unreferenced waivers were dropped and this behavior was left with neither a scenario nor an allowlist entry. `case-readconfig-merged-computed-empties` is its first scenario and restores `wvr-readconfig-merged-computed-empties` (rationale and `unadjudicated` ruling verbatim — #423 wired enforcement, it did not adjudicate). ADJUDICATED 2026-08-03 and KEPT: the merge-logic table is silent on zero-contribution emission, and the reference's `-Infinity` is invalid by the spec's own memory format — reproducing it would adopt a bug. |
| read-configuration --include-merged-configuration over the tier1 corpus emits a merged configuration that matches the reference after normalization. | matches the reference | 24 scenarios | Backed by the 24 declarative merged-mode variants case-merged-decl-* (023 T039). The 11 of those that declare Features were the nightly's largest red group, every one diverging at `featuresConfiguration.{dstFolder,featureSets}` — deacon's own gap, closed at #439 (see the `--include-features-configuration` row for the measured field inventory). The carve-out this row used to carry was that gap; with it closed the whole group compares equal against the pin, so there is nothing left to except. |
| `mergedConfiguration` reports all five plural lifecycle hook arrays (`onCreateCommands`, `updateContentCommands`, `postCreateCommands`, `postStartCommands`,… | matches the reference | 2 scenarios | The asymmetry is the whole content of this behavior and is measured, not assumed: on a config declaring three of the five hooks the reference emitted… |
| A workspace folder with no devcontainer configuration at all is rejected rather than resolved to an invented empty config. | matches the reference | 2 scenarios | both-reject agreement case: both CLIs reject a workspace with no discoverable configuration. Asserted directly by its declarative case. The migrated wvr-missing-config… |
| A `portsAttributes` / `otherPortsAttributes` entry reports the keys the author wrote and no others; an unset attribute is absent rather than explicitly null. | matches the reference | 2 scenarios | deacon serialized the whole `PortAttributes` struct, so a one-key entry came back with six extra explicit nulls and diverged on six paths at once. Distinct from the… |
| Variable substitution reaches the object-shaped fields that carry user templates — `build.args`, `containerEnv`, `mounts`, and `customizations` — and a `${localEnv:VAR}`… | matches the reference | 1 scenario | `customizations` was the field actually missed once (#312), which is why it is named in the statement rather than left to 'object-shaped fields'. Both CLIs were observed… |
| Reading real-world devcontainer.json configurations from the tier1 corpus produces resolved configurations that match the reference after normalization, except for the… | deacon follows the spec, the CLI deviates | 24 scenarios | Backed by case-tier1-corpus (parity_corpus_tier1 over the tier1 corpus) and by the 24 declarative per-workspace variants case-tier1-decl-* (023 T038). Retiring the… |
| Unknown / forward-compatible top-level fields are accepted and preserved verbatim in read-configuration output, matching the reference's fidelity. | matches the reference | 2 scenarios | both-accept agreement case; guards against silently dropping unmodeled fields (spec extensibility model). Asserted directly by its declarative case. The migrated… |
| A modelled field whose value is outside the schema's closed enum (for example `userEnvProbe: telepathy`) is rejected during configuration resolution, with a diagnostic… | deacon follows the spec, the CLI deviates | 4 scenarios | The pinned schema declares the enum closed, so a value outside it is not a valid configuration and deacon rejects it (constitution IV). The reference's… |
| The reported `workspace.workspaceFolder` is the automatic source-code mount location for THIS workspace: when the workspace folder is a subdirectory of the mounted root,… | matches the reference | 2 scenarios | devcontainerjson-reference.md defines the `workspaceFolder` default as "the automatic source code mount location", which for a workspace inside a git repository is the… |
| The `workspace` section carries exactly `workspaceFolder` and the optional `workspaceMount`, and no other field. | matches the reference | 3 scenarios | The output envelope is a reference-CLI contract, not a spec-defined shape, so `spec` is `unspecified` and the decision is to match the reference rather than to follow… |
| A `features` value that is a bare string instead of an object is rejected (type-strict, matching the schema shape); the reference keeps the raw JSON and accepts. | deacon follows the spec, the CLI deviates | 3 scenarios | The schema declares `features` an object; a bare string is not a valid configuration, and deacon enforces the declared shape (constitution IV) as it already did for the… |
| A `forwardPorts` value that is a bare string instead of an array is rejected (typed deserialization); the reference keeps the raw JSON and accepts. | deacon follows the spec, the CLI deviates | 4 scenarios | The schema declares `forwardPorts` an array; a bare string is not a valid configuration, and deacon's typed deserialization rejects it. The reference keeps the raw JSON… |
| `containerEnv` values carrying embedded double quotes, a literal `$`, a backslash, leading and trailing runs of whitespace, or a whole shell command survive configuration resolution byte for byte. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. Each of those characters has broken a shell-quoting path upstream at some point, which is why `src/test/configs/image-metadata-containerEnv` exists there; running it differentially asks the same question of deacon. Measured at oracle 0.87.0 on `fx-upstream-containerenv-escaping`: byte-identical. |
| `$$`-escaped `${…}` placeholders in `containerEnv` are reported literally, and a Compose service that sets two of the same names in its own `environment:` block does not change what the configuration read reports. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The escape half of the same question, on the Compose shape (`src/test/configs/image-containerEnv-issue`, upstream's regression fixture for ROS-style `$${severity}` values). A read that consulted Compose's own `environment:` would report `Ros from compose` for `ROSCONSOLE_FORMAT`; neither side does. Measured at oracle 0.87.0 on `fx-upstream-containerenv-dollar-escape`: byte-identical. |
| `remoteEnv` may reference a container variable the configuration itself creates — `runArgs: ["-e", "TEST_CE=…"]` read back as `${containerEnv:TEST_CE}` — and both CLIs defer `${containerEnv:*}` to the running container rather than resolving it eagerly against the image. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The third source in one document, alongside `${localEnv:PATH}` and `${containerEnv:PATH}`. It is the one that separates the two plausible implementations: a CLI resolving `${containerEnv:*}` against the IMAGE finds nothing for a name `runArgs` has not injected yet. Measured at oracle 0.87.0 on `fx-upstream-runargs-env-subst`: byte-identical, both reporting the template unresolved. |
| A `mounts` entry in the OBJECT form with no `source` — the anonymous-volume shape — parses and is reported as authored, including a JSONC trailing comma inside the object. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. Every mount fixture in this suite before it used the STRING form, so the object form's parse, its optional `source` and JSONC tolerance inside a NESTED object were all unexercised. Measured at oracle 0.87.0 on `fx-upstream-mount-object-anonymous-volume`: byte-identical. |
| `initializeCommand` in the OBJECT (parallel) form parses and is reported as authored. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The suite covered the string and array forms of the host-side hook and the object form of the CONTAINER-side hooks, but not this combination — which matters to deacon specifically, because `initializeCommand` is the one hook that runs on the developer's host and therefore the one the workspace-trust gate stands in front of. The operation is a configuration read, so nothing executes on either side and the case measures the parse alone. Measured at oracle 0.87.0 on `fx-upstream-initialize-command-object`. |
| A cycle among `installsAfter` (SOFT) dependency edges is rejected rather than resolved by dropping an edge. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. Genuinely open rather than a restatement of the `dependsOn` cycle case the suite already had: `installsAfter` is a mere ordering preference, so an implementation could plausibly break the cycle and install anyway. `feature-dependencies.md` §(B3) says otherwise — "If there is ever a round where no elements are added to `installationOrder`, the algorithm should terminate and return an error" — and both sides exit 1 on `fx-upstream-installsafter-circular`. |
| `overrideFeatureInstallOrder` assigns round priority to Features pulled in TRANSITIVELY, not only to the ones the configuration declares. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The configuration declares `./a` alone and the override names `./c` and `./d`, neither of which is a user Feature — so an implementation that applies the override only to declared Features does nothing here and still exits 0. Upstream pins the answer as `./c`, `./d`, `./b`, `./a`; measured at oracle 0.87.0 on `fx-upstream-override-order-intermediate`, both sides produce exactly that, with identical options and `consecutiveId`s. |
| `overrideFeatureInstallOrder`'s round priority holds back an independent Feature that a naive round-based sort would commit in the first round. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. `./c` has no dependencies, so it is eligible in round one alongside `./a`; the override's job is to keep it last. §(B3) commits only the nodes sharing a round's MAXIMUM `roundPriority` and returns the rest to the worklist. Upstream pins `./a`, `./b`, `./c`; measured at oracle 0.87.0 on `fx-upstream-override-order-round-priority`, both sides agree. |
| A Feature carrying BOTH a `dependsOn` and an `installsAfter` edge installs the hard dependency — pulling it in even though the configuration never named it — and drops the soft edge whose target nothing declares. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. Crossing the two edge kinds' semantics produces either a spurious install or a spurious error, and both are silent-looking failures. Measured at oracle 0.87.0 on `fx-upstream-dependson-installsafter-local`: both sides install `./c` then `./a`, and neither installs `./b`. |
| An explicit `--config` naming a devcontainer.json nested BELOW `.devcontainer/` wins over the discoverable root config, and `build.dockerfile` resolves against the named config's directory rather than the workspace root. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. Two things have to be right at once, and the fixture is built so that getting the second wrong still exits 0 — the subfolder has its OWN Dockerfile, so resolving against the workspace root silently builds a different image. Measured at oracle 0.87.0 on `fx-upstream-subfolder-config`: byte-identical. |
| The pre-`customizations` VS Code properties declared by a FEATURE (`extensions`, `settings` in `devcontainer-feature.json`) are migrated into `customizations.vscode` on the resolved Feature. | matches the reference | 2 scenarios | Was [#487](https://github.com/get2knowio/deacon/issues/487), closed 2026-08-05: deacon modeled neither legacy key and DROPPED both, so a consumer of `featuresConfiguration` could not recover what the Feature author declared from any location — the inverse of `bhv-readconfig-unknown-field-preserved` applied to `FeatureMetadata`. The migration now happens at the single metadata-parse chokepoint (`parse_feature_metadata`), before typing, so every consumer sees one shape: `featuresConfiguration`, the `devcontainer.metadata` label and lifecycle merging alike. The merge rules were read off the reference's own `Ob()` and are reproduced exactly — legacy `extensions` are APPENDED after any already-authored `customizations.vscode.extensions`, legacy `settings` are merged UNDER them (an authored key wins), and the legacy keys are removed. Measured at oracle 0.87.0 on `fx-upstream-local-feature-inside-devcontainer` and `…-outside-devcontainer`: every resolved Feature object is byte-identical to the reference's modulo the `cachePath` normalization, and an `up` on a Feature declaring the legacy pair stamps the migrated block on the container's `devcontainer.metadata` label. This is the FEATURE-level twin of the CONFIGURATION-level question ([#486](https://github.com/get2knowio/deacon/issues/486)), which was ruled a fix on 2026-08-05 and now shares this implementation — see the row below. Backed by `case-readconfig-upstream-local-feature-inside-devcontainer` and `…-config-at-workspace-root`, which now also prove the claim they were WRITTEN for — that a local Feature path resolves against the folder containing the active `devcontainer.json`. |
| A local Feature whose source resolves OUTSIDE the `.devcontainer/` folder is rejected. | matches the reference | 1 scenario | Was [#488](https://github.com/get2knowio/deacon/issues/488), closed 2026-08-05: deacon ACCEPTED a document the specification forbids — the fail-fast direction of constitution IV, and the inverse of every other characterized read-configuration divergence here. `devcontainer-features-distribution.md` §Locally Referenced Features: "A local Feature's source code **must** be contained within a sub-folder of the `.devcontainer/ folder`." The predicate is now one function, `core::features::resolve_local_feature_dir`, called from every local-Feature ingress (`read-configuration`, the shared resolver and its `dependsOn` closure, `up`/`build` staging and its `dependsOn` closure): the path resolves against the ACTIVE config's directory, then the canonicalized result must sit under the canonicalized `<workspace folder>/.devcontainer` — the anchor the reference uses, which is the workspace root and NOT the config's own folder. That distinction is what keeps a config at `.devcontainer/<subfolder>/devcontainer.json` legal. Measured at oracle 0.87.0 on `fx-upstream-local-feature-missing-folder` (no `.devcontainer` folder at all, Features under `./local-features/`): both sides now exit 1. The two legal anchorings — config inside `.devcontainer/` naming `./localFeatureA`, and config at the workspace root naming `./.devcontainer/localFeatureA` — stay green on the sibling cases, which is what proves the fix did not over-correct. |
| A local Feature referenced by ABSOLUTE path is rejected. | matches the reference | 1 scenario | Was [#495](https://github.com/get2knowio/deacon/issues/495), ruled and closed 2026-08-05 on spec authority. `devcontainer-features-distribution.md` §Locally Referenced Features: "A local Feature may **not** be referenced by absolute path." This is the OTHER clause of the section [#488](https://github.com/get2knowio/deacon/issues/488) implemented, and closing it REMOVES a capability deacon added deliberately in [#126](https://github.com/get2knowio/deacon/issues/126) — absolute paths were accepted at every local-Feature dispatch site, for parity with `read-configuration`'s own dispatch — so it is a strictness ruling rather than a bug fix. #488's containment predicate narrowed the divergence but left absolute paths landing INSIDE `.devcontainer/` accepted. MEASURED at oracle 0.87.0: the reference exits 1 with `An Absolute path to a local feature is not allowed.` from an unconditional `path.isAbsolute(userFeatureId)` that fires before it computes the containment root and before it touches the filesystem; deacon exited 0 and resolved the Feature. The rejection now sits at the same single chokepoint, `core::features::resolve_local_feature_dir`, ahead of the containment check, so `up`, `build`, `read-configuration` and the `dependsOn` closure are covered by one branch. The reference also rejects an absolute path in `installsAfter` and `overrideFeatureInstallOrder` (both measured); deacon matched on the latter with a misleading message and SILENTLY NON-MATCHED on the former, and both now name the real cause. deacon's own message is precise rather than a copy of the reference's — it names the offending path, cites the spec clause, and gives the copy-pasteable `./`-relative migration. Also measured and deliberately NOT affected: `${localWorkspaceFolder}/.devcontainer/x` — neither side substitutes `features` map keys, so it stays literal, is not lexically absolute, and falls through to OCI parsing where both sides exit 1. Backed by `case-readconfig-local-feature-absolute-path`, whose first draft declared the absolute path in `devcontainer.json` and AGREED with the rejection compiled out (the temp workspace cannot appear in fixture file contents, so both sides failed on the missing directory instead); it was rewritten to pass the path through `--additional-features`, where `${WORKSPACE}` resolves and the Feature really exists, and then diverges on both channels when the check is removed. |
| A `dependsOn` key or `installsAfter` entry naming a Feature by a BARE id is canonicalized at ingress exactly as a `features` map key is — mapped when it is one of the deprecated v1 ids, rejected otherwise, including when it happens to match a declared local Feature's metadata `id`. | matches the reference | 2 scenarios | Was [#505](https://github.com/get2knowio/deacon/issues/505), measured and closed 2026-08-05. `feature-dependencies.md` gives both properties "the same syntax as the `features` object", and the reference enforces that literally: every non-path entry goes through `getBackwardCompatibleFeatureId` during feature-set assembly, so a bare single-segment id is the deprecated v1 (GitHub Release era) form and only [#491](https://github.com/get2knowio/deacon/issues/491)'s eighteen-id table survives. Surfaced while fixing [#493](https://github.com/get2knowio/deacon/issues/493): two examples named local `dependsOn`/`installsAfter` targets by the sibling's metadata id, which [#488](https://github.com/get2knowio/deacon/issues/488)'s containment failure had been masking. MEASURED at oracle 0.87.0 on four minimal `.devcontainer/`-contained workspaces, and the four probes together are what make this ONE rule rather than a judgment call. (1) `dependsOn: {"base": {}}` with `./base` a sibling — BOTH sides already exited 1 with `Legacy feature 'base' not supported`, because #491/#497 had routed `dependsOn` keys through `canonicalize_user_feature_id` at ingress; nothing owed. (2) The same reference in `installsAfter`, with `./base` DECLARED — the reference exits 1, deacon exited 0 and actively ordered `base` before `app`, because deacon's dependency matcher resolves a reference by canonical id, source **or metadata-id alias**, a form neither the spec nor the reference has. (3) `installsAfter: ["nope-not-here"]`, matching nothing — the reference exits 1, deacon exited 0; this is the half that hid the divergence, since `installsAfter` is the SOFT edge and an unresolved entry is ordinary and extremely common (every real Feature's `installsAfter` on `common-utils` is one), so an ILLEGAL entry was indistinguishable from a legitimate non-match — the same failure mode [#495](https://github.com/get2knowio/deacon/issues/495) found for absolute paths in this exact field. (4) `dependsOn: {"terraform": {}}` and `installsAfter: ["terraform"]` — BOTH sides exit 0, the former auto-mapping to `ghcr.io/devcontainers/features/terraform:1` (and pulling in its own `github-cli` dependency identically on both), the latter canonicalizing, matching nothing and being soft-skipped. Probe 4 is why the rule is "canonicalize", not "reject every bare id", and why the fix is the SAME call the `features`-map ingress already makes rather than a second rule for dependency references: `core::features::validate_feature_dependency_references` runs `canonicalize_user_feature_id` over both properties at the three sites that build a Feature set from a configuration, ahead of the ordering pass. The matcher's metadata-id alias is deliberately LEFT IN PLACE and is now unreachable from a configuration — it still serves `overrideFeatureInstallOrder`, a separate surface this measurement does not touch. deacon's message names the referring Feature and the property, then carries the reference's own diagnostic verbatim, including its `prepend your Feature name with "./"` migration. Backed by `case-readconfig-installsafter-bare-metadata-id` (target declared, so the alias resolved and enforcement is the only thing between deacon and exit 0) and `case-readconfig-installsafter-unmatched-bare-id` (target absent, pinning the soft-skip path separately); both diverge on exit code and structured output when the validation is compiled out. Probe 4's accept-direction resolves nothing and would only add a registry fetch, so it is pinned hermetically by a unit test instead of a case. |
| Local Features are identified by contents AND options, so two dependents requesting the same local Feature with different options get their own instances. | matches the reference | 1 scenario | Was [#489](https://github.com/get2knowio/deacon/issues/489), closed 2026-08-05: deacon COLLAPSED them onto one node carrying the CONFIGURATION's option set, so the four dependents that asked for `./b` with their own options silently received someone else's. `feature-dependencies.md` §Definition: Feature Equality — equal only if the Features "point to the same exact contents **and are executed with the same options**"; §(B1) skips a node only if "the **exact** Feature … has already been added". Measured at oracle 0.87.0 on `fx-upstream-dependson-local-with-options`, where `./b` is requested with five distinct option sets: NINE installation nodes (`b_0`…`b_4`, then `./d`, `./e`, `./c`, `./a`), and deacon now produces the same nine with identical option sets and `consecutiveId`s. The option set is now half the Feature's identity everywhere it is decided — the `dependsOn` accumulator, the dependency graph's nodes and edges, and §Round Stable Sort's third comparison — which is the equality rule #430 settled for duplicate OCI tags, one level deeper. Identical option sets still dedup (measured: two nodes, not three), and #430's rule that an unpinned hard dependency is satisfied by the user's pinned declaration is unchanged. Proven end-to-end by a Docker build asserting one marker per instance inside the produced image, not merely by the JSON. |
| A bare (v1-era) Feature id either resolves the way the reference resolves it, or is rejected at ingress as a deprecated identifier. | matches the reference | 1 scenario | Was an open nonconformance ([#491](https://github.com/get2knowio/deacon/issues/491)): deacon did NEITHER — it derived `ghcr.io/devcontainers/<id>`, one path segment short, and surfaced the resulting 401 as an authentication failure against a repository that does not exist, so NO bare id resolved to anything published. The reference's rule was read off the oracle rather than inferred: `getBackwardCompatibleFeatureId` is a FIXED TABLE of 18 deprecated ids plus two renames (`golang`→`go`, `common`→`common-utils`), each mapped to `ghcr.io/devcontainers/features/<name>:1`; anything else bare is rejected with `Legacy feature '<id>' not supported`. Measured at 0.87.0, the mapping is applied UNCONDITIONALLY — `--skip-feature-auto-mapping` does not gate it, which a run with and without the flag confirmed produced identical output. `core::feature_ref::canonicalize_user_feature_id` now implements exactly that and is applied at every Feature ingress (`read-configuration`, `up`/`build` feature staging, the `dependsOn` closure, `upgrade`); a bare id outside the table now fails at ingress with the reference's own guidance instead of a misleading 401. The reference reports the id the CONFIGURATION wrote — `userFeatureId` and `userFeatureIdWithoutVersion` both stay `terraform` — so the original is carried through to the report rather than the resolved reference. Verified on `fx-upstream-feature-digest-and-v1-ids`: exit 0, `terraform`/`node` resolving to `ghcr.io/devcontainers/features/<id>` at version `1`, and every `featureSet` byte-identical to the reference's. Closing it exposed one residual on the SAME case that the exit-1 had been hiding — a digest-pinned reference carried `featureRef.tag` where the reference emits `featureRef.digest` — now keyed on the form the id used, since a tag cannot contain the digest algorithm's `:`. `case-readconfig-upstream-feature-v1-shorthand-ids` is green against the pinned oracle with no tolerance. |
| `--skip-feature-auto-mapping` is accepted and changes nothing. | matches the reference | 1 scenario | Was a flag-surface divergence ([#498](https://github.com/get2knowio/deacon/issues/498)): the two CLIs spelled the same flag and meant different things. MEASURED at oracle 0.87.0 rather than inferred from the name. The reference declares the flag on `up`, `build`, `read-configuration`, `exec` and `run-user-commands`, all hidden and all self-described as "Temporary option for testing." — and never reads it. The value is threaded into the internal options object and dropped there: the one gate that would consume it is `processFeatureIdentifier`'s sixth parameter, which suppresses `getBackwardCompatibleFeatureId`, and `generateFeaturesConfig` calls that function with FIVE arguments. All thirteen occurrences of the identifier in the bundle are writes or forwards; there is no read. Two runs confirmed the consequence: a config declaring `terraform` and `golang` produced identical output with and without the flag — deprecation warnings and the `golang`→`go` rename included, differing only in a timestamped temp path — and a config passing `--additional-features` resolved the overlay Feature either way. deacon's flag meant "drop `--additional-features`" (007 FR-004, read off the flag's NAME), and in `read-configuration` it additionally suppressed the `"features": {"x": "1.2"}` shorthand's expansion to `{version: "1.2"}` — a second invention the reference performs unconditionally. Both gates are gone. The flag is now inert on all three subcommands and emits a `warn!` saying so, because an accepted-but-ignored flag that says nothing is a silent fallback. The behavior was RENAMED, not removed: `--ignore-additional-features` carries it, so no capability was lost. `case-readconfig-skip-feature-auto-mapping-inert` is the same fixture and argv as `case-readconfig-overlay-dockerfile-depends` with the flag appended, and it was watched to fail — under the old semantics it resolves one featureSet instead of two. |
| The pre-`customizations` VS Code properties declared by the CONFIGURATION (`settings`, `extensions`, `devPort` at the top level of `devcontainer.json`) are migrated into `customizations.vscode`. | matches the reference | 1 scenario | Was [#486](https://github.com/get2knowio/deacon/issues/486), filed for a ruling rather than as a defect and ruled a FIX on 2026-08-05. deacon echoed all three where they were authored and synthesized no `customizations` block at all, reaching them through the unmodeled `#[serde(flatten)] extra` passthrough, whose contract is to preserve rather than rewrite. The pinned spec mandates no migration — `supporting-tools.md` says only where such properties belong, `devcontainerjson-reference.md` models none of the three, and `devPort` appears nowhere in `parity/spec/113500f4/` — so this is the standing rule that on a spec-silent OUTPUT surface the reference is the authority, plus an internal consistency argument: the same properties on a FEATURE were already migrated ([#487](https://github.com/get2knowio/deacon/issues/487)), and migrating on one document while echoing on the other is worse than either answer applied to both. There is now ONE migration in the codebase, called from the configuration-parse chokepoint (`ConfigLoader::load_from_path`) and the metadata-parse chokepoint (`parse_feature_metadata`) alike, before typing in both — so `read-configuration`, the merged configuration, the `devcontainer.metadata` label and every document of an extends chain see the one shape. The rules are the reference's own `Rb()`: legacy `extensions` APPENDED after any authored `customizations.vscode.extensions`, legacy `settings` merged UNDER them, the legacy keys removed, and no empty `customizations` created for a document that authors none of them. `devPort` is the one property `Rb()` reads and the Feature reader `Ob()` does not, and it moves only when `customizations.vscode.devPort` is absent — an authored one wins and the top-level key then STAYS, measured rather than assumed. Verified at oracle 0.87.0 on `fx-upstream-devport-userenvprobe`: the reference maps `devPort` to `customizations.vscode.devPort` and the whole output document is now byte-identical to it. `case-readconfig-upstream-legacy-vscode-properties` is green with no tolerance. |
| `featuresConfiguration…sourceInformation.userFeatureId` reports the Feature id the CONFIGURATION wrote, for every reference form, and `userFeatureIdWithoutVersion` is that id with its version component stripped. | matches the reference | 2 scenarios | Was [#490](https://github.com/get2knowio/deacon/issues/490), filed for a ruling rather than as a defect and ruled a FIX on 2026-08-05. A Feature authored `ghcr.io/devcontainers/features/git` was reported as `…/git:latest`: a RESOLVED value in a field whose contract is the DECLARED one, the same class as the `outdated` report-key divergence [#407](https://github.com/get2knowio/deacon/issues/407) closed but on a surface that fix did not reach. `parity/spec/113500f4/` says nothing about `userFeatureId`, so the reference is the authority on its own output contract; the deciding argument was consistency, deacon having already committed to the declared reference on `outdated` (#407) and for bare v1 ids (#491). The authored id is now carried for EVERY Feature through the one canonical → authored map #491 introduced — the same mechanism `up`'s staging path already keeps for the lockfile, which upstream `generateLockfile` also keys by the authored form — and `userFeatureIdWithoutVersion` is derived from it by the reference's own `/[:@][^/]*$/` strip (leftmost-first, so a `:tag@sha256:…` loses both) instead of being re-derived from the resolved reference. Canonicalization stays internal to resolution; only the report changed. Verified at oracle 0.87.0 on `fx-upstream-feature-no-version-tag` — both sides report `ghcr.io/devcontainers/features/git` on both fields, with the same `computedDigest` and `consecutiveId` as before — and on `fx-upstream-feature-digest-and-v1-ids`, where a bare v1 id, a digest-pinned id and a tagged id all still agree. `case-readconfig-upstream-feature-no-version-tag` and `case-readconfig-upstream-feature-v1-shorthand-ids` are green with no tolerance. |

### `run-user-commands`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| A lifecycle hook that exits non-zero fails the run rather than being reported as a successful setup. | deacon follows the spec, the CLI deviates | 7 scenarios | The failing hook arrives as a command-line override so the preceding `up` can succeed: a hook failure during creation is a different case, and conflating the two would… |
| Lifecycle hooks run in spec order — onCreate, updateContent, postCreate, postStart — and a Feature-contributed hook runs exactly once alongside the configuration's own. | matches the reference | 6 scenarios | Observed by having each hook append its own name to a file inside the container, so the file IS the order. `grep -c` rather than a presence check pins the Feature hook's… |
| The running container's `devcontainer.metadata` contributes `remoteUser`, `remoteEnv` and lifecycle hooks to `run-user-commands`, as it already does to `up` and `exec`. | matches the reference | 1 scenario | Fixes #405. (The SOURCE of that label, and the composition it takes part in, were both corrected by [#527](https://github.com/get2knowio/deacon/issues/527) — see the row below. What this row records is unaffected: an image-contributed hook still reaches `run-user-commands`, because `up` stamps the image's entries onto the container it creates.) Measured against the pin on a base image whose label carried all three and a devcontainer.json that declared none: the reference ran the image's `postCreateCommand`, as `metauser`, with `META_ENV` set; deacon ran nothing, as `root`, with neither — the exit code was 0 on both sides, so only the marker file showed it. `exec` had done this merge inline since #223 and `run-user-commands` loaded the workspace config alone, which is the whole asymmetry. The merge is now the one shared `container_metadata::resolve_config_against_container`, and `remoteEnv` is layered over `containerEnv` for the hook environment the way `up` layers it. Backed by `case-run-user-commands-image-metadata-differential` (nightly; its `assertion` also pins deacon's side) and by the Docker-gated `run_user_commands_feature_lifecycle::run_user_commands_honors_image_metadata_user_env_and_hook` on the PR lane. `up`'s COMPOSE path never called the merge either; that was tracked on #448 and is now closed (see the `up` section). |
| `exec` and `run-user-commands` read `devcontainer.metadata` off the CONTAINER inspect, and the container's IDENTITY LABELS pick the composition: on the dev container for this workspace that label is the COMPLETE lifecycle record, so neither the caller's own hooks nor its resolved Features are added; on a foreign or raw-labeled container the label is extra layers beneath both. | matches the reference | 1 Docker-gated test, 1 parity case, 6 hermetic tests | Was an open nonconformance ([#527](https://github.com/get2knowio/deacon/issues/527)), closed 2026-08-06. Found while adjudicating [#524](https://github.com/get2knowio/deacon/issues/524), whose own premise was refuted: `set-up`'s label read was measured CORRECT on all three probe shapes, and the same probe found the INVERSE bug where nobody had looked. The shared `container_metadata::resolve_config_against_container` (#405) called `merge_image_metadata_after_image_ready` on `container.image`, so both consumers read a label off the IMAGE. Measured live at oracle 0.87.0 on a fixture built to make the two sources DISAGREE — an image whose label's `postCreateCommand` writes `image`, and a container created with `docker run --label devcontainer.metadata=…` whose hook writes `container`: the reference wrote `container`, deacon wrote `image`. **The source was only half of it, and the half that was easy to see.** Swapping it alone made every lifecycle hook run TWICE — measured on `fx-ruc-image-feature` after `up --skip-post-create` + `run-user-commands`, where the reference writes one each of `config-onCreate` / `config-updateContent` / `feature-postCreate` / `config-postCreate` / `config-postStart` and deacon wrote two of every one — because `up` stamps the container label as `[…image entries, …one entry per installed Feature, the config entry]` while `run-user-commands` ALSO resolves the Features itself (#137) and ALSO carries the config's own singular hooks (#467/#477). Reading the IMAGE label had never collided only because a feature-extended image inherits nothing but the BASE image's entries. The maintainer ruled for the reference's own architecture rather than a suppression heuristic, and the branch is derived from the bundle, not invented: `Tr` returns `[…labelEntries, SI(config, rG)]` when `Object.keys(pt(idLabels)).every(k => container.Config.Labels[k])`, where `rG` is exactly `["remoteUser","userEnvProbe","remoteEnv"]` — **no lifecycle command**, which is what makes the label the sole source of hooks there — and otherwise `Tt`, which layers `[…labelEntries, …featureEntries, SI(config, SV)]`. `bg` builds `idLabels` as `[devcontainer.local_folder]` plus `devcontainer.config_file` only when the container already carries that key, so the `every` test reduces to a non-empty `devcontainer.local_folder`; values are never compared. Both branches were measured before being implemented, and the second one refutes the obvious guess that the branch tracks how the container was ADDRESSED: a container the reference `up` created runs each hook exactly once even when named with `--container-id` (identity labels present ⇒ complete record), while a raw `docker run --label` container under the same `--container-id` runs the label's hook AND the config's own, label first. One more line of `Tr` is load-bearing and is easy to drop: `if (!labels[RI]) return Tt(…)` — a container with the identity labels but NO metadata label takes the LAYERED branch, without which clearing the caller's hooks would leave a deacon-created container with nothing to run at all. deacon renders the complete-record branch by clearing the caller config's five singular hooks before the fold and reporting `MetadataComposition::suppresses_caller_features()` so `run-user-commands` skips `resolve_features_ordered`; the scalars still last-win to the caller's config, which is the same outcome upstream gets by appending its three-property pick last. Skipping that resolution also skips its fail-fast, and that is correct on this path — the Features are already installed, so what the configuration declares NOW cannot change what runs. `exec` is unaffected by the hook clearing: it runs no lifecycle phase and reads only `remoteUser` / `remoteEnv`. NOT changed: the three remaining image-label reads, each correct for its caller — `up`'s pre-container merge (there is no container yet), `container_metadata_entries` (building the label to STAMP, which needs the base image's own entries), and `read-configuration`'s no-container path (upstream's `inspectDockerImage`, which pulls on demand). The two branches are guarded on different lanes because no single one reaches both: `case-run-user-commands-feature-contributed-hook` (`parity_docker`, gates every PR and the release) pins the complete-record branch end-to-end at `grep -c` == 1, and the Docker-gated `run_user_commands_feature_lifecycle::run_user_commands_reads_metadata_from_the_container_not_the_image` pins the layered branch over a raw-labeled container — which no parity case can express, since `op-up` stamps both label locations identically and masks the difference by construction. Watched to fail at both levels: forcing the layered branch everywhere (the naive source swap) turns `case-run-user-commands-feature-contributed-hook: chan-stdout: Diverge` red again and fails the two complete-record hermetic tests; dropping only the no-label fallback fails `identity_labels_without_a_metadata_label_stay_layered` with `left: CompleteRecord`. |

### `secrets`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| The --secrets-file flag auto-detects and accepts both a flat JSON object and a .env-format KEY=VALUE file; the reference CLI models only JSON and rejects the .env form. | deacon extension | 1 scenario | Intentional Deacon extension (a strict superset). Recorded by ext-secrets-file-env-format; backed by case-secrets-dotenv. See docs/DIFFERENTIATORS.md and… |

### `set-up`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| `set-up --include-merged-configuration` emits `mergedConfiguration` with the collected plural lifecycle arrays, as `read-configuration` does. | matches the reference | 1 scenario | Was an open nonconformance ([#483](https://github.com/get2knowio/deacon/issues/483)), closed 2026-08-05. deacon emitted its flat configuration under a snake_case `merged_configuration` key, with the five lifecycle hooks as SINGULAR fields — three defects in one block, and none of them visible to a test of what set-up RAN, because #477 had already made the EXECUTION correct. Re-measured live at oracle 0.87.0 on a container `up` stamped from a workspace config plus a differently-worded `--config` layered on top: the reference reports `onCreateCommands` `["echo base-onCreate", "echo overlay-onCreate"]` and `postCreateCommands` `["echo base-postCreate", "echo overlay-postCreate"]`, with `updateContentCommands`/`postAttachCommands` as `[]`, `init`/`privileged` as `false`, and a `configFilePath` whose `scheme` is `file` (not the `vscode-fileHost` the same reference uses for `read-configuration`, which keys the scheme off whether the CALLER NAMED the path). deacon emitted `merged_configuration` carrying `onCreateCommand: "echo overlay-onCreate"` — one command per phase, under a key no consumer of the reference's output looks for. It now agrees on every key after normalization; the only elisions are the three synthesized empties (`remoteEnv`, `containerEnv`, `portsAttributes`) that `drop_absent_optional` drops on BOTH sides under the standing `bhv-readconfig-merged-computed-empties-omitted` characterization, so the case declares no tolerance at all. The fix is a routing change, not a second serializer: `set_up.rs` now calls `read_configuration`'s `apply_upstream_merge_shape` + `normalize_merged_configuration_shape` + `insert_config_file_path`, ordering the collected entries exactly as `aggregate_lifecycle_commands` replays them (label layers in label order, `--config` last), so what the block REPORTS and what set-up RUNS are built from one input. Backed by `case-set-up-merged-configuration-shape` — the first parity case to invoke `set-up`, which is why the subcommand joined the harness's `CONSUMER_SUBCOMMANDS`/`CONTAINER_SUBCOMMANDS` vocabulary here — plus three hermetic unit tests on the PR lane that pin the plural arrays, the camelCase key and the `configFilePath` presence rule. Watched to fail: with the product fix reverted and the case left in place, the `docker-shared` differential group reports exactly one divergence, `case-set-up-merged-configuration-shape: chan-structured-output: Diverge`, out of 61 cases. One difference remained on this surface and was NOT this row: the `configuration` block, where deacon folded the container's image metadata in and emitted no `configFilePath`. It was untouched by this change, filed as [#502](https://github.com/get2knowio/deacon/issues/502) and closed on 2026-08-05 with the scenario it was waiting on — see the row below. |
| `set-up --include-configuration` echoes the `--config` document AS AUTHORED, with its `configFilePath`, rather than the metadata-merged configuration set-up resolved. | matches the reference | 1 scenario | Was an open nonconformance ([#502](https://github.com/get2knowio/deacon/issues/502)), closed 2026-08-05. Same authored-vs-resolved contract as #490 and #407: `configuration` reports what the CALLER SUPPLIED, `mergedConfiguration` (the row above) reports what set-up resolved. Measured live at oracle 0.87.0 on a container `up` stamped from a workspace config authoring `remoteUser: root` and `containerEnv: {FROM_BASE: "1"}`, with an overlay `--config` authoring NEITHER: the reference's block is the overlay alone — `name`, `postCreateCommand`, and a `configFilePath` whose `scheme` is `file` — while deacon's carried `remoteUser` and `containerEnv` lifted off the container's `devcontainer.metadata` label, as though the caller had written them, and no `configFilePath` at all. Both shapes now agree key-for-key. The no-`--config` shape was measured on the same oracle and needed no change: both CLIs emit a bare `"configuration": {}`, which falls out of the same rule rather than being special-cased — `config_file_path_value` keys the `file` scheme off whether the caller NAMED the path, and a set-up with no `--config` has no config file to name. The fix is a two-line routing change plus reuse, not a second serializer: `set_up.rs` stopped cloning the merged config into the reported block and now substitutes the authored `--config` document, then calls `read_configuration`'s existing `insert_config_file_path` — the same helper #483 wired into the merged block. Backed by `case-set-up-configuration-authored`, a pure data edit (fixture + case) on the vocabulary #483's case opened, plus two hermetic unit tests on the PR lane pinning the no-fold rule and the caller-named `configFilePath`. Watched to fail: with the product fix reverted and the case left in place, the differential lane reports exactly one failing case, `case-set-up-configuration-authored` — the `jsonSubset` assertion missing `configuration.configFilePath` AND `chan-structured-output: Diverge` on the folded keys — out of 62. One difference remained on this surface and was NOT this row: variable substitution CONTEXT — deacon anchored set-up's `SubstitutionContext` at the process cwd as a stand-in for the absent workspace, so `${localWorkspaceFolder}` and `${devcontainerId}` resolved to cwd-derived values where the reference leaves both literal. It was untouched by this change, filed as [#510](https://github.com/get2knowio/deacon/issues/510) and closed on 2026-08-05 with the scenario it was waiting on — see the row below. |
| `set-up` leaves the workspace-derived variables — `${localWorkspaceFolder}`, `${localWorkspaceFolderBasename}` and `${devcontainerId}` — LITERAL, in the blocks it reports AND in the lifecycle commands it execs, because it takes no `--workspace-folder`. `${localEnv:*}` still resolves. | matches the reference | 1 scenario, 2 hermetic tests | Was an open nonconformance ([#510](https://github.com/get2knowio/deacon/issues/510)), closed 2026-08-05. `set-up` adopts an already-running container and has no `--workspace-folder`, so deacon anchored its `SubstitutionContext` at the PROCESS CWD as a best-effort stand-in and then answered the workspace variables from it — inventing a workspace the caller never named. Measured live at oracle 0.87.0 with an overlay `--config` authoring the probe variables in `remoteEnv` plus a `postCreateCommand` echoing three of them, run from a cwd deliberately outside the fixture: the reference resolves `${localEnv:HOME}` (`/home/vscode`, both sides agree) and leaves `${localWorkspaceFolder}`, `${localWorkspaceFolderBasename}` and `${devcontainerId}` LITERAL; deacon emitted the cwd, the cwd's basename, and a computed 12-char id. `${localWorkspaceFolderBasename}` was not in the issue's table and was found by widening the probe. The EXECUTION surface was measured too and was the same defect, which no test of the reported JSON could see: the reference hands the literal tokens to the container shell, which expands the unset names to empty (`ws=[] base=[] id=[]`), while deacon substituted the cwd INTO the command string it exec'd (`ws=[/tmp/…/scratchpad] base=[scratchpad] id=[55a0e24e02bb]`). After the fix deacon's hook writes `ws=[] base=[] id=[]` byte-for-byte. The fix REUSES the mechanism `${devcontainerId}` already had rather than adding a second one: `resolve_devcontainer_id` (#219) proved the shape — a per-pass gate that makes `resolve_variable` return `None` so the token survives — so a sibling `resolve_workspace_folder_vars` gates the two `localWorkspaceFolder*` variables, and `SubstitutionContext::without_workspace` sets both flags false in one named constructor. That constructor still takes the cwd, but as a MECHANICAL anchor only: `container_lifecycle.rs` roots lifecycle phase markers and the host-side working directory at `local_workspace_folder`, so blanking the field would have broken marker persistence. Naming the constructor `without_workspace` is what makes the absence explicit — the anchor is still there, it is just no longer reachable by any variable, and a test asserts it never leaks into a substituted value. One neighbour was re-measured and changed deliberately: the `containerEnv`-only pass in `container_env_probe.rs` builds a context with an EMPTY `local_workspace_folder`, where resolving those tokens would silently BLANK them; it now sets the same flag false, so a surviving token is preserved rather than emptied. Backed by `case-set-up-substitution-scope`, a pure data edit (fixture + case) on the vocabulary #483 opened, plus two hermetic tests on the PR lane — one on the engine (literals survive while `${localEnv:*}` resolves) and one at the set-up layer asserting the SAME literals in the reported `remoteEnv` and in the `postCreateCommand` string, which is the surface #510 said a JSON-only test would miss. The case observes both surfaces: `chan-structured-output` for the two reported blocks and `chan-file-content` for the hook's artifact, written into a bind mount the base fixture pins at `/work` so it is host-visible. It passes `--container-data-folder` because `op-up` leaves the reference's lifecycle markers in the default folder and a marked phase does not re-run — without it the hook is silently skipped and the execution channel proves nothing, which is a trap any future set-up case that wants to observe execution will hit. Watched to fail: with the product fix reverted and the case left in place, the `docker-shared` differential group reports exactly one failing case, `case-set-up-substitution-scope`, diverging on BOTH declared channels out of 63 — `chan-structured-output` first mismatching at `configuration.remoteEnv.PROBE_LOCAL_WS`, and `chan-file-content` at `setup-substitution.txt`, so the reported blocks and the EXECUTION are each independently pinned. With the fix restored the group is 63/63 green with an empty diverging set. One assertion is written in POST-NORMALIZATION vocabulary and must stay that way: `devcontainer_id_token` rewrites the literal `${devcontainerId}` to `<ID>` inside `configuration`/`mergedConfiguration`, so the absolute claim on `PROBE_DEVCONTAINER_ID` is `<ID>` — and since that same rule maps both a literal token AND a substituted 12-char hex id onto `<ID>`, the differential on that one key cannot pin it, which is why the two un-rewritten `localWorkspaceFolder*` keys and the execution channel carry the sharp claims. One difference remains on this surface and is NOT this row: `${containerWorkspaceFolder}`, which deacon never resolves in the reported blocks (even when the `--config` authors `workspaceFolder`, where the reference emits it) and invents as `/` at exec time when it is unauthored (where the reference leaves it literal). It is a different defect in kind — the value EXISTS when authored, so the fix is to read it from the authored config rather than to stop answering — and it needs a seam in the shared `ContainerLifecycleConfig` to separate the substitution value from the exec cwd, so it was left out and filed as [#513](https://github.com/get2knowio/deacon/issues/513) — closed on 2026-08-06 with the pair of scenarios it was waiting on, see the row below. The case is built blind to it on purpose: its overlay authors no `${containerWorkspaceFolder}`, so this row's three variables are pinned without depending on the unsettled one. |
| `set-up` resolves `${containerWorkspaceFolder}` and `${containerWorkspaceFolderBasename}` from the `--config` document's OWN `workspaceFolder` — in the blocks it reports AND in the lifecycle commands it execs — and leaves both LITERAL when that document omits one, rather than inventing `/`. | matches the reference | 2 scenarios, 2 hermetic tests | Was an open nonconformance ([#513](https://github.com/get2knowio/deacon/issues/513)), closed 2026-08-06. This variable is the one workspace-shaped token that CAN have an answer in `set-up` — which is why it was not fixed by [#510](https://github.com/get2knowio/deacon/issues/510), whose three variables have no answer at all — and deacon was wrong in BOTH directions while disagreeing with itself. Re-measured live at oracle 0.87.0 across all four cells (authored/unauthored × reported-block/hook-exec), with an overlay `--config` authoring both tokens in `remoteEnv` plus a `postCreateCommand` echoing them into a bind-mounted file. AUTHORED `workspaceFolder: /work`: the reference reports `/work` and `work` in both blocks and its hook writes `cwf=[/work] base=[work]`; deacon's hook agreed but its reported blocks emitted the raw `${containerWorkspaceFolder}` — the `SubstitutionContext` #510 introduced carried `container_workspace_folder: None` and nothing ever filled it. UNAUTHORED: the reference leaves both literal on both surfaces, so the container shell expands the unset names and the artifact is `cwf=[] base=[]`; deacon wrote `cwf=[/] base=[]`, an invented `/` reaching the command text from `merged_config.workspace_folder.unwrap_or("/")` — a DEFAULT for the exec cwd, not a value anyone authored. After the fix all four cells agree byte-for-byte. The SOURCE was measured too, and it is the `--config` document ALONE, not the merged configuration: on a container whose `devcontainer.metadata` label authored `workspaceFolder` with an overlay that did not, the reference still left both tokens literal — its config reader seeds the context with `containerWorkspaceFolder: <the config document's raw workspaceFolder>`, undefined when the document omits it. So deacon reads `base_config`, and `case-set-up-container-workspace-folder-unauthored` is built to FAIL a naive fix that reads the merged config. The fix is a seam, not a special case: `ContainerLifecycleConfig` now carries `substitution_workspace_folder: Option<String>` beside the `container_workspace_folder: String` cwd, because the two are different uses of one path and only the cwd is guaranteed to exist — `docker exec -w` needs a path, so set-up's cwd still falls back to `/`, while the substitution value is simply absent. The config field is AUTHORITATIVE over the caller's context (`Some` sets it, `None` clears it), which is what lets `set-up` express absence at all; `up` and `run-user-commands` both have a real workspace and pass `Some` of the same path they pass for the cwd, so they are unchanged by construction — proven by running their Docker binaries, not argued. set-up now reads its substitution value back OFF the context it already built, so the reported blocks and the commands it RUNS can no longer disagree the way they did here. Backed by two parity cases (a pure data edit — fixture + case — on the vocabulary #483 opened): `case-set-up-container-workspace-folder-authored` pins the resolution and `…-unauthored` pins the literal, each observing BOTH surfaces — `chan-structured-output` for the two reported blocks and `chan-file-content` for the hook's artifact — plus two hermetic tests on the PR lane. One drives the SEAM directly through the mock runtime: with `None` the token survives verbatim into the exec'd command and with `Some` it resolves, while `working_dir` is the same path in both runs, which is the separation stated as an assertion. The other is at the set-up LAYER, asserting the same authored/unauthored pair in the reported `remoteEnv` and in the `postCreateCommand` string built from the same context, which is where the two halves used to disagree. Both cases pass `--container-data-folder`, the trap #510 documented: `op-up` leaves the reference's lifecycle markers in the default folder and a marked phase does not re-run, so without a fresh one the hook is silently skipped and the execution channel proves nothing. Watched to fail: with the product fix reverted (the cases and the seam left in place) the `docker-shared` differential group reports exactly two divergences out of 65 cases, and they land on DIFFERENT channels, so each half of the defect is independently pinned — `case-set-up-container-workspace-folder-authored: chan-structured-output: Diverge` (the reported blocks leaving the token literal) and `case-set-up-container-workspace-folder-unauthored: chan-file-content: Diverge` (the invented `/` reaching the hook). With the fix restored the group is 65/65 green with an empty diverging set, and the full `--profile parity` run is 8/8. One neighbour was measured and is NOT this row: on that same metadata-only probe deacon reports `mergedConfiguration.workspaceFolder: "/work"` where the reference reports `null`. First attributed to a label-SOURCE difference and filed as [#524](https://github.com/get2knowio/deacon/issues/524); the adjudication measurement (2026-08-06, ruling packet on that issue) REFUTED the attribution — both CLIs read the same source, the container inspect's `Config.Labels`, into which Docker already merges the image's labels with run-time `--label` winning, confirmed on all three probe shapes (diverging, image-only, container-only). The real cause is the merge PROPERTY SET: the reference's `mergeConfiguration` folds an enumerated metadata property list where deacon's `merge_configs` folds everything — seven leaked fields (`workspaceFolder`, `name`, `runArgs`, `appPort`, `workspaceMount`, `features`, `overrideFeatureInstallOrder`), tracked as [#526](https://github.com/get2knowio/deacon/issues/526) — closed on 2026-08-06, see the row below, where the census turned out to be a floor rather than a total (the label is arbitrary JSON, so any property NOT on upstream's enumerated list leaked, not only the seven a fixture happened to author). The same measurement surfaced the INVERSE source bug where nobody had looked: `exec`/`run-user-commands`' shared `resolve_config_against_container` read the IMAGE label where the reference reads the container inspect ([#527](https://github.com/get2knowio/deacon/issues/527)) — load-bearing because deacon's own `up` stamps the container only. That one closed on 2026-08-06 and carries its own row in the `run-user-commands` section; the source turned out to be only half of it, and fixing it needed the reference's id-label BRANCH as well, or every lifecycle hook ran twice. No set-up case reaches either defect (every set-up case builds its container with `op-up`, which stamps both label locations identically). |
| A lifecycle hook contributed by the CONTAINER's `devcontainer.metadata` label runs ALONGSIDE the ones other entries of that label — and a `--config` — declare for the same phase, rather than only the last one running. | matches the reference | 1 Docker-gated test — no parity case, see notes | Was an open nonconformance ([#477](https://github.com/get2knowio/deacon/issues/477)), closed 2026-08-04. Both collapse points are gone and all three shapes are byte-identical to oracle 0.87.0, re-measured live on the issue's own repro: with `--config` over a single-entry label, `img-onCreate` / `ws-onCreate` / `img-postCreate` / `ws-postCreate` (deacon previously wrote only the two `ws-` lines); against a three-entry `[image, feature, config]` label with NO `--config`, `e0-onCreate` / `feat-onCreate` / `e2-onCreate` / `e0-postCreate` / `e2-postCreate` / `feat-postStart` (deacon previously wrote only `e2-onCreate` / `e2-postCreate` / `feat-postStart`); and the same label plus a `--config`, which interleaves `ws-onCreate` / `ws-postCreate` last in their phases. The FEATURE entry's hook running at all is new: `set-up` never called `aggregate_lifecycle_commands`, so a Feature-contributed hook ran only when no other entry declared that phase. The fix reuses #467's machinery rather than adding a parallel one — `config_from_metadata_label` lifts each fragment's hooks onto `DevContainerConfig::metadata_lifecycle_layers` and leaves the five singular fields EMPTY, so `aggregate_lifecycle_commands` replays them in label order ahead of the `--config`'s. Leaving a hook in both homes is the double-run trap #467 documented, and it is observed rather than argued: with the clearing removed the Docker-gated test reports `e2-onCreate` / `e2-postCreate` / `feat-postStart` each appearing twice. **No parity case:** `set-up` was not in the harness's `CONSUMER_SUBCOMMANDS` vocabulary when this closed, and that fix was not a reason to grow the case model — the evidence is `test_set_up_collects_container_label_hooks_alongside_the_config_hook` (docker-shared, gates every PR), which asserts the WHOLE marker log on both shapes because a `contains` assertion cannot see an appended duplicate. Two neighbours were re-measured and are clean, so nobody re-checks them: `read-configuration --container-id --include-merged-configuration` is byte-identical before and after the change (it collects through `apply_upstream_merge_shape`, never through this helper), and `exec --container-id` — the other caller of `config_from_metadata_label` — reads only the LAST-WINS `remoteUser` / `remoteEnv` off it and runs no lifecycle phase at all, so it was never exposed and still recovers both across a multi-entry label. One divergence remained on this surface and was NOT #477: `set-up --include-merged-configuration` emitted deacon's flat config under a `merged_configuration` key, where the reference emits `mergedConfiguration` carrying the collected `onCreateCommands` / `postCreateCommands` plural arrays. That block was already the wrong shape before this change and was untouched by it; it was filed as [#483](https://github.com/get2knowio/deacon/issues/483) and closed on 2026-08-05 — see the row above. **No parity case** was true of this row only at the time: `set-up` joined the harness's vocabulary with #483's case, so a future set-up scenario is now a pure data edit. This row also disposes of [#475](https://github.com/get2knowio/deacon/issues/475), filed against the same surface while #477 was in flight and closed on 2026-08-06 as already fixed rather than as work: re-measured at oracle 0.87.0 on a raw-labeled container whose TWO fragments each declare `onCreateCommand` and `postCreateCommand` with a `--config` declaring a third `postCreateCommand` — three same-phase contributors — both CLIs' hook logs read `A-ONCREATE` / `B-ONCREATE` / `A-POSTCREATE` / `B-POSTCREATE` / `CONFIG-POSTCREATE` / `A-POSTSTART`, identical including order, and both reported blocks carry the same three-element `postCreateCommands`. The fix #475 proposed (`route the label-fragment fold through aggregate_lifecycle_commands + LifecycleHookLayer`) is verbatim what #477 landed. The surface gained a SECOND guard with #526, which is not redundancy: that change round-trips the metadata config through JSON and `metadata_lifecycle_layers` is `#[serde(skip)]`, so a lost re-attach would stop every label-contributed hook silently — see the row below. |
| A container's `devcontainer.metadata` label contributes only the ENUMERATED properties upstream's `mergeConfiguration` reads off a metadata entry — to `mergedConfiguration` AND to what `set-up` runs — never every property the label happens to author. | matches the reference | 1 Docker-gated test, 2 hermetic tests — no parity case, see notes | Was an open nonconformance ([#526](https://github.com/get2knowio/deacon/issues/526)), closed 2026-08-06. The reference's merge is not a fold of two configurations, which is the assumption deacon was built on: it is `{...config minus the eight collected properties}` PLUS one expression per ENUMERATED metadata property (bundle `Xi` at `dist/spec-node/devContainersSpecCLI.js`, `mergeConfiguration` in `devcontainers/cli/src/spec-node/imageMetadata.ts`). The list is upstream's `pickConfigProperties` (bundle `SV`) plus `entrypoint` — 25 names — and a property with no expression is simply never read from a label, so the base config is its only source. deacon's `set_up.rs::merge_configs` folded EVERYTHING through `ConfigMerger::merge_configs([metadata, --config])`. Re-measured live at oracle 0.87.0 on a container created raw (`docker run --label devcontainer.metadata=…`) so the label is exactly as authored — which is the only shape that reaches this, see below — carrying #524's seven-field census plus `image`, `service`, `runServices` and `initializeCommand`: the reference's `mergedConfiguration` reports NONE of the eleven and deacon's reported ALL eleven, while the two agreed key-for-key on every enumerated property (`remoteUser`, `containerEnv`, `forwardPorts`, `userEnvProbe`, the collected hook arrays). The census in the issue is therefore a floor, not a total: the label is arbitrary JSON, so anything a base image writes and upstream does not enumerate leaked. **It was never only a reporting defect**, and that is what the JSON-only framing missed: `execute_lifecycle_hooks` passes `merged_config.workspace_folder` as the `docker exec` CWD, so a label authoring `workspaceFolder: /meta-ws` — a directory that does not exist in the container — made deacon fail EVERY hook with exit 127 and exit non-zero, where the reference ran all four. The fix is one projection at the chokepoint #524's ruling packet named, not a second merger: `restrict_to_metadata_properties` retains `METADATA_MERGE_PROPERTIES` on the SERIALIZED metadata document before `ConfigMerger` ever sees it. Applying it to the serialized form is deliberate — the list is upstream's, in upstream's camelCase spelling, and that is the only form in which the two can be compared by eye — and it disposes of `DevContainerConfig::extra` for free, where a field-by-field copy would have carried an unmodeled label key through untouched. Projecting after the fragment fold rather than before it is equivalent, because `ConfigMerger` folds field-wise. `metadata_lifecycle_layers` is re-attached across the round-trip because it is `#[serde(skip)]` merge provenance: losing it would silently stop every label-contributed hook from running, undoing the row above, which is why a test pins it directly. **No parity case, and that is measured rather than assumed.** Through `op-up` the reference stamps a run-time `--label devcontainer.metadata` carrying only its own picked entries, and a run-time label overrides the image's, so a hand-written base-image entry is invisible to `set-up` on both sides: on a Dockerfile fixture whose base `LABEL` authored `name`, `runArgs` and `overrideFeatureInstallOrder`, the reference's container label read `[ {"overrideCommand":false} ]` — the config entry alone. `docker run --label` is the only way to put an arbitrary metadata document on a container, and it is outside the harness's operation vocabulary, so the evidence is `test_set_up_folds_only_the_enumerated_metadata_properties` (docker-shared, gates every PR), which asserts BOTH halves — the eleven keys absent and the enumerated ones present, because an absence assertion alone would pass for a restriction that dropped everything — plus the whole hook log. Watched to fail, on each surface independently: with the projection neutered the test fails first on EXECUTION (`chdir to cwd ("/meta-ws") … no such file or directory`, exit 127), and with the label's `workspaceFolder` pointed at an existing directory so the run survives, it fails on the absence assertion at `workspaceFolder`. The two hermetic tests were broken separately too: neutering the projection fails `merge_configs_folds_only_the_enumerated_metadata_properties`, and removing only the layer re-attach fails `restricting_metadata_properties_preserves_the_lifecycle_layers` together with the pre-existing #477 unit test — which is the double-guard that row describes. One neighbour was measured on the same probe and is NOT this row: `customizations`. The reference reports it as per-tool ARRAYS in `mergedConfiguration` (`{"vscode": [ {...} ]}`) and set-up emits the deep-merged object; `read-configuration` already builds the array form with `apply_customizations_shape` and set-up simply never reaches it. It is a different shape of fix — the helper needs one entry per CONTRIBUTOR, so the label's fragments must be threaded down the way `metadata_lifecycle_layers` threads the per-fragment hooks — so it was left out and filed as [#532](https://github.com/get2knowio/deacon/issues/532), closed on 2026-08-07; see the row below. |
| `set-up`'s `mergedConfiguration.customizations` is one array SLOT PER CONTRIBUTING METADATA ENTRY, keyed by tool — never the deep-merged object. | matches the reference | 1 Docker-gated test, 4 hermetic tests — no parity case, see notes | Was an open nonconformance ([#532](https://github.com/get2knowio/deacon/issues/532)), closed 2026-08-07. Found on #526's own probe and deliberately left out of it, because the fix is a different shape. Upstream's `mergeConfiguration` does not fold `customizations` at all: the property is on the `kV` DELETE list, stripped from the base config, then rebuilt as `entries.reduce((acc, e) => { for (let tool in e.customizations) tool in acc ? acc[tool].push(…) : acc[tool] = [ … ] }, {})` and emitted only when a key survives (`Object.keys(t).length ? t : void 0`) — one slot per contributor, with each consuming tool reconciling its own slots. deacon reported whatever `ConfigMerger` had deep-merged into a single object, which reads as ONE contributor and silently resolves the conflicts the reference hands over intact. Re-measured live at oracle 0.87.0 on a raw-labeled container whose two fragments both author `vscode.settings` — `{"a": 1}` and `{"b": 2}` — plus a `--config` authoring a third: the reference reports `{"vscode": [{"settings":{"a":1},"extensions":[…]}, {"settings":{"b":2}}, {"settings":{"fromConfig":true}}], "jetbrains": […], "codespaces": […]}` and deacon reported one `vscode` object carrying `{"a":1,"b":2,"fromConfig":true}`. Five probe shapes now agree byte-for-byte (single fragment, multi-fragment, multi-fragment + `--config`, `--config` only, and neither), and the whole `mergedConfiguration` document diffs clean against the reference on a mixed probe modulo the standing `portsAttributes` elision (`bhv-readconfig-merged-computed-empties-omitted`). The fix REUSES `read_configuration::apply_customizations_shape` — the array builder that has been correct since #407 — rather than growing a second one; the reason set-up never reached it is that `merged_configuration_document` routed through `apply_upstream_merge_shape` alone, by which point `ConfigMerger` had already collapsed the fragments. So the per-fragment objects ride down on `DevContainerConfig::metadata_customizations_layers`, the exact carrier `metadata_lifecycle_layers` gave the hooks in #477: `config_from_metadata_label` captures each fragment's `customizations` BEFORE the fold, `merge_two_configs` concatenates rather than overwrites, and `restrict_to_metadata_properties` re-attaches across #526's JSON round-trip — the same `#[serde(skip)]` trap that row documents, and losing it would be silent because the fold's own `customizations` key survives the restriction. Entries are ordered `[…label fragments in label order, --config]`, which is upstream's `Tt` (`[...labelEntries, pick(config, pickConfigProperties)]`), so the `--config`'s slot carries the document AS AUTHORED and not the merge — a hermetic test asserts that directly, because reading `merged.customizations` there would still pass every single-contributor case. The layers are variable-substituted with the same context set-up applies to the configuration, because the reference substitutes them the same way (`Tr` → `IG`: `i.map(e)`); measured on a label carrying both tokens, `${localEnv:*}` resolves and `${localWorkspaceFolder}` stays literal on both sides, so the fix could not trade one divergence for another. **No parity case, for the reason #526 measured**: through `op-up` the reference stamps a run-time `devcontainer.metadata` label carrying only its own picked entries, which overrides the image's, so a hand-written base-image entry is invisible to `set-up` on both sides — and `docker run --label` is outside the harness's operation vocabulary. The evidence is `test_set_up_reports_customizations_as_per_tool_arrays` (docker-shared, gates every PR) plus four hermetic tests. Watched to fail: with the `apply_customizations_shape` call removed and everything else in place, the Docker-gated test fails with the deep-merged `{"vscode": {"settings": {"a":1,"b":2,"fromConfig":true}}}` against the three-slot expectation, and all four hermetic tests fail independently — the array shape, the omit-when-empty rule, the authored-config slot, and the substituted layer. |


### `templates-apply`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| `templates apply` scaffolds the template's files into the target directory with `${templateOption:...}` placeholders substituted — from a supplied option, or from the… | matches the reference | 8 scenarios | The spec's devcontainer-templates document defines option substitution; the reference implements `templates apply` but its required --template-id accepts only an OCI… |

### `trust`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| Host-side lifecycle hooks (initializeCommand and other workspace-resident host hooks) are gated behind an explicit workspace-trust opt-in before any host execution. | deacon extension | 1 scenario | Deacon-specific security gate the upstream spec does not mandate (--trust-workspace[-persist], a persisted allowlist, DEACON_NO_PROMPT=1 to fail closed). Recorded by… |

### `up`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| Re-entering a workspace whose configuration has CHANGED since its container was created provisions a new container for the new configuration rather than reattaching to… | documented choice | 2 scenarios | Measured directly at oracle 0.87.0 against fx-up-stale-config: two `up` runs whose documents differ only in `name` and `containerEnv` return the IDENTICAL containerId… Ruled KEEP 2026-08-02. ENFORCEMENT IS NOW DIFFERENTIAL AND SELF-INVALIDATING (2026-08-03): `case-up-stale-config-reentry-differential` runs both CLIs over `fx-up-stale-config-lifecycle` and consumes `wvr-up-changed-config-recreates`. WHERE the divergence is read is the durable part. A containerId is incomparable across sides by construction, and reading it from the created container did not work either: the workspace label matches BOTH generations (deacon keeps the superseded one — see the row below), so which one a probe observed was a coin flip on their hex ids, and an earlier draft observing `chan-container-state` failed about one run in three for exactly that reason. It is read instead from the host workspace, where a `postCreateCommand` marker records each generation that was actually created: deacon's `generations.txt` reads `first\nsecond`, the reference's `first`, because it reattached and the new document's work never ran. That is the choice's own safety argument, measured. The ambiguity itself is resolved as of [#371](https://github.com/get2knowio/deacon/issues/371) — exactly one generation is live and the runner observes the RUNNING container — but the host-marker read is KEPT: it does not depend on which container a probe picked, and it states the divergence's cost where a user would actually meet it. |
| A successful `up` leaves exactly ONE running container for the workspace: any container a previous `up` created for the same workspace is STOPPED — not removed — once the current one is live. | deacon extension | 1 scenario | The necessary counterpart to the recreate above, and only meaningful because of it. deacon's identity includes a configHash, so editing `devcontainer.json` provisions a new container instead of reattaching; until [#371](https://github.com/get2knowio/deacon/issues/371) the previous one was left RUNNING, so a workspace accrued one live container per configuration edit, each holding its ports and memory. Measured on `fx-up-stale-config` before the fix: two `up` runs, `docker ps -a --filter label=devcontainer.local_folder=$W` reporting two containers BOTH `Up`. The reference CLI has no equivalent to compare against — it reattaches, so it never creates a superseded container at all (verified at oracle 0.87.0: identical containerId from both runs of the same sequence). STOPPED, NOT REMOVED, is a maintainer ruling (2026-08-07): stopping releases the ports and memory while the container's local state and volumes survive, so a generation stays recoverable with `docker start` — and it is what makes editing a document and editing it back reattach to the original container rather than rebuild it. `--remove-existing-container` remains the explicit removal path, and lifecycle markers are untouched (only #117's explicit replace clears those). The sweep is scoped to containers carrying BOTH `devcontainer.source=deacon` and this workspace's `devcontainer.local_folder`, filtered daemon-side, so a container the reference CLI or the VS Code extension created in the same folder is never touched. Compose is covered symmetrically: its project name embeds the configHash too, so a changed document orphans a whole project, and that project is swept through `com.docker.compose.project` — the only label its dependency services carry. That project expansion is driven by what the CANDIDATES are, never by what the current `up` is: [#551](https://github.com/get2knowio/deacon/issues/551) found it gated on the current `up` being compose itself, so a workspace whose document changed SHAPE (compose one day, a plain `image` the next) stopped the superseded project's primary — which carries deacon's labels — and stranded its `depends_on` sidecars, which do not. Measured directly before the fix: `deacon_63651e03_532a7bdd-app-1 exited` beside `deacon_63651e03_532a7bdd-db-1 running`, the project network still reporting one attached container; after, both `exited` and the network at zero. Asserted by `case-up-stale-config-reentry` on `chan-temporal`'s `workspaceContainers` census (`total: 2, running: 1`), which read `running: 2` before the #371 fix. The cross-shape transition is asserted OUTSIDE the parity suite, in the Docker-gated `integration_up_supersede_shape`, and deliberately so: that census is a `devcontainer.local_folder` label count and the stranded sidecar carries no such label, so the one container the regression is about is exactly the one the census cannot see. |
| A `containerEnv` variable declared by BOTH the configuration and a Feature takes the configuration's value in the created container; variables contributed by only one… | matches the reference | 4 scenarios | Authored in 024 US5 after the de-suppression found deacon on the wrong side: `commands/up/container.rs` merged the Feature-built image's containerEnv over the… |
| `up` applies the declared container user so the container process runs as that user with that user's UID and GID, whether the user is created by the image or by a… | matches the reference | 3 scenarios | Observed twice over: the container's declared user spec (`Config.User`, parsed into name/uid/group/gid by the derived `userSpec`) and the EFFECT, read by an exec running… |
| up creates a container from the resolved configuration such that a subsequent exec observes the expected environment and remote user, matching the reference. | matches the reference | 7 scenarios | Backed by the declarative case-up-exec-decl-traditional. It previously shared a single reported outcome with bhv-exec-container-id-metadata on the hand-written… |
| Entrypoints contributed by multiple Features are chained and all run, in the resolved install order, before the container's own command. | matches the reference | 2 scenarios | The two CLIs build the chain differently — deacon mounts a generated `/devcontainer/entrypoint-wrapper.sh`, the reference inlines the scripts into the container command… |
| A Feature that cannot be installed — its install script exiting non-zero, or a dependency graph with no resolvable order — fails `up` rather than producing a container… | matches the reference | 2 scenarios | Authored by the 024 US4 error-path tier. Deliberately separate from `bhv-up-feature-install-order`: that claim is about WHICH Features run and in what order on the… |
| `up` installs the configuration's Features into the container it creates, in the resolved install order, across image, Dockerfile, and Compose configuration sources. | matches the reference | 8 scenarios | Evidenced by a live differential on the outcome plus a spec-expectation case that reads the marker each Feature's install.sh writes INSIDE the container — the lesson… **The ORDERING half of this row was VACUOUS until [#417](https://github.com/get2knowio/deacon/issues/417) (closed 2026-08-06).** All five original cases declared at most ONE Feature, so every one of them would have passed against an implementation that installed Features in map-iteration order, in reverse, or in any order at all — the "across image, Dockerfile and Compose" half was covered and the half the behavior is NAMED for was backed by nothing. Three cases now close it, each reading the sequence three local Features' `install.sh` scripts appended to one marker file inside the created container, asserted with `equals` (a `contains` cannot see an appended line). `case-up-feature-multi-declared-order` fixes the order by DECLARATION: `overrideFeatureInstallOrder` names `charlie, alpha, bravo` while the `features` map declares `alpha, bravo, charlie`, so the expected sequence differs from map order, from the §Round Stable Sort lexicographic tie-break an implementation ignoring the override would produce, and — three Features rather than two — from reverse map order. `case-up-feature-multi-dependency-order` DERIVES it instead: `late` is declared first but carries `installsAfter: ["./features/early"]` and `dependsOn: {"./features/pulled": {}}`, so resolution must invert the declaration and pull in a Feature the configuration never names. `…-differential` runs that fixture against the reference so the agreement is enforced rather than recorded. MEASURED at oracle 0.87.0 on 2026-08-06, both CLIs, no divergence: `charlie\nalpha\nbravo\n` and `early\npulled\nlate\n`; the reference's own `devcontainer.metadata` label independently records the same two sequences. The coverage hole was real; the behavior behind it was not. |
| A lifecycle hook runs with its working directory set to the container workspace folder, so a command that resolves a relative path resolves it against the project… | matches the reference | 1 scenario | Authored by 024 T150 out of the one remaining un-restated `non-testable` clause: features-contribute-lifecycle-scripts' "As with all lifecycle hooks, commands are… |
| A lifecycle hook declared in the array (argv) form and one declared in the object (named commands) form are both executed, the object form running every named command. | matches the reference | 3 scenarios | Each form writes its own marker file into the bind-mounted workspace, so the observation is the file rather than the exit code — a hook that silently did not run leaves… |
| Each declared mount is applied with both its declared source and its declared shape — the mount type and the read-only flag — and two mounts differing only in source are… | matches the reference | 3 scenarios | FR-053's two axes are separated by construction in the fixture: `/mnt/ro` and `/mnt/rw` have the SAME shape and different sources, `/mnt/rw` and `/mnt/vol` have the same… |
| A PATH segment contributed by a Feature's `containerEnv` is prepended to the image PATH, and the resulting PATH is observable segment-by-segment in the created container. | matches the reference | 2 scenarios | PATH was captured but NOT compared until 024 US5: `drop_noise_env` removed it at capture, so the declarative container-state channel never saw it. It is compared… |
| Re-entering a workspace whose container already exists, with the SAME configuration, reattaches to it rather than creating a second one. | matches the reference | 2 scenarios | Asserted as a metamorphic relationship (`first-create-vs-restart`) rather than against a fixed output, because what is being pinned is the relation between the two runs… |
| The Compose service image's `devcontainer.metadata` contributes `remoteUser`, `remoteEnv` and lifecycle hooks to a compose `up`'s own lifecycle run, as it already does on the single-container path. | matches the reference | 1 scenario | Fixes #448, the second instance of the #405 shape and the last known call site. `merge_image_metadata_after_image_ready` had exactly two callers — `up`'s single-container path and the shared `container_metadata::resolve_config_against_container` that `exec` and `run-user-commands` use — and `up`'s COMPOSE path called neither. Measured against oracle 0.87.0 on a compose service whose image label carried all three and whose devcontainer.json declared only its own `remoteEnv`: the reference wrote `metauser from-image-metadata from-workspace-config`; deacon wrote no file at all, exit 0 on both sides, so only the marker showed it. The merge now runs on the compose path at the same point in the flow — image ready, before lifecycle — against the image the container actually runs (the Feature-extended tag when Features were built, else the service's own `image:`), reusing the one implementation. Making it observable also required the compose post-create exec to stop passing `user: None` and an EMPTY env: it now resolves both through the shared `resolve_env_and_user`, which is why the workspace's own `remoteEnv` had been missing too — the identical third miss #405 found. Backed by `case-up-compose-image-metadata-differential` (nightly; its `assertion` also pins deacon's side) and by the Docker-gated `integration_compose_config_mounts::test_compose_up_merges_service_image_metadata`, which runs under the `default`/`full` profiles (`make test-nextest`) — that binary is selected by neither PR lane, `dev-fast` excluding it and `mvp-integration` not listing it. The parity case measures all four cells, but only did so from its THIRD revision, and the two before it are the useful part of the record. The first declared `remoteUser` and passed locally solely because the dev container's uid and the image user's were both 1000; on GitHub's runners it failed with `Permission denied`, which is how #462 was found. The second dropped `remoteUser` so a #448 regression stayed distinguishable from a uid coincidence, parking the user cell in the Docker-gated test. The third took it back once #462 landed: with the remap in place, and the fixture image pinning its user to uid 1234, no plausible host uid matches by accident, so the coincidence is gone from the verdict rather than merely averaged over. The marker records `whoami` and not `id -u` for the same reason in reverse — the remapped uid is the HOST's and would make a fixed assertion a statement about the runner. The tail this row carried — that the compose post-create still ran `postCreateCommand` in its STRING form only and no other phase — was closed by [#460](https://github.com/get2knowio/deacon/issues/460); see the full-lifecycle-phase-set row. |
| A compose `up` remaps the container user's uid to the host's (`updateRemoteUserUID`), so a non-root `remoteUser` can write the bind-mounted workspace. | matches the reference | 1 scenario + 2 Docker-gated tests | Fixes [#462](https://github.com/get2knowio/deacon/issues/462), the second of the two compose-path gaps that closing #448 surfaced. deacon called `apply_user_mapping` on `up`'s single-container path only, so a compose service's non-root `remoteUser` kept the IMAGE's uid and any hook writing into the bind-mounted workspace died with `Permission denied` unless the host's uid happened to match. Re-measured against oracle 0.87.0 on the issue's repro — a compose service whose image pins its user to uid 1234, a host at uid 1000: before, the reference's hook reported `user=metauser uid=1000` and wrote while deacon's wrote nothing; after, deacon reports the same `user=metauser uid=1000`. The two CLIs reach that observable by different MECHANISMS and it is worth writing down which: the reference builds a derived `<image>-uid` image whose final `RUN` layer rewrites `/etc/passwd`/`/etc/group`, then points the service at it through its generated `docker-compose.devcontainer.containerFeatures-*.yml` override (verified by reading both the override file and `docker history` of the image it names); deacon remaps in the running container, which is what `apply_user_mapping` already did for the single-container path. Reusing it keeps ONE uid-remap implementation and inherits every spec rule it encodes — default true on Linux, no-op for a root `remoteUser` (#90) or a root host user, and an explicit `false` still opts out. The fix is a call site, not an algorithm. Its cost was the accuracy of the two assertions that now cover it, and both were broken once to prove they can fail: with the call removed, `test_compose_up_merges_service_image_metadata` fails exactly as CI did (`Permission denied`, no marker); with the `updateRemoteUserUID` knob forced true, `test_compose_up_update_remote_user_uid_false_keeps_image_uid` fails `uid=1000` against `uid=1234`. That first test also DROPPED the `chmod 0777` it carried while this row was open — the workaround existed to take permissions out of what a #448 test measured, and with the remap in place the uid-1234 pin makes the write itself the evidence. The parity fixture `fx-up-compose-image-metadata` took its `remoteUser` back at the same time, so `case-up-compose-image-metadata-differential` now measures all four cells of #448's matrix directly — hook ran, hook USER, image `remoteEnv`, workspace `remoteEnv` — against the live oracle. That restoration was broken once too, and it is the strongest of the three: with the call removed, the whole `differential_group_docker_shared` lane run against the pinned 0.87.0 reports exactly ONE diverging case, `case-up-compose-image-metadata-differential: chan-file-content: Diverge` on `up-compose-image-metadata.txt`, and passes clean with the call in. A restored cell that could not fail would be worse than the empty one it replaced. |
| A compose `up` runs the full lifecycle phase set in every command form, as the single-container path does. | matches the reference | 2 scenarios | Fixes [#460](https://github.com/get2knowio/deacon/issues/460). `up`'s compose path carried its OWN post-create exec — `if let Some(cmd_str) = post_create_cmd.as_str()` — so it ran `postCreateCommand` only in its STRING form and queued no other phase; the array and object forms, `onCreate`/`updateContent`/`postStart`/`postAttach`, Feature-contributed hooks and the `.devcontainer-state` phase markers were all dropped on compose while the single-container path ran them. Measured against oracle 0.87.0 on `fx-up-compose-lifecycle-phases`, whose five phases span all three command forms: the reference ran six commands and wrote three marker files; deacon ran NONE and wrote NO file, exit 0 on both sides — so the markers are the whole observation. After the fix deacon's three files are byte-identical to the reference's, including the phase ORDER (`onCreate-string`, `updateContent-array`, `postCreate-array`, `postAttach-string` in one appended log) and both named commands of the object form (their own files, because the spec does not order named commands). The fix deletes the compose-specific exec and calls `up::lifecycle::execute_lifecycle_commands` — the same engine, so phase set, ordering, aggregation of Feature commands, `waitFor`, dotfiles, markers and the skip flags come from one implementation. Two inputs are still compose's to resolve and are: the lifecycle user/env via the shared `resolve_env_and_user` (#448), and the lifecycle CWD via `resolve_container_cwd`, read from the RUNNING container's mounts because deacon injects no workspace mount on compose and `docker exec -w` hard-fails on a directory the service never mounted (#294/#295). The `if !args.skip_post_create` guard that wrapped the old call is gone with it, because the decision belongs to `InvocationContext` rather than to a call site — which is what let the flag's own meaning be corrected later, in ONE place, when [#476](https://github.com/get2knowio/deacon/issues/476) measured it to defer every phase rather than postCreate onward. A second, unlooked-for conformance gain came with the CWD: the old wrapper was `cd '<dir>' 2>/dev/null; …`, which SHRUGGED OFF a `workspaceFolder` the compose service never mounts, and `docker exec -w` does not. Measured at oracle 0.87.0 on that exact shape (explicit `workspaceFolder: /workspace`, no volume providing it, container kept alive): the reference emits `chdir to cwd ("/workspace") set in config.json failed: no such file or directory`, the hook exits 127 and `up` exits 1 — character for character what deacon now emits, where deacon previously ran the hook from the image's default directory and exited 0. The leniency was the divergence; `smoke_compose_override_command::test_compose_override_command_lifecycle_runs` had encoded it, and its fixture — which declared `/workspace` while mounting nothing there — now declares no `workspaceFolder` at all, resolving to the compose default `/`. Backed by `case-up-compose-lifecycle-phases` (spec-expectation, so it runs on the PR- and release-gating Docker lane) and `case-up-compose-lifecycle-phases-differential`, plus three Docker-gated tests in `integration_compose_config_mounts` covering the image-metadata-contributed phase, `--skip-post-create` and `--skip-non-blocking-commands`. An adjacent divergence measured while doing this is NOT compose's and is not fixed here: when the image's `devcontainer.metadata` and the workspace config declare the SAME phase, the reference runs both (image first) and deacon runs only the workspace's — identically on the single-container path, so it is a config-merge issue, filed as [#467](https://github.com/get2knowio/deacon/issues/467) and given its own row. |
| A lifecycle hook contributed by the image's `devcontainer.metadata` runs ALONGSIDE one the configuration declares for the same phase, rather than being replaced by it. | matches the reference | 2 scenarios + 2 Docker-gated tests | Fixes [#467](https://github.com/get2knowio/deacon/issues/467), found while closing #460 and deliberately out of its scope: a config-MERGE defect, not a lifecycle-execution one, reproducing IDENTICALLY on the single-container and compose paths — which is what identified it. The spec's image-metadata Merge Logic table gives each of the five lifecycle hooks a "Collected list of all `<phase>Command`s", with "the devcontainer.json is considered last"; every OTHER row of that table is last-wins, and that is how deacon folded the label in — through `ConfigMerger`, where a hook is an ordinary scalar the higher-precedence layer overwrites. Precedence was right, accumulation was missing. Measured at oracle 0.87.0 on `onCreateCommand` and `postCreateCommand` declared in BOTH the image label and the devcontainer.json: the reference's marker log read `img-onCreate` / `ws-onCreate` / `img-postCreate` / `ws-postCreate`, deacon's only the two `ws-` lines; after the fix deacon's log is byte-identical to the reference's on BOTH paths. Both sides exit 0 throughout, so the marker file is the entire observation. The fix reuses the aggregation that already existed rather than adding a second one: `aggregate_lifecycle_commands` had always run Feature-contributed hooks ahead of the configuration's, and image-metadata hooks now join that same list ahead of both, carried from the merge on `DevContainerConfig::metadata_lifecycle_layers` (not serialized — it is merge provenance, not an authored property). Because the carrier is the config itself, the one merge site reaches all four consumers that share it: `up`'s single-container path, `up`'s compose path (#448) and `container_metadata::resolve_config_against_container`, which `exec` and `run-user-commands` use (#405). `run-user-commands` was verified directly rather than assumed — on a metadata-carrying container its log reads `img-onCreate` / `ws-onCreate` / `img-postCreate` / `ws-postCreate`, byte-identical to the reference's. The half that is easy to get wrong, and that the first draft DID get wrong, is the non-colliding case: an image hook in a phase the config leaves unset was already correct (#448), and recording it as a collected layer while the merge ALSO left it in the singular field ran it TWICE. The merged config's five singular fields therefore carry only what the devcontainer.json itself declared, and every assertion here spans the WHOLE marker log rather than checking for one line — a `contains` assertion cannot see an appended duplicate. Collecting the hooks does NOT make the scalar rows collect: `remoteUser` declared on both sides still resolves last-wins to the config's, measured by the hook's own `whoami`. Backed by `case-up-image-metadata-same-phase-differential` and `case-up-compose-image-metadata-same-phase-differential` (nightly; their `assertion`s also pin deacon's side), and on the PR lane by the Docker-gated `integration_feature_lifecycle::test_up_collects_image_metadata_and_config_hooks_for_the_same_phase` and `integration_compose_config_mounts::test_compose_up_collects_image_metadata_and_config_hooks_for_the_same_phase`, one per path because the defect is path-independent and a single-path test would misattribute a regression. All four were broken once and watched to fail, in both directions: with the layer aggregation removed the docker-shared differential lane reports exactly four diverging cases (the two new ones plus the two existing image-metadata cases, which the same break also kills), and with the singular-field restore removed every assertion reports the doubled `img-postStart`. |
| Lifecycle hooks contributed by installed Features and by the configuration are collected and run TOGETHER in all five phases, with the Feature hooks ordered by the Feature installation order. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. This is the shape [#467](https://github.com/get2knowio/deacon/issues/467) and [#477](https://github.com/get2knowio/deacon/issues/477) were both defects in, and the reference keeps a fixture that covers the whole matrix at once: two local Features ordered by `installsAfter`, each declaring all five phases in the string form and the array form, plus the configuration declaring the same five itself. The observable is an append-only `lifecycle-order.log`, so an implementation that collects only one source per phase, orders the two sources the other way, or skips a phase writes a different file while still exiting 0. Measured at oracle 0.87.0 on `fx-upstream-lifecycle-hooks-inline`: identical. The fixture's base is `debian:bookworm-slim` deliberately and must stay a base with no `remoteUser`: on `mcr.microsoft.com/devcontainers/base:bookworm` both CLIs uid-remap, and the reference stages its remap Dockerfile at a version-scoped global path (`/tmp/devcontainercli-runner/updateUID.Dockerfile-<version>`), so concurrent reference `up` runs race on the rename and one exits 1 with `ENOENT`. That made the case red for a reason indicting neither side, and only in CI — a host uid of 1000 matches `vscode` and skips the remap entirely. uid-remap coverage lives with the one case that claims it. |
| Object-form (parallel) lifecycle hooks declared by a Feature AND by the configuration both run, and a configuration hook whose command substitutes a binary a Feature installed resolves it. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The harder forms of the same matrix: object-form `postCreateCommand` on both sources with one member a string and the other an array, array-form `postStart`/`postAttach` from the Features, and configuration hooks containing command substitution of Feature-installed binaries — which can only succeed if `install.sh` already ran and the hook's PATH carries its output. Sixteen marker paths are observed rather than an ordered log, deliberately: the parallel members of an object-form hook have no defined order between them, so presence is the only honest observable, and absence is captured as `exists:false` rather than as a gap. Measured at oracle 0.87.0 on `fx-upstream-lifecycle-hooks-advanced`: identical. The fixture's base is `debian:bookworm-slim` deliberately and must stay a base with no `remoteUser`: on `mcr.microsoft.com/devcontainers/base:bookworm` both CLIs uid-remap, and the reference stages its remap Dockerfile at a version-scoped global path (`/tmp/devcontainercli-runner/updateUID.Dockerfile-<version>`), so concurrent reference `up` runs race on the rename and one exits 1 with `ENOENT`. That made the case red for a reason indicting neither side, and only in CI — a host uid of 1000 matches `vscode` and skips the remap entirely. uid-remap coverage lives with the one case that claims it. |
| On the single-container Dockerfile path, a `remoteUser` whose uid the image fixes is remapped to the host's, so a lifecycle hook running as that user can write to the bind-mounted workspace. | matches the reference | 1 scenario | Mined from the reference CLI's own e2e test fixtures at tag `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 1) — coverage chosen by the reference's authors rather than by ours. The single-container analogue of the Compose case that found [#462](https://github.com/get2knowio/deacon/issues/462). The image pins `foo` to uid 1234 — not a plausible host uid — so neither half of the claim can pass by coincidence: the marker exists only if the hook ran AS `remoteUser` and that user's uid was remapped. It records the NAME rather than `id -u`, because the remapped uid is the host's and a fixed assertion on it would be a statement about the host. Measured at oracle 0.87.0 on `fx-upstream-updateuid-remoteuser`: both sides write `foo`. |
| `up --skip-post-create` defers EVERY lifecycle phase — `onCreate` and `updateContent` as well as `postCreate`, `postStart`, `postAttach` and dotfiles — to a later `run-user-commands`, which then runs each of them exactly once. | matches the reference | 2 scenarios + 1 Docker-gated test + 5 hermetic tests | Fixes [#476](https://github.com/get2knowio/deacon/issues/476), surfaced while verifying #467. deacon ran the two base phases and deferred `postCreate` onward — what the flag's NAME says, and what deacon's own 008 FR-005/SC-003 had written down; the reference defers all five. **Adjudicated by the standing authority rule, not by a waiver ruling**: the flag is spec-silent (a CLI surface; `grep -r skip-post-create parity/spec/113500f4/` returns nothing), so the reference is the authority and the difference is deacon's. Transcribed from the pinned bundle rather than inferred: `up` puts `postCreateEnabled: !skipPostCreate` into its params, `EN(postCreateEnabled, …)` turns that into `lifecycleHook.enabled`, and the lifecycle runner is reached only through `params.lifecycleHook.enabled && await runPostCreateCommands(…)` — the ENTIRE chain (`onCreate` → `updateContent` → `postCreate` → dotfiles → `postStart` → `postAttach`) lives inside that one call, so the flag cannot reach a subset of it. The CLI's help text says the same in prose: "Do not run onCreateCommand, updateContentCommand, postCreateCommand, postStartCommand or postAttachCommand and do not install dotfiles". **The full matrix**, measured at oracle 0.87.0 against deacon at 66294dc on a fixture whose five hooks each append their phase name to one file — reference first, deacon second: plain `up` → all five / all five (agree); `up --skip-post-create` → NOTHING / `onCreate`, `updateContent` (the divergence); `up --skip-non-blocking-commands` → `onCreate`, `updateContent` / same (agree); `up --skip-post-create` then `run-user-commands` → all five once / the two base phases then all five again. That the two skip flags AGREE while only `--skip-post-create` diverges is the measurement that rules out the tempting shortcut of treating them as one rule: `--skip-non-blocking-commands` is a `waitFor` cutoff, `--skip-post-create` is an on/off gate over the whole runner. **The `run-user-commands` pairing** is the half that makes deferral safe, and it needed no change: a deferred `up` writes no completion markers, so the following `run-user-commands` runs all five exactly once on both sides. The marker mechanisms differ without changing that — deacon's are host-side (`{user_data_folder}/state/<workspace-hash>/`), the reference's container-side (`{containerDataFolder}/.<phase>CommandMarker`, keyed on the container's `createdAt`) — because neither side records a phase that never ran. The separately characterized difference on this pairing (deacon RE-runs a completed hook where the reference skips it, `wvr-ruc-completed-hooks-not-rerun`) is untouched, and is why the resume assertion is taken after a DEFERRED `up` rather than a plain one. **The fix is one function**: `LifecyclePhase::is_skipped_with_skip_post_create` now returns true for every phase, written as an exhaustive `match` so a phase added later must be classified deliberately. Everything else already routed through `InvocationContext::should_skip_phase`, so the single-container and compose paths changed together and no call site re-decides it — the property #460 bought when it deleted compose's local copy of the guard. NOT changed: `run-user-commands`'s own `--skip-post-create`, a deacon extension the reference has no counterpart for (its `run-user-commands` hard-codes `postCreateEnabled: true`), so there is no reference behavior to align it to; and `set-up --skip-post-create`, which already meant "skip everything" and already matched. **Two scenarios, split by an observation-timing constraint** worth writing down: `chan-filesystem` / `chan-file-content` are captured ONCE, after every operation of a case has run (`runner::execute_ops`, then the `capture_channel` loop) — the `operation` field on a filesystem expectation labels the evidence, it does not pick a point in time. The deferral itself therefore has to be observed by a case with ONE operation: `case-up-skip-post-create-defers-every-phase` (spec-expectation, so it runs on the PR- and release-gating `parity_docker` lane) runs only `up --skip-post-create` and asserts the log file is ABSENT. The resume half is `case-up-skip-post-create-run-user-commands-resume-differential`, where the end-of-run capture is exactly the right instant: it adds `run-user-commands` and asserts the log holds exactly the five phases — the line COUNT is what catches a leak, since before the fix deacon wrote seven. A first draft put both operations in ONE case and asserted `absent` on `op-up`; it failed on the first live run because by capture time `run-user-commands` had written the file — an assertion that looked like coverage and could only ever have passed by accident. **Watched to fail at three levels** before landing. Restoring the old two-phase classification turns `case-up-skip-post-create-defers-every-phase` red (`chan-filesystem: Diverge`); fails the compose-path Docker test `integration_compose_config_mounts::test_compose_up_skip_post_create_defers_every_phase` with `left: Some("onCreate-string\nupdateContent-array\n"), right: None`; and fails four of the five hermetic tests — the classification table (`core::lifecycle::tests::test_lifecycle_phase_helpers`), both orchestrator tests, and `up::lifecycle::tests::test_build_invocation_context_skip_post_create_mode`, which asserts the exact loop `execute_lifecycle_commands` walks. The three `up_dotfiles.rs` `--skip-post-create` tests were corrected in the same commit but stay `#[ignore]`d for the pre-existing reason their attribute gives (dotfiles are not in the MVP), so they are not counted as evidence. |
| `up --frozen-lockfile` accepts an on-disk lockfile that agrees SEMANTICALLY with the freshly-resolved Feature set, whatever serialisation it happens to use — and the lockfile deacon WRITES is byte-identical to the one the reference writes for the same input. | matches the reference | 1 scenario | Fixes [#557](https://github.com/get2knowio/deacon/issues/557), which had two halves and closed with both, deliberately in one change: a tolerant comparison alone would have left the files deacon writes different from the reference's, and an emission change alone would have rewritten every existing deacon-written lockfile while the byte comparison was still in place. **The comparison.** `up::helpers::compare_lockfile_frozen` compared the on-disk file to a re-serialisation of the resolved lockfile BYTE FOR BYTE, so any key order, indentation or trailing-newline difference was reported as `Lockfile does not match.` — and the consequence was not hypothetical: deacon rejected every lockfile the reference CLI writes. It now parses both sides and compares them as DOCUMENTS (`deacon_core::lockfile::lockfile_text_matches`), which is the question the flag asks. The reference normalises the same way before comparing (`JSON.stringify(JSON.parse(raw), null, 2) + '\n'`) and carries a dedicated test for it, `frozen lockfile matches despite formatting differences`. Text that is not valid JSON still never matches, mirroring the reference's `try { … } catch {}`. **The emission order.** deacon alphabetised a Feature entry's keys; it now writes `version, resolved, integrity` (then `dependsOn`), which is the order the reference's `generateLockfile` builds its entry literal in and the order every example in `parity/spec/113500f4/devcontainer-lockfile.md` uses. `devcontainer-lockfile.md` prescribes the three properties and says nothing normative about their order, so this was a spec-silent output surface of the class [#486](https://github.com/get2knowio/deacon/issues/486) and [#490](https://github.com/get2knowio/deacon/issues/490) were; the maintainer ruled MATCH on 2026-08-11, and no tolerance was authored because there is now nothing to tolerate. All writers share one serialiser (`deacon_core::lockfile::canonical_lockfile_json`), so a lockfile's bytes do not depend on whether `up`, `build` or `upgrade` produced it — three copies of a recursive key-sort were the reason they could have. Feature ids stay SORTED, matching the reference's `.sort((a, b) => a.id.localeCompare(b.id))`; deacon sorts by code point, which agrees with `localeCompare` on every legal lowercase OCI reference measured, and chasing ICU exactly would pin the output to one ICU version and one host locale. MEASURED at oracle 0.87.0, before and after, on `fx-upstream-lockfile-frozen`, whose committed `.devcontainer-lock.json` IS the reference's own output copied verbatim: before, the reference exited 0 and left it unchanged while deacon built the Feature image and exited 1; after, both exit 0 and leave it byte-unchanged. Byte-identity of the WRITE path was measured separately, on a fresh workspace (`up` writes the fixture's exact 327 bytes) and on a four-Feature config resolved through `upgrade --dry-run` (`common-utils`, `git-lfs`, `git`, `node` — declared in a different order, emitted `diff`-clean against the reference's file). `case-up-upstream-lockfile-frozen-matching` asserts exit 0 and compares `chan-file-content` across both CLIs, so a frozen run that started REWRITING the file would also be caught. Mined from the reference CLI's own e2e fixtures at `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 2). |
| `up --frozen-lockfile` on a configuration that declares no Features neither refuses nor writes a lockfile — the container comes up and the flag is simply never reached. | matches the reference | 1 scenario | MEASURED at oracle 0.87.0 against a freshly built `main` at `e107a92`, on a three-line `{"image": "debian:bookworm-slim"}` with no `.devcontainer-lock.json` on disk: BOTH CLIs exit 0, both print a `success` result document naming a container they created, and NEITHER writes `.devcontainer-lock.json`. This is the carve-out [#565](https://github.com/get2knowio/deacon/pull/565) introduced incidentally while fixing [#556](https://github.com/get2knowio/deacon/issues/556) — before it, deacon exited 1 with `Lockfile does not exist.` and created nothing, a refusal the reference never makes. The change was disclosed and the with-Features rows above were updated, but they document the WITH-Features case; the featureless path had no row and no scenario until this one, so the ledger's own rule (every measured behaviour gets both) was owed a debt here. TRANSCRIBED rather than inferred, because an exit code does not say why: `--frozen-lockfile` is consulted only inside `writeLockfile` (`mQ`), whose single caller `generateFeaturesConfig` (`UQ`) opens with `let o = BV(xQ(t, i), r); if (!o) return;`, and `xQ` (`userFeaturesToArray`) returns `undefined` when the configuration's `features` and the `additionalFeatures` overlay are both empty. With no Features declared the reference returns before the lockfile is read, so the flag is never reached rather than satisfied. deacon spells that early return once, as `commands/shared/lockfile::declares_features`, shared by `up` and `build` so the two cannot drift. `case-up-frozen-lockfile-no-features` is `spec-expectation` on purpose and the LANE is the reason: a `live-differential` case routes to `parity_differential`, which runs in the nightly ONLY and gates no pull request, and a case whose whole point is that nothing gated this behaviour belongs where it gates — `docker-shared` + `spec-expectation` puts it in `parity_docker`, selected by the release gate and by every Docker-provisioned profile. Its three channels are each load-bearing: `chan-exit-code: 0` is the regression signal itself, `chan-structured-output`'s `outcome: success` is what makes that 0 mean the container came up rather than something quietly declining to happen, and `chan-filesystem` `absent` catches the other failure shape — exiting 0 by WRITING a lockfile would satisfy both other assertions while doing the one thing the flag exists to prevent. One adjacency is deliberately NOT claimed here: the reference's early return keys off the union of the configuration's Features and `--additional-features`, while deacon's gate reads the configuration alone, so Features supplied by the flag are refused later, after the Feature image is built ([#569](https://github.com/get2knowio/deacon/issues/569), measured, classification pending). |
| `up --frozen-lockfile` refuses a workspace with no lockfile, creates no container, and writes no lockfile. | matches the reference | 1 scenario | MEASURED at oracle 0.87.0 on `fx-upstream-lockfile-no-lockfile`: both CLIs exit 1 before creating anything and neither writes `.devcontainer-lock.json`. deacon validates pre-build in `commands/up/mod.rs` and reports `Lockfile does not exist.` with the expected path and how to generate one; the reference reports the same sentence as its result document's `message`. The error ENVELOPES differ in shape (deacon puts the sentence in `description` under a generic `message`), so `chan-structured-output` is deliberately not declared — it would report an envelope difference this case is not about. The filesystem claim is what makes the case more than an exit code: a run that refused AFTER writing the lockfile would satisfy `equals: 1` having already done the thing the flag exists to prevent. This row was the green control that gave [#556](https://github.com/get2knowio/deacon/issues/556) its shape — `up` implemented the gate `build` did not, which is what made that defect a missing wire rather than a missing capability. Both subcommands now reach the gate through one helper (`commands/shared/lockfile`), so this row and its `build` sibling can no longer drift apart. Mined from the reference CLI's own e2e fixtures at `v0.87.0` ([#480](https://github.com/get2knowio/deacon/issues/480), batch 2). |

### `upgrade`

| Behavior | Status | Evidence | Notes |
|---|---|---|---|
| `upgrade` regenerates the lockfile from the configuration AFTER the command-line overlays are applied: `--override-config` replaces the base document and… | deacon extension | 3 scenarios | The reference CLI's `upgrade` accepts NEITHER flag — `--merge-config` does not exist anywhere in its surface, and its `upgrade` has no `--override-config` even though… |
| `upgrade` on a configuration that declares no Features regenerates an EMPTY lockfile and exits 0. | deacon follows the spec, the CLI deviates | 3 scenarios | devcontainer-lockfile.md defines the lockfile as "a JSON object with a `features` property" and nowhere makes an empty Feature set an error, so `{"features":{}}` is the… |
| `upgrade` regenerates the devcontainer lockfile from the effective configuration's resolved Feature set, including Features contributed by a parent link of an extends… | matches the reference | 3 scenarios | Both CLIs resolve the same pinned Feature reference to the same digest and integrity, so the comparison is on the lockfile document itself rather than on the exit code.… |
