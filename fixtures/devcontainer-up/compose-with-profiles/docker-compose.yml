version: '3.8'

services:
  app:
    image: ubuntu:22.04
    volumes:
      - ../..:/workspace:cached
    command: sleep infinity
    environment:
      - TEST_ENV=compose-app
    profiles:
      - dev

  db:
    image: alpine:latest
    command: sleep infinity
    environment:
      - DB_NAME=testdb
    profiles:
      - dev
