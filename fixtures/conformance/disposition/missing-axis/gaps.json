{
  "schemaVersion": 1,
  "records": [
    {
      "id": "gap-readconfig-remote-user",
      "kind": "knowledge",
      "behaviors": ["bhv-readconfig-remote-user-probe"],
      "description": "The reference CLI's remoteUser derivation for an image without an explicit remoteUser is not yet characterized, so the behavior's reference status is unknown.",
      "tracking": "https://github.com/get2knowio/deacon/issues/0"
    }
  ]
}
