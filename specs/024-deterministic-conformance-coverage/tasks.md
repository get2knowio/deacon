---

description: "Task list for 024-deterministic-conformance-coverage"
---

# Tasks: Deterministic Conformance Coverage

**Input**: Design documents from `/specs/024-deterministic-conformance-coverage/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/

**Tests**: Test tasks are **MANDATORY** for this feature. FR-071 makes every acceptance
scenario an automated test, and FR-072 restricts execution to nextest under a named profile.
Test tasks are therefore not optional and are written before the implementation they cover.

**Organization**: Grouped by user story. Phase 2 is split into two blocks — hermetic core
(blocks everything) and live infrastructure (blocks US3–US6 only), so the US1 MVP does not
pay for machinery it never uses.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1–US6)
- Every task names its exact file path

## Path Conventions

- Hermetic data/validation/reporting: `crates/conformance/src/`
- Live execution/observation/injection: `crates/parity-harness/src/`
- Live test drivers: `crates/deacon/tests/`
- Records: `conformance/registry/`, `conformance/obligations/`, `conformance/fixtures/`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Wire the new module and command surfaces so later tasks land in real files

- [X] T001 Confirm the pre-change baseline is green: run `cargo run -p deacon-conformance -- validate` and `cargo run -p deacon-conformance -- certify` and record both exit codes in the PR body — both exit 0 (validate: "ok: registry is valid"; certify: "certified: prof-linux-amd64-docker-0870 (0 blocking, 10 waived)")
- [X] T002 [P] Create empty module stubs `scenario.rs`, `obligation.rs`, `coverage_report.rs`, `regression.rs` in `crates/conformance/src/` and declare them in `crates/conformance/src/lib.rs`
- [X] T003 [P] Add the `Coverage` command enum variant and `CoverageCommand` subcommands (`Generate`, `Check`, `Report`, `Scaffold`) to `crates/conformance/src/bin/conformance.rs` per `contracts/coverage-cli.md`, dispatching to `todo!()` stubs
- [X] T004 [P] Add the V26–V30 rows to the violation-class index table in `conformance/RULES.md` with links to the sections added in later tasks
- [X] T005 [P] Add `target/conformance/coverage-*.json`, `target/conformance/coverage-*.md`, and `target/conformance/regressions.json` to `.gitignore` — already covered by the existing wholesale `target` ignore (verified no negation rule un-ignores it); no edit needed
- [X] T006 [P] Create the `conformance/obligations/` directory with a `.gitkeep` so the machine-owned tree exists before generation writes to it

---

## Phase 2: Foundational (Blocking Prerequisites)

**⚠️ CRITICAL**: Block A blocks every user story. Block B blocks US3–US6 only and is **not**
required for the US1/US2 MVP.

### Block A — Hermetic core (blocks ALL stories)

- [X] T007 Migrate `conformance/registry/cases.json` to `conformance/registry/cases/<area>.json`, splitting by the area of each case's first linked behavior, in its **own commit** with no content changes (research Decision 7) — landed as its own commit; verified byte-identical records (concatenate + id-sort reproduces the original record list exactly). Per-area counts: build 1, exec 4, host-ca 1, observable-state 9, ports 1, profiles 1, read-configuration 65, secrets 1, trust 1, up 4 = 88
- [X] T008 Add directory-aware case loading for `cases/<area>.json` to `crates/conformance/src/load.rs`, preserving id-sorted deterministic ordering across files — `load_case_files` reads every `cases/*.json` in sorted-filename order, keeps each record's origin file + in-file index (so `check_case_shapes`/`check_allowed_differences` still name a reviewable `cases/<area>.json:records[i]`), then stable-sorts the concatenation by case id. The 24 fixture registries under `fixtures/conformance/` moved to the same layout. `validate`/`certify` still exit 0
- [X] T009 Add a hermetic guard test in `crates/conformance/tests/registry_valid.rs` asserting the loaded case count is exactly 88 after migration, so the split cannot silently lose records — `every_migrated_case_survives_the_per_area_split` asserts the count against `MIGRATED_CASE_COUNT = 88`, that the concatenated set is id-sorted, and that no two records share an id across files
- [X] T010 Add the `scenario_context: IndexMap<String, String>` field to `TestCase` in `crates/conformance/src/model.rs` with `#[serde(default, skip_serializing_if = "IndexMap::is_empty")]` — round-trips as `scenarioContext` via the struct-level `rename_all`; a sibling of `context` (environment), never a replacement. Contents are unvalidated here by design: V26/V16 are US1 (T041)
- [X] T011 Include `scenarioContext` in the case-hash input in `crates/conformance/src/case_hash.rs`, so changing what a case exercises re-records its snapshot — added to the explicit allow-list document. **Omitted when the assignment is empty**: an empty map declares nothing, and hashing it would have marked the one committed snapshot (`case-readconfig-snapshot`) stale for a record whose bytes did not change, making staleness mean "the hash function grew a field" instead of "the evidence-determining inputs drifted". Unit tests cover: adding an assignment re-records, changing a value re-records, reordering keys does not, an empty assignment does not. `snapshot check` still reports 1 fresh, 0 stale
- [X] T012 [P] Add the `obl-bhv-`/`obl-cmb-` id constructors to `crates/conformance/src/obligation.rs`, hashing over sorted assignment keys via the existing `hash8` helper per `contracts/obligation.md` — `behavior_obligation_id(behavior, &[Condition])` and `combination_obligation_id(operation, assignment)`. The `hash8` helper is **local**, not the private one in `inventory.rs`/`clause.rs`: those hash a schema pointer and a prose excerpt, so coupling to either would tie obligation identity to an unrelated record's field set. Only the truncation convention (first 8 lowercase-hex of SHA-256) and the `0x1f` separator are shared. `canonical_context` sorts conditions AND each condition's value subset, both of which are sets and so must not perturb the id
- [X] T013 [P] Add a unit test in `crates/conformance/src/obligation.rs` proving two assignments differing only in key order produce the same id — `assignment_key_order_does_not_change_the_combination_id` builds the two assignments from `IndexMap`, which PRESERVES insertion order, so a function that failed to sort internally forks visibly (a `BTreeMap` would have sorted on the caller's behalf and made the test vacuous); it also asserts the fixtures really do iterate differently, and re-checks agreement through a `Vec` in a third order and a `BTreeMap`. Five further tests cover substance-tracking, separator injectivity, cosmetic context reordering, and prefix disjointness — 6/6 pass
- [X] T014 Update the hermetic-conformance convention comment block in `.config/nextest.toml` (currently lines 43–73) to enumerate this feature's new binaries, recording the live/hermetic classification each one gets (Constitution VII; FR-073, FR-074) — documentation only: a 024 section appended to the existing block, forward-referencing binaries later tasks create (the same style the block already uses). No real overrides added; `parity_conformance_docker`'s wiring stays T020/T021's job. Also records that the `coverage` command group is a bin surface, not a test binary. All six profiles still parse (`cargo nextest list --profile <p>` exits 0 for default/dev-fast/full/ci/mvp-integration/parity)
  - **Hermetic, NO override** (run in every profile by the standing convention): `coverage_model`, `obligation_gating`, `workflow_coverage`, `error_path_coverage`, `denormalized_coverage` (conformance crate)
  - **Hermetic with an `fs-heavy` override** mirroring `observation_faults`: `injection_faults`, `prereq_faults` (parity-harness crate)
  - **Live, per-profile overrides in ALL profiles**: `parity_conformance_docker` only (T021)

**Checkpoint A**: US1 and US2 can begin.

### Block B — Live execution infrastructure (blocks US3–US6 only)

- [X] T015 Reshape `crates/deacon/tests/parity_conformance_runner.rs` from one test function into one driver function per `resourceGroup` value, each filtering the declarative case set by group (research Decision 4) — the one serial `#[tokio::test]` became two driver functions, `conformance_group_none` (66 cases) and `conformance_group_fs_heavy` (0 today), over a new shared `parity_harness::driver`. This binary keeps only the groups needing NO container runtime; `fs-heavy` stays here because its own model definition is "significant filesystem operations, no Docker" — putting it behind a daemon prerequisite it never touches would be the mirror of the registry lie T020 fixes. SC-013 holds: all four `ResourceGroup` variants now have a driver across the two binaries, so a case with an existing group is a pure data edit; a hermetic unit test (`every_resource_group_is_owned_by_exactly_one_binary`) fails if a fifth variant is added with no driver
- [X] T016 Create `crates/deacon/tests/parity_conformance_docker.rs` driving the Docker-backed resource groups, including the error-path tier — `conformance_group_docker_shared` (12 cases today) and `conformance_group_docker_exclusive` (0 today), both fail-loud on absent Docker/oracle before selecting anything. The error-path tier needs no code here: `errorPathTier` is not yet a case-model field (US4 T101), and whatever area an error-path case lands in, a Docker `resourceGroup` routes it to this binary with no change to this file. An empty group is NOT a silent skip — it still writes the fragment that proves the binary ran, and says so on stderr
- [X] T017 Add a per-case `tokio::time::timeout` of 5 minutes to the case loop in `crates/parity-harness/src/runner.rs`, failing loudly with the case id on expiry (FR-077b) — the bound wraps `run_case` itself (`CASE_TIMEOUT`), so EVERY caller gets it, not just the driver loop, and expiry is a new `HarnessError::CaseTimeout { case, bound }` naming the case id. It is strictly wider than `exec`'s per-INVOCATION bound: a case that hangs BETWEEN invocations (a teardown that never returns, a probe against an unresponsive daemon) is invisible to that one. The driver records a timeout as that case's failure and KEEPS GOING — one wedged case must not cost the tier its remaining coverage — while every other `HarnessError` still aborts the group fail-loud. Abandoning the future drops it, which runs the RAII workspace guard, so a timed-out case still reclaims its container/network/volume/temp dir
- [X] T018 Add bounded concurrency via `JoinSet` plus a semaphore to the Docker driver, routing every blocking `docker inspect` through `spawn_blocking` so the executor is never blocked (Principle V) — `drive_group` spawns each case into a `JoinSet` bounded by a `tokio::sync::Semaphore` sized from the group (`docker-shared` 4, matching the nextest group's `parallel-4`; `docker-exclusive` 1; config-only groups left at 1 so a green lane is not perturbed as a side effect of the reshape). Results are collected in completion order then id-sorted, so the verdict report stays byte-stable. Audited every blocking call on the now-concurrent path, not just `docker inspect` (which was already offloaded): `execute_ops` created the temp workspace and recursively copied fixture trees inline; `spec-expectation` / `snapshot` / `invariant-metamorphic` held the workspace guard to end of scope so `deacon down` + several `docker` calls ran on the executor at drop; and `live_differential` offloaded deacon's teardown but not the reference's. All now route through one `runner::release_workspace`
- [X] T019 Add the 30-minute tier wall-clock assertion to `crates/deacon/tests/parity_conformance_docker.rs`, reporting elapsed time and the slowest cases on failure (FR-077a, research Decision 10) — measured and asserted by the binary, with nextest's `slow-timeout` raised to 35m as the backstop ABOVE it (a backstop that fired first would replace a diagnosable failure with "the binary was slow"). The tier spans sibling nextest PROCESSES, so each group records its span into `target/parity/tier/<binary>/<run>/<group>.json` keyed by `NEXTEST_RUN_ID`; the fold is `max(finished) - min(started)` — the true wall clock, not the sum (which would over-count concurrent groups) — and the run-id scoping stops yesterday's artifact widening the span to a day. A process running before its siblings sees a partial fold, which can only UNDER-report, so the tier can never pass by measuring less than happened. Trivially passes today (12 cases); load-bearing as US3–US5 add cases
- [X] T020 Register `parity_conformance_docker` in `fixtures/parity-corpus/registry.json` with `docker_required: true`, and correct the stale `docker_required: false` on `parity_conformance_runner` — the runner's `false` was stale because 12 of its cases drove Docker; the T015/T016 split makes it TRUE rather than papering over it, so the correct edit is to keep `false` and let the split earn it (asserted, both directions, by T022). Registering a seventh live binary broke `baseline generate` (`UnknownProgram`): the baseline records a PRE-MIGRATION world, so a binary created after the freeze has nothing to enumerate. Added an explicit `POST_FREEZE_PROGRAMS` allow-list in `baseline.rs` — an allow-list, not a fallthrough, so a typo'd registry name is still `UnknownProgram`. Erroring instead would have made the parity registry unable to grow at all
- [X] T021 Add `parity_conformance_docker` to the `[profile.parity]` `default-filter` and to the exclusion filters in `[profile.default]`, `[profile.dev-fast]`, `[profile.full]`, `[profile.ci]`, and `[profile.mvp-integration]` in `.config/nextest.toml` — all six `binary(=…)` sites updated symmetrically with `parity_conformance_runner`, plus a `[profile.parity]` override placing it in the `parity` test group with the 35m backstop timeout (T019). T014's documentation-only forward reference left intact. Verified: all six profiles parse, and `cargo nextest list --profile parity` selects exactly the four conformance driver functions across the two binaries plus the five scenario binaries
- [X] T022 Extend `crates/deacon/tests/parity_registry_check.rs` to assert registry ↔ `tests/*.rs` ↔ `.config/nextest.toml` agreement for the new binary and that no `coverage`/`regressions` command leaks into the shipped `deacon` CLI — `the_docker_conformance_driver_is_registered_selected_and_excluded` names ONE binary even though `check_nextest_profiles` covers it generically, because a half-landed split (source present, registry entry or filter missing) reads as "Docker cases covered" while nothing selects them under `--profile parity`; it also asserts the SIBLING's `docker_required: false`, since a still-Docker-requiring runner would mean the split bought nothing. `no_coverage_or_regression_command_reaches_the_shipped_cli` walks the clap tree ONE LEVEL DEEPER than the existing top-level check: `coverage` has subcommands, and the cheapest way to leak one is to hang it off an existing consumer command. Positive control (`up` must be found) so a parse matching nothing cannot pass vacuously. Both functions authored in `FUNCTION_UNITS`, as 023 US4 did for its seven fault-injection guards. `the_surviving_set_is_mutually_consistent`'s live-set count 6 → 7

**Checkpoint B**: US3–US6 can begin.

---

## Phase 3: User Story 1 - See the shape of the hole (Priority: P1) 🎯 MVP

**Goal**: A constrained context model with applicability rules, plus generated reports that
divide the valid combination space into covered and uncovered — delivering value with zero
new conformance cases.

**Independent Test**: Run `coverage report` against the existing, unchanged 88-case set and
confirm it enumerates the valid space, marks what those cases cover, and lists the remainder.

### Tests for User Story 1

- [X] T023 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: the report lists every valid combination and omits every rule-forbidden one (scenario 1) — `the_report_lists_every_valid_combination_and_omits_every_forbidden_one` compares the report against an INDEPENDENT re-enumeration (`brute_force_valid_pairs`, a second reading of the same records) in BOTH directions; calling the production evaluator to check the production evaluator would have been a tautology
- [X] T024 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: an excluded combination appears in neither population and names its excluding rule id (scenario 2) — `an_excluded_combination_is_absent_from_both_populations_and_names_its_rule` pins `read-configuration × sdim-container-state=running`: present in `excluded` with a resolvable rule id, absent from every pair AND from the whole inventory
- [X] T025 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: two generations from an unchanged record are byte-identical (scenario 3, SC-010) — byte-compares two generations of the obligations, all three report JSON documents, and the rendered Markdown
- [X] T026 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: a value permitted by no rule is reported as a dead value (scenario 4, FR-010) — one ADDED rule strands `sdim-features: lockfile` under every operation; asserts both the report's `deadValues` and the V26 violation, and that the unmodified registry has none (so the assertion is not vacuous)
- [X] T027 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: obligations of a modelled-but-inactive environment are enumerated as `inactive-environment`, counted as neither covered nor gap (scenario 5) — a behavior constrained to `dim-runtime: podman` under the active Docker profile buckets `inactive-environment`, claims no evidence, stays IN the denominator, and the six buckets partition the obligation set
- [X] T028 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: marking a fixture profile active re-buckets its obligations with zero changes to model, rules, or cases (scenario 6, SC-015, FR-004b) — the ONLY edit is which profile is `active`; asserts `scenario.json`, `applicability.json`, and every `cases/<area>.json` are byte-identical across the two runs while the obligation re-buckets from `inactive-environment` to `covered`
- [X] T029 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: the full Cartesian product is never materialized — obligation count equals the enumerated valid-pair count, not the product of all dimension sizes (FR-013) — 718 combination obligations vs a 4,800-value Cartesian product; also asserts every combination's pinned-dimension count equals its declared arity
- [X] T030 [P] [US1] Test in `crates/conformance/tests/coverage_model.rs`: `coverage generate` writes **only** `conformance/obligations/obligations.json` — it never modifies a file under `conformance/registry/obligation-dispositions/`, a case, a behavior, a waiver, or a gap. Assert by fingerprinting the registry tree before and after a generation run (FR-018, the invariant `only_the_refresh_bin_writes_committed_snapshots` guards for snapshots) — fingerprints the whole `conformance/` tree before and after a real `coverage generate` subprocess run, so the guarantee covers files the test never thought to name; `obligations/obligations.json` is the single changed path

### Implementation for User Story 1

- [X] T031 [US1] Implement the `sdim-` dimension and `rule-` record models in `crates/conformance/src/scenario.rs`, reusing `model::Condition` verbatim for rule conditions — `ScenarioDimension`/`ApplicabilityRule`/`HighRiskTriple` + the `ApplicabilityFile` two-record-kind document (the `MappingFile` precedent, since `Collection`'s `deny_unknown_fields` admits only `schemaVersion`+`records`). The four new id prefixes also join `RecordType`, so V2's grammar/uniqueness/prefix↔type checks cover them
- [X] T032 [US1] Implement the applicability evaluator in `crates/conformance/src/scenario.rs` per `contracts/scenario-model.md`: a combination is invalid iff some rule's conditions are all satisfied; rules are pure exclusions with no ordering dependence — a rule constrains only the dimensions it names; a PARTIAL combination missing a named dimension is inconclusive, never excluded; a conditionless rule excludes nothing (treating it as excluding everything would turn one malformed record into an empty denominator)
- [X] T033 [US1] Implement dimension-level pruning in `crates/conformance/src/scenario.rs`: a dimension all of whose values are excluded under an operation is inapplicable and contributes no pairs — `applicable_dimensions` returns `(dimension, permitted values)` so the caller never recomputes the filter it just paid for
- [X] T034 [US1] Add loading of `scenario.json` and `applicability.json` to `crates/conformance/src/load.rs` — `scenario.json` via the existing `load_collection`; `applicability.json` via its own `load_applicability_file` (two record kinds in one document). A malformed file yields empty AND a located `SchemaError`, never a silently partial rule set that would quietly widen the valid space
- [X] T035 [US1] Author `conformance/registry/scenario.json` with the six required dimensions and the minimum value sets from data-model.md §1 (FR-003, FR-005 – FR-009) — six dimensions, exactly the FR-005 – FR-009 minimum value sets
- [X] T036 [US1] Author `conformance/registry/applicability.json` with the exclusion rules, each carrying a `ground` — at minimum: no container state for operations that create no container; no Features for `down`/`doctor`; no structured output mode for operations emitting no document — **8 rules**, each grounded in a mechanism. Deliberate divergence from data-model.md §2's illustrative rule recorded in RULES.md: it excludes only the three container-ful states and NOT `none`, because excluding `none` too would leave `read-configuration`/`build`/`doctor` with no valid total assignment at all while data-model.md §3 requires a case to assign every dimension — the operation would become unrepresentable
- [X] T037 [US1] Implement pairwise obligation generation in `crates/conformance/src/obligation.rs` per `contracts/obligation.md`, partitioned by operation, excluding environment dimensions, emitting directly from the two-dimension cross product — 718 pairs. Pair emission also re-checks the 3-element combination, so a future rule naming BOTH pair members is honoured without changing the loop
- [X] T038 [US1] Implement behavior-obligation generation in `crates/conformance/src/obligation.rs` from each behavior's `applicability` — **exactly one obligation per behavior**: an empty applicability is one universal context (zero would erase the behavior from the denominator), and a non-empty one IS the context, because a condition pins a value SUBSET meaning "any of these" — expanding it per value would multiply the two kinds, which FR-019 forbids and research D2 rejected on arithmetic
- [X] T039 [US1] Implement `coverage generate` in `crates/conformance/src/bin/conformance.rs` with atomic temp-file + `fs::rename` write, id-sorted output, and `--out` redirection — V26 runs first and, on any violation, reports and writes NOTHING (contracts/coverage-cli.md "reported before any write"). Default path resolves as a registry sibling via the new `obligations_file_for`, mirroring `clause_paths_for`/`migration_paths_for`
- [X] T040 [US1] Implement `coverage check` in `crates/conformance/src/bin/conformance.rs`, regenerating in memory and byte-comparing, naming the first differing unit id on drift — exit 0 match / 1 drift (naming the first differing unit id and whether added, removed, or changed) / 2 unreadable committed file
- [X] T041 [US1] Implement V26 (scenario-model integrity: dead values, unknown dimension/value in a rule, rule with no ground, partial or invalid case `scenarioContext`) in `crates/conformance/src/validate.rs` as a new function, not inline growth — a free function, not a `Checker` method. Also covers empty/duplicated value sets, missing required dimensions, rule arity <2, and malformed triples. A registry declaring no scenario dimensions opts out entirely, so the pre-024 fixture registries stay silent
- [X] T042 [US1] Implement V27 (obligation provenance: committed ≠ regenerated, revision mismatch, obligation referencing a removed value) in `crates/conformance/src/validate.rs` — runs alongside V14/V17 in `validate_path_with_inventory` (it needs the registry's `obligations/` sibling). A declared model with NO committed inventory is a violation, not a skip: an absent file would otherwise read as "nothing to check"
- [X] T043 [US1] Implement coverage matching in `crates/conformance/src/coverage.rs`: a pair is covered iff a declarative case's `scenarioContext` matches both values under the same operation and the case is executable — a legacy carrier counts only while an open residual names it, so coverage decays with the residual instead of outliving it. Behavior obligations reuse `Coverage::evaluate` rather than a second evaluator, so the two denominators can never disagree
- [X] T044 [P] [US1] Implement the `coverage-pairwise` renderer in `crates/conformance/src/coverage_report.rs` per `contracts/coverage-report.md` §1, including the `excluded` list with rule attribution and the `deadValues` list — buckets are the five FR-026 ones plus `undispositioned` (the honest sixth state until US2's disposition records exist). `excluded` carries single-dimension prunes AND pair-level exclusions, both attributed
- [X] T045 [P] [US1] Implement the `coverage-operations` renderer in `crates/conformance/src/coverage_report.rs` per §3, including `missingInputClasses` and `missingConfigSources` — `inputClasses` are DERIVED (the record has no input-class field until US3); `boundary`/`unsupported` have no derivable signal and are therefore always reported missing. Cases are attributed by the subcommands they actually invoke, which keeps the report informative for the 88 pre-scenario-model cases (read-configuration 65, up 12, exec 4, seven operations at zero)
- [X] T046 [P] [US1] Implement the `coverage-observables` renderer in `crates/conformance/src/coverage_report.rs` per §4, including `channelsBelowFloor` and `unscopedNormalizationRules` — `fields` counts what is COMPARED (a `jsonSubset` leaf, or an allowed-difference path, which exists only because the comparison reaches it), never what is captured. Reports 6 channels below the three-case floor and `chan-file-content` at zero
- [X] T047 [US1] Implement the Markdown renderers in `crates/conformance/src/coverage_report.rs`, rendering from the same ordered in-memory model as the JSON so the two can never disagree — rendered from the same in-memory model in the same order; T025 byte-compares them
- [X] T048 [US1] Implement `coverage report` in `crates/conformance/src/bin/conformance.rs`, read-only with respect to the record, exiting non-zero only on load/model failure (FR-063) — read-only; exit code never reflects coverage (reporting never gates, gating never reports)
- [X] T049 [US1] Add a hermetic determinism test in `crates/conformance/tests/registry_valid.rs` running `coverage check` against the committed obligations — `committed_obligations_match_a_fresh_regeneration` in the existing hermetic PR gate
- [X] T050 [US1] Add the scenario-model and obligation-provenance sections to `conformance/RULES.md`, keeping the `validate.rs` ↔ `RULES.md` lockstep the index exists to make checkable — new `## Scenario model and obligation provenance (V26 – V27)` section at the anchor the T004 index rows already link to
- [X] T051 [US1] Commit the generated `conformance/obligations/obligations.json` and record the resulting obligation count in the PR body — **745 obligations: 718 combination (all arity 2; no `hrt-` triples until US3) + 27 behavior**. `validate` and `certify` both still exit 0, and `certify`'s output is unchanged (0 blocking, 10 waived)

**Checkpoint**: The denominator exists, is deterministic, and the shape of the hole is visible
with zero new cases. This is the MVP.

---

## Phase 4: User Story 2 - Nothing applicable stays unclassified (Priority: P1)

**Goal**: Every applicable obligation carries exactly one disposition, and gaps and expired
waivers block strict certification.

**Independent Test**: Introduce an undispositioned obligation into a fixture registry, confirm
`certify` fails naming it, then apply each of the four dispositions and confirm two permit
release and two block.

### Tests for User Story 2

- [X] T052 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: an undispositioned obligation fails `certify` naming the obligation, behavior, and context (scenario 1) — two tests, one per obligation kind: a combination names its operation AND its assignment, a behavior names its behavior AND its context (`context: any` when it applies everywhere), so the diagnostic is actionable without opening the machine-owned inventory
- [X] T053 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: a `gap` disposition blocks `certify` (scenario 2) — and produces NO violation of its own: an admitted gap is a well-formed judgement, so it blocks through the `gap-` record it names (`BlockingKind::Gap`), which is where gap semantics already live. A second test proves a `gap` disposition naming no declared record is V29 — an admission nothing backs would block nothing, so the admission would be free
- [X] T054 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: a waiver expiring before `--today` blocks and is named (scenario 3, SC-009) — the SAME fixture certifies at one date and blocks at another, so what changed is the date and not the registry. The blocker names the **waiver**, the record V6 names everywhere else. `certify` gained a `today` parameter for this one date-dependent condition; a sibling test pins the boundary (`expires == today` still passes), so the gate and V6 cannot disagree by a day
- [X] T055 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: unexpired `waived` and `non-testable` do not block and are enumerated (scenario 4) — asserts the six counts SUM to the total, so a bucket cannot be quietly folded into another to make one number look better
- [X] T056 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: two dispositions on one obligation fail validation (scenario 5) — V28 names BOTH records, and the obligation buckets `undispositioned` rather than falling through to the evidence (a behavior changed to make this test true rather than to make it pass: the evidence fallback answers "nobody has looked at this yet", and someone plainly has, twice)
- [X] T057 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: a filler rationale is rejected (scenario 6, FR-025) — BOTH failure shapes the reused V23 test recognizes, because they fail for different reasons: a bare restatement too short to have explained anything ("Out of scope."), and prose long enough to look considered that names no ground. Contrasted against nine grounded rationales of the same disposition kind in `base`, so what is judged is the prose, not the word. Two sibling tests cover V29's other arms: a triple dispositioned by argument, and a `waived` disposition backed by a blanket (`field: "*"`) waiver scope
- [X] T058 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: adding a behavior without dispositioning its generated obligations is rejected (SC-014) — the fixture gives the new behavior a case, so V5 is satisfied and NOTHING blocks as `uncovered`; it still blocks as V28. That is the whole point of the second denominator: behavior-level coverage can be complete while the obligation queue is not
- [X] T059 [P] [US2] Test in `crates/conformance/tests/obligation_gating.rs`: a disposition whose obligation no longer resolves is reported stale (FR-024) — driven by renaming a dimension VALUE, not by hand-writing a dangling id. Both produce a stale record; only the model edit shows the mechanism the drift workflow actually meets, and it proves the other half at the same time — the re-hashed obligations come back undispositioned, one per stranded record, because disposition is never inherited by name
- [X] T060 [P] [US2] Create the fixture registries covering each blocking and non-blocking disposition (FR-078) — landed at **`fixtures/conformance/obligation-gating/`**, not `crates/conformance/tests/fixtures/`: every one of the 24 existing conformance fixture registries lives under `fixtures/conformance/`, and the loader helpers (`workspace_root().join("fixtures/conformance")`) resolve there. A second fixture root would have forked the convention for one feature. Shaped as `base/` + eight overlay directories holding ONLY the files that differ, so each scenario's one interesting edit is the literal content of its directory rather than three lines inside a 200-line copy; the machine-owned obligation inventory is generated at test time because V27 already makes it a pure function of the registry, so a committed copy per variant would be a second thing to keep in sync with no independent authority. `base/` doubles as the control (clean, certifies) and as T055's scenario. See that tree's README.md

### Implementation for User Story 2

- [X] T061 [US2] Implement the `odp-` disposition record model in `crates/conformance/src/obligation.rs` with deserialize-time exactly-one-of enforcement across `cases`/`rationale`/`waiver`/`gap` — a raw intermediate + `TryFrom`, following `residual.rs`. Enforced at load rather than as a V-class because a record whose payload disagrees with its word is a half-stated judgement, not a nuance for a validation pass to interpret; that leaves V28 owning arity ACROSS records and V29 owning semantics, both of which genuinely need the rest of the registry. `RecordType::ObligationDisposition` (`odp-`) joins the V2 id grammar / uniqueness / prefix↔type checks
- [X] T062 [US2] Add loading of `conformance/registry/obligation-dispositions/<area>.json` to `crates/conformance/src/load.rs` — `load_dir_collections`, the same call `behaviors/` and `classifications/` use, so adding an area is a file and not a code change. A missing directory is empty — which is not "nothing to decide" but "nothing decided yet", and V28 says so
- [X] T063 [US2] Implement disposition resolution in `crates/conformance/src/obligation.rs`: explicit only, no inheritance, no default — and no winner picked from a conflict. `audit_dispositions` is the SINGLE join behind V28, `certify`'s blocking set, and `coverage scaffold`'s worklist, for the reason `join_inventory` exists: if the scaffold's idea of "undispositioned" could drift from V28's, filling in everything the scaffold printed would still fail the gate. `evaluate_obligations` consults it with a stated precedence — derived `inactive-environment` first (an author must not retire an obligation by declaring its environment inactive), then the single record, then the evidence; the last step is a REPORTING fallback, never a disposition default, since V28 fires regardless of the bucket printed
- [X] T064 [US2] Implement V28 (zero or >1 dispositions on an applicable obligation) in `crates/conformance/src/validate.rs` — a free function, wired into `run()` so `report` and `certify` see it and not only the `validate` command. "Applicable" excludes `inactive-environment` obligations (nobody owes a decision about an environment that is not active); scoped to registries declaring a scenario model, exactly as V27 is, or every pre-024 fixture would suddenly owe one decision per behavior
- [X] T065 [US2] Implement V29 (filler rationale, triple dispositioned by rationale/waiver, stale disposition) in `crates/conformance/src/validate.rs`, reusing the ground-naming test V23 already applies to `outOfScopeRationale`, and reject a `waived` disposition whose backing waiver scope is blanket rather than specific (FR-023, mirroring the FR-032 rule V19 already enforces) — plus dangling payload references (a case/waiver/gap the registry does not declare). The blanket-scope rule adapts to `Scope`'s actual shape: it is a closed union and only `StateField` can be widened (`field` matches exactly OR as a trailing-`*` prefix, so a bare `"*"` matches every field of the fixture — a global ignore in a waiver's clothes, and one that could never self-invalidate). `CorpusCase` names one case of one corpus and has no wildcard form at all, so there is nothing there to reject; inventing a blanket shape for it out of symmetry would reject correct records. **`executable_case_ids` is deliberately NOT reused** for the `case` payload's "all executable" requirement: that predicate encodes the 023 residual-carrier decay, and applying it here would declare six live integration-test binaries nonexistent
- [X] T066 [US2] Add `BlockingKind::Obligation` with a `code` field to `crates/conformance/src/certify.rs`, mirroring the existing `Constraint`/`Clause` shape so the output format does not fork — carrying `V28`, `V29`, or `V6`
- [X] T067 [US2] Wire V28/V29 and expired-waiver dispositions into the blocking set in `crates/conformance/src/certify.rs` — via the SAME `check_obligation_dispositions` `validate` runs, so the release verdict cannot depend on which command was run. `certify` gained a `today` parameter for the expired-waiver arm. A `gap` disposition gets NO blocker of its own: V29 already requires the `gap-` it names to resolve, and every gap record already blocks, so a second entry would double-count one fact
- [X] T068 [US2] Add the five-bucket reporting (`covered`/`waived`/`non-testable`/`gap`/`inactive-environment`) to `crates/conformance/src/certify.rs`, never folded together (FR-026) — plus the `undispositioned` queue as a separate count (folding it into `gap` would overstate what is known; into `covered`, understate the hole). Reported ALONGSIDE the behavior-level numbers, never instead of them: the two denominators answer different questions, and collapsing them would let progress on one hide the absence of progress on the other
- [X] T069 [US2] Implement `coverage scaffold` in `crates/conformance/src/bin/conformance.rs`, emitting `UNREVIEWED`-sentinel skeletons to **stdout** only, never writing the registry — the sentinel appears TWICE per skeleton: in `disposition`, where the closed enum refuses it and so forces the author to pick one of the four words rather than inherit a default, and in `rationale`, which contracts/coverage-cli.md names and which is exactly the shape V29's filler test rejects if it survives. Exits 0 when nothing is undispositioned: making the workflow's terminal state an error would put success in the failure channel
- [X] T070 [US2] Add loader rejection of the `UNREVIEWED` sentinel, so a scaffold committed unedited fails — implemented in the `odp-` deserializer (`obligation.rs`) rather than in `load.rs`, because that is where the record's own field rules already live (`residual.rs` sets the same precedent); `load.rs` calls it. A blank payload is refused by the same helper: a field written as `""` is an authoring mistake, not an intentional absence
- [X] T071 [US2] Author the initial `conformance/registry/obligation-dispositions/<area>.json` files, dispositioning every obligation generated in T051 — **745 records across 16 area files: 27 `case` (26 behavior + 1 combination… see below), 718 `gap`, 0 `waived`, 0 `non-testable`**. Precisely: all **27 behavior obligations → `case`**, pointing at the cases already linked to each behavior in `cases/<area>.json` (all 27 have ≥1, so none needed a waiver or gap); all **718 combination obligations → `gap`**, because no committed case declares a `scenarioContext` yet (US3–US5) and `non-testable` would be an argument standing in for evidence nobody has. The 718 point at **10 shared `gap-pairwise-<operation>` records**, not 718 individual ones: the gap is a single fact per operation, and 718 restatements would be noise a reviewer cannot read. Those records carry `behaviors: []` — this is missing coverage in the combination space, not a claim about any behavior, and linking one would flip that behavior's coverage state and R7 disposition for an unrelated reason. Generated mechanically from the committed inventory (obligation → gap is determined purely by `operation`), never hand-typed. **`certify` now exits 1 with 10 blocking gaps — the checkpoint, not a regression**; `validate` stays exit 0. The one test asserting the real registry certified with an EMPTY blocking set was retargeted at the invariant that still holds: nothing but a declared coverage gap blocks
- [X] T072 [US2] Add the obligation-disposition sections to `conformance/RULES.md`, including the gap-vs-waiver-vs-rationale distinction and the triple restriction — at the `#obligation-dispositions-v28--v29` anchor the T004 index rows already link to. Gives equal weight to the reverse mistake: reaching for `gap` when the difference is already characterized inflates the blocker list with things nobody intends to change, and a blocker list that is mostly noise stops being read

**Checkpoint**: Undispositioned work now blocks the release. US1 + US2 are the complete
hermetic foundation.

---

## Phase 5: User Story 3 - Deterministic coverage of the shared consumer workflow (Priority: P2)

**Goal**: Deterministic cases across the whole consumer workflow, spanning all five input
classes, for all ten operations.

**Independent Test**: Run the case set against an unmodified tree and confirm it passes; then
perturb one workflow stage and confirm failure attributed to that stage.

**Depends on**: Phase 2 Block B (T015–T022).

### Tests for User Story 3

- [X] T073 [P] [US3] Test in `crates/conformance/tests/workflow_coverage.rs`: every case reaches a definite verdict and none is skipped, ignored, or conditionally excluded (scenario 1, SC-012) — refuses all THREE shapes of "skipped", not the easiest one: no driver selects it (the `ResourceGroup` partition is asserted total AND both drivers asserted non-empty, so one binary's selection cannot go dead), nothing can observe it (a channel with no observer, or no channel at all — the latter is worse, since it can only ever pass), and its evidence cannot be gathered (a `spec-expectation` channel with no assertion). An `#[ignore]`-style opt-out is deliberately NOT asserted: the record has no field that could express one, and asserting the absence of a field the schema cannot represent is a test of `serde`
- [X] T074 [P] [US3] Test in `crates/conformance/tests/workflow_coverage.rs`: each workflow stage shows ≥1 valid-behavior case and ≥1 case per permitted input class (scenario 2, FR-040) — read off the SAME report a reviewer reads rather than recomputed, so the test cannot pass while the report says something else. **All ten operations now report `missingInputClasses: []` and `missingConfigSources: []`.** Required a declared `TestCase.inputClass`: inference could only ever distinguish three of the five classes, so `boundary` and `unsupported` were reported permanently missing however many such cases existed
- [X] T075 [P] [US3] Test in `crates/conformance/tests/workflow_coverage.rs`: a reference-lenient case is paired with a spec-expectation twin pinning the direction (scenario 3, FR-043) — the twin is matched on a SHARED BEHAVIOR, not on a name suffix: a naming convention can be satisfied by a file rename, a shared behavior cannot. 11 reference-lenient cases, each twinned
- [X] T076 [P] [US3] Test in `crates/conformance/tests/workflow_coverage.rs`: an operation with no pinned-reference equivalent uses `spec-expectation` and the report states the substitution (scenario 4, Assumption 5) — measured against the pinned oracle's `--help`: `down` and `doctor` do not exist there at all, and `templates apply` exists but its required `--template-id` takes only an OCI reference while deacon takes the template positionally, so no shared argv can invoke both. All three are recorded in `OPERATIONS_WITHOUT_RUNNABLE_DIFFERENTIAL` with the substitution the report prints, and `reference-lenient` is then NOT a permitted input class for them — demanding a case that cannot be written turns an honest measure into noise
- [X] T077 [P] [US3] Test in `crates/conformance/tests/workflow_coverage.rs`: a case whose observable output could vary between runs produces the same verdict on repeated execution, asserted per case rather than only in aggregate (scenario 5) — repetition itself is the live tier's job; what is checkable hermetically is the thing repetition would EXPOSE, so each case is inspected for the three ways a verdict moves: a `snapshot` oracle over a channel whose evidence carries container/project/image ids, an assertion embedding a run-varying literal (temp path, or a 64-char hex run that reads as a container id), and a runtime-touching case with no Docker `resourceGroup` and therefore no isolated workspace
- [X] T078 [P] [US3] Test in `crates/conformance/tests/workflow_coverage.rs`: every high-risk triple is covered by an executable case, never by rationale or waiver (SC-003, FR-015) — **14/14 covered**. V29 already refused a rationale or a waiver on a triple, so the failure this test exists to catch is the remaining one: a triple dispositioned `gap`, which is honest, permitted by FR-015, and still leaves SC-003 unmet. It caught exactly that during authoring (`hrt-upgrade-…`, see T079)

### Implementation for User Story 3

- [X] T079 [US3] Author the `hrt-` high-risk triples in `conformance/registry/applicability.json`, at least twelve, each with a `reason` (SC-003, FR-014, FR-016) — **14**, spanning nine operations, each selected for an interaction whose halves are individually covered and whose defect mode is nameable (Compose × Feature × re-entry; an inherited Dockerfile whose build path is config-dir-relative across an extends chain; a `--image-name` overlay on a Feature-extended build, which this project has shipped wrong once). `doctor` is deliberately NOT represented: its layering, Features and container-state each permit exactly one value, so a "triple" there carries no more information than the pair it contains. `hrt-upgrade-…` was selected as `cli-overlay` and re-selected as `extends-chain` after measuring that `upgrade` exposes no overlay flag reaching Feature resolution — a triple with no invocable case is a selection mistake, not a gap to admit
- [X] T080 [US3] Implement the `coverage-triples` renderer in `crates/conformance/src/coverage_report.rs` per `contracts/coverage-report.md` §2, carrying `reason` into the report — joined to generation by RECOMPUTING the substance-anchored obligation id (`triple_obligation_id`, now shared with `generate_triples`) rather than matching by position or arity, so a reordered or renamed triple still resolves to the disposition that judged it. Uncovered a real disagreement while wiring it: the report derived dead values from the emitted obligations, so a triple naming a value the PAIR space cannot reach silently rescued it — the opposite of what V26 says. Liveness is now computed from arity-2 obligations only, matching `check_scenario_model`
- [X] T081 [US3] Extend V29 in `crates/conformance/src/validate.rs` to reject a triple dispositioned by rationale or waiver — the rationale/waiver half already existed from US2; what was missing is that FR-015 asks for an EXECUTABLE case, not merely a declared one. A triple dispositioned `case` against a legacy carrier whose residual has closed names a program that no longer exists; on any other obligation that is a coverage question, on a triple it is the quietest way to lose the evidence
- [X] T082 [P] [US3] Add configuration-discovery and JSON/JSONC parsing cases to `conformance/registry/cases/read-configuration.json` with fixtures under `conformance/fixtures/` (FR-027, FR-028) — all three spec-named locations plus the NEGATIVE half (a plain `devcontainer.json` at the workspace root is not one; without it "discovery works" would be satisfied by an implementation that searched everywhere). Surfaced a real divergence: the pinned oracle does not search `.devcontainer/<folder>/devcontainer.json` and exits non-zero where deacon resolves it — deacon is on the spec's side, recorded as `bhv-readconfig-discovery-locations` + `wvr-discovery-named-subfolder`. JSONC parsing (comments, trailing commas, duplicate keys, hard syntax errors) was already covered by the migrated error and tier-1 corpora, now annotated with the contexts they always exercised
- [X] T083 [P] [US3] Add variable-substitution and validation-timing cases to `conformance/registry/cases/read-configuration.json` (FR-029, FR-030) — substitution asserted on the OBJECT-shaped fields (`build.args`, `containerEnv`, `mounts`, `customizations.vscode.settings`), `customizations` named explicitly because it is the one that was actually missed (#312), plus `${localEnv:UNSET}` resolving to the empty string rather than staying literal. Validation timing is carried by the declared `expectFailurePhase` on every rejecting case, and by a new closed-enum rejection (`userEnvProbe: telepathy`) whose differential shows the reference accepting and echoing what deacon refuses
- [X] T084 [P] [US3] Add extends-chain and merge-precedence cases, including conflicts, cycles, and missing parents, to `conformance/registry/cases/read-configuration.json` (FR-031) — precedence over a THREE-link chain, because two links cannot distinguish "the child wins" from "the last file read wins": `LAYER` is set at every link and the child's survives, link-unique keys all survive, `postCreateCommand` comes from the middle link and `onCreateCommand` from the base. Cycles and missing parents were already covered by the migrated error corpus
- [X] T085 [P] [US3] Add Feature resolution and install-order cases — declared order, dependency-derived order, CLI override — to `conformance/registry/cases/up.json` (FR-032), and to `read-configuration` where the resolved order is inspectable without a container. Local Features throughout: same resolution and install path as an OCI Feature, no network, and inside the Docker tier's budget. The order assertion is a stdout regex, NOT `jsonSubset` — a subset match over an array is order-insensitive by design, so it alone would pass for an implementation that appended the CLI overlay after the declared set instead of resolving both together
- [X] T086 [P] [US3] Add lockfile production and consumption cases to `conformance/registry/cases/up.json` and `conformance/registry/cases/outdated.json` (FR-033) — production in `cases/upgrade.json` (`--dry-run`, which both CLIs support and which keeps the case from mutating its own fixture), consumption and version reporting in `cases/outdated.json`. `current`/`wanted` are pinned by an exact `:1.3.2` reference and asserted; `latest` is deliberately unpinned, because it is whatever the registry publishes next and an assertion on it would decay into a false failure
- [X] T087 [P] [US3] Add image-reference and Dockerfile build cases, including build arguments and build-time failure, to `conformance/registry/cases/build.json` (FR-034) — 7 cases from 1. The `--image-name` case pins the reported image AND the user's tag in one ORDERED match, because the defect this project shipped once was a tag pointing at the pre-Feature base while the build reported success. `chan-image` could not be used: it reads an inspect the runner captures at an `up`/`exec` boundary, and a build produces no container
- [X] T088 [P] [US3] Add Compose cases, including multi-service shapes and created project resources, to `conformance/registry/cases/up.json` (FR-035) — plus Compose cases under `build`, `exec`, `run-user-commands` and `down`, so `compose` is a covered configuration source for every operation that permits it. The created project resources are observed on `chan-process-graph`, which captures EVERY container matching the workspace label — an exit code cannot show a duplicated service container, and that is the failure mode
- [X] T089 [P] [US3] Add container creation and setup cases covering identity, labels, mounts, environment, and user to `conformance/registry/cases/up.json` (FR-036) — the six `cases/observable-state.json` cases already covered identity, labels, mounts, environment and user across image/Dockerfile sources and were annotated with the contexts they exercise rather than duplicated; what is genuinely new is a Feature marker read from INSIDE the container, the lesson from the `--image-name` defect that an outcome-only assertion reports success for an image with nothing in it
- [X] T090 [P] [US3] Add lifecycle-execution cases covering hook ordering, Feature-contributed hooks, and hook failure to `conformance/registry/cases/run-user-commands.json` (FR-037) — each hook appends its own name to a file inside the container, so the FILE is the observed order. The Feature-contributed hook is asserted with `grep -c` rather than a presence check: a Feature hook that runs on every invocation instead of once is present either way. The failing hook arrives as a command-line override so the preceding `up` can succeed — a hook failure during creation is a different case, and conflating them would leave neither pinned
- [X] T091 [P] [US3] Add restart and resume cases distinguishing first creation from re-entry to `conformance/registry/cases/up.json` using `invariant-metamorphic` (FR-038) — two, both `first-create-vs-restart`: re-entry into a running Compose project, and re-entry against a configuration that has SINCE CHANGED. The changed configuration arrives as `--override-config` because the runner materializes a fixture once and does not edit it mid-case; the observable situation is identical either way
- [X] T092 [P] [US3] Add exec, outdated, upgrade, down, and cleanup cases — including resources each removes and leaves behind — to the matching `conformance/registry/cases/<area>.json` files (FR-039) — removal is asserted by EXECING into the container that should no longer exist, because a `down` exit code is identical whether or not anything was removed, and `chan-process-graph` reads an inspect that a successful teardown leaves nothing to produce. What `down` LEAVES behind is recorded too: the Dockerfile case deliberately does not assert on the built image
- [X] T093 [P] [US3] Add `templates apply` and `doctor` cases to `conformance/registry/cases/templates.json` and `conformance/registry/cases/doctor.json` (FR-005) — the templates cases run `fs-heavy`, which now gets an isolated temp workspace with temp-dir-only cleanup: the group means "significant filesystem operations", and those must not land in the committed fixture tree every other case reads. The malformed template is a directory that EXISTS but carries no `devcontainer-template.json`, chosen over a non-existent path because a path that resolves to nothing locally falls through to an OCI lookup and the case would then measure the network. `doctor`'s cases pin the property that is easy to get backwards: an unparseable configuration must NOT stop the diagnosis
- [X] T094 [US3] Add the behaviors these cases require to `conformance/registry/behaviors/<area>.json`, each traced to a named source unit per research Decision 9, and disposition their generated obligations — **18 behaviors, all from source (a)**: the observable contract of a previously-uncovered operation. Each is registered in `POST_BRANCH_BEHAVIORS` with why it is not a variant of an existing claim, so the denominator guard reviews the growth instead of merely permitting it. Dispositions are regenerated MECHANICALLY from the same coverage match the report uses (as at T071): an obligation is `case` when a declarative case actually reaches it and `gap` otherwise, so the ledger and the report cannot disagree. 315/732 combination obligations flipped from `gap` to `case`; 45/45 behavior obligations `case`; 0 undispositioned
- [X] T095 [US3] Re-review the `non-testable` clause classifications whose ground was the absence of a container-backed tier, updating `conformance/registry/clause-classifications/*.json` where the ground no longer holds — the 156 `non-testable` records carry only two distinct rationales, and only the 56 "pending a dedicated conformance behavior" ones are the reservoir Decision 9 names; the other 100 are descriptive prose, whose ground is unchanged. **8 re-classified to `behavior-mapped`** where a US3 behavior now covers them: the lifecycle sequence and its per-command exit requirement (`reference`), the lockfile's name, `version`, `integrity` and `resolved` fields (`lockfile`), and `dependsOn` install-before plus the tool's responsibility to calculate an order (`feature-dependencies`). The `feature-dependencies` round-priority ALGORITHM clauses are deliberately left: the new cases pin the resulting order, not the intermediate rounds, and mapping them would claim evidence for steps nothing observes. The remaining 48 stay queued for T150

**Checkpoint**: All ten operations carry cases across the permitted input classes.

---

## Phase 6: User Story 4 - Parity does not stop at acceptance (Priority: P2)

**Goal**: A container-backed error-path tier whose cases begin from inputs configuration read
accepts on both sides.

**Independent Test**: Take an input both sides accept at read time, run the tier, and confirm
it reports the later-stage outcome for both sides with a definite verdict.

**Depends on**: Phase 2 Block B (T015–T022).

### Tests for User Story 4

- [X] T096 [P] [US4] Test in `crates/deacon/tests/parity_conformance_docker.rs`: an error-path case records the failing stage and each side's outcome (scenario 1) — `an_error_path_case_records_the_failing_stage_and_each_sides_outcome` runs the whole tier and asserts four things a green run does not by itself establish: a definite verdict, the DECLARED stage in the exit-code detail, that deacon's declaring operation really did fail, and (for a differential) that the reference's outcome is recorded too. Recording the declared stage needed a change: `attach_failure_phase` carried only the coarse subcommand inference, which maps EVERY `run-user-commands` failure to `exec` — so a case pinning `lifecycle:postStart` had its own recorded stage contradicted by the report it appeared in. The declaration now wins, the inference is kept alongside when it differs, and a new `sides` block records each side's exit code and whether it failed (FR-042's "outcome for each side", which a differential verdict alone never stated)
- [X] T097 [P] [US4] Test in `crates/parity-harness/tests/prereq_faults.rs`: absent Docker or a mismatched oracle fails with a cause-specific error, never a skip or a pass (scenario 2, FR-044) — 018 already unit-tested the RESOLUTION half inside `oracle.rs`/`prereq.rs`; what was missing is the half FR-044 actually turns on, that nothing downstream can convert a missing prerequisite into a green. Three guards cover it: every error-path case's `resourceGroup` is one the Docker driver owns (a tier case drifting to `none`/`fs-heavy` would be run by the binary defined to need no daemon, so neither prerequisite would ever be checked — a "pass" arriving through scheduling); the closed `Outcome` vocabulary has no word for a skip; and every real error-path differential run with `oracle: None` fails `OracleMissing` before executing anything, rather than comparing deacon against nothing and finding no differences. `probe_docker` and a new `oracle::verify_binary` are the injection seams — a test cannot drive the env-var resolution, since `std::env::set_var` is `unsafe` under this edition and `unsafe_code = "deny"`
- [X] T098 [P] [US4] Test in `crates/deacon/tests/parity_conformance_docker.rs`: every container, network, volume, and temp dir is reclaimed on success and on unwind (scenario 3, FR-045) — `docker_channels.rs` already proves the RAII guard fires on an unwind in isolation; `the_error_path_tier_reclaims_every_resource_it_creates` checks the property in situ over the real tier, which is where it matters: an error-path case fails partway BY CONSTRUCTION, so the step that would have torn something down never happens. Counted rather than name-matched (containers, networks, volumes, `deacon-conf-*` temp dirs, before vs after) so a leak nobody predicted the name of still shows up; a docker probe that cannot run counts zero on BOTH sides, so it can never manufacture a pass by shrinking the "after"
- [X] T099 [P] [US4] Test in `crates/deacon/tests/parity_conformance_docker.rs`: two concurrent cases observe none of each other's resources (scenario 4) — `two_concurrent_error_path_cases_observe_none_of_each_others_resources` runs the SAME case twice at once, which is the strongest form of the question: identical fixture and configuration mean identical container identity UNLESS each run's isolated workspace really does make it different. The case chosen creates a container and then tears it down, so a collision is not theoretical — two runs sharing a `devcontainer.local_folder` label would have one run's `down` remove the other's container. Asserting both reach their alone-verdict, rather than merely that the workspace paths differ: the weaker check passes on a harness that computes distinct paths and labels both containers identically anyway
- [X] T100 [P] [US4] Test in `crates/conformance/tests/error_path_coverage.rs`: no error-path case reaches its verdict at configuration read (SC-007) — checked three ways, because it can be lost three ways: no declared phase is `config-resolution`; every operation declaring a later stage is OBSERVED by an `expected` entry (else the stage is recorded and never compared) and at least one observation comes from an operation that can get past configuration read; and no tier case reuses a fixture the registry elsewhere uses to demonstrate a configuration-read REJECTION — the sharpest of the three, since such a fixture is the registry's own evidence that the document is not accepted. Three further tests cover the SC-007 stage span (both directions: every stage covered, every tier case attributable to one), the FR-045 reclamation declaration, and the FR-043 direction twin

### Implementation for User Story 4

- [X] T101 [US4] Add the `errorPathTier` marker and failure-stage assertion to the declarative case shape in `crates/conformance/src/model.rs`, reusing the existing `expect_failure_phase` rather than adding a predicate — the stage stays `expect_failure_phase`; what the boolean adds is MEMBERSHIP, which a predicate over the phase cannot express in either direction: `case-down-removes-container` declares `expectFailurePhase: exec` on a verification step and is not an error-path case, while a genuine tier case whose phase was left off would silently leave the tier. Declared, both become validation failures. Also added: `FailurePhase::{ALL,as_str,is_configuration_read,is_lifecycle}`, `TestCase::{declared,later_stage}_failure_phases`, and the closed `phases_reachable_by` mapping. Excluded from `caseHash` for the same reason as `inputClass` — joining the tier must never re-record a snapshot
- [X] T102 [US4] Extend V16 in `crates/conformance/src/validate.rs` to require an error-path case to declare a later-stage failure phase and to reject one whose verdict is reachable at configuration read — four rules. A tier case must (1) declare a later-stage phase somewhere, (2) declare `config-resolution` nowhere, and (3) be Docker-backed. Rule (4) governs EVERY declarative case: a declared phase must be one its subcommand can REACH. That one is the load-bearing addition — `read-configuration` reaches exactly one phase, so a case declaring `lifecycle:postCreate` on it describes a run that cannot happen and would otherwise validate, run, and report a green nothing. `down` deliberately stops at `config-resolution`: teardown owns no phase in the closed set (022 data-model §8, reused not extended), which is why the teardown case declares its failure on the operation that OBSERVES the teardown. RULES.md gains the matching section
- [X] T103 [P] [US4] Add build-stage error-path cases to `conformance/registry/cases/build.json` with pinned image inputs (FR-042, FR-046) — **2 cases**, both `live-differential`, both verified against oracle 0.87.0 in an isolated workspace (both sides read the configuration cleanly, both fail at build). `case-errorpath-build-missing-target`: `build.target` names a stage the Dockerfile does not define — a free-form string no reader can validate until the builder resolves the graph. `case-errorpath-build-arg-failure`: a `RUN` that fails only when the configuration's `build.args` value reaches it, so a CLI that dropped build args would SUCCEED — which makes the failure evidence of delivery that the existing configuration-comparison build-args case cannot supply
- [X] T104 [P] [US4] Add container-creation error-path cases to `conformance/registry/cases/up.json` — **2 cases**, both `live-differential`, both verified live. `case-errorpath-up-invalid-run-args`: `runArgs: ["--cpus", "not-a-number"]`, a well-formed array of strings that both CLIs pass through by design and the runtime CLI refuses to parse. `case-errorpath-up-missing-mount-source`: a bind mount whose host source does not exist — a request the DAEMON refuses to satisfy. Kept separate because a CLI can validate its own argv construction without ever asking the daemon, so the two rejections come from different places
- [X] T105 [P] [US4] Add Feature-installation error-path cases to `conformance/registry/cases/up.json` — **2 cases**, both `live-differential`, both verified live, under a new behavior `bhv-up-feature-install-failure` (source-linked, dispositioned, registered in `POST_BRANCH_BEHAVIORS`). `case-errorpath-up-feature-install-failure`: a local Feature whose `install.sh` exits 29. `case-errorpath-up-feature-dependency-cycle`: two local Features whose `dependsOn` name each other, so no install order exists. Deliberately not folded into `bhv-up-feature-install-order`: an implementation that installs the right Features in the right order and SWALLOWS a failing install script satisfies that claim and hands back a container that looks complete. Both declare `expectFailurePhase: build`, because on both implementations Features are installed by a Docker build over the base image
- [X] T106 [P] [US4] Add lifecycle-execution error-path cases to `conformance/registry/cases/run-user-commands.json` — **2 cases** (`updateContentCommand` and `postStartCommand` failures), and they found a REAL divergence the story was written to find: the reference's `run-user-commands` skips a lifecycle command the container has already completed and exits 0 without running it, while deacon runs the configured commands on every invocation and propagates the failure. Verified directly (after `devcontainer up` a plain `run-user-commands` does not re-run `postCreateCommand`; after `up --skip-post-create` it does — so the skip is keyed on completion). This also falsified `bhv-run-user-commands-hook-failure`'s `reference: aligned`, authored in US3 without running the differential: corrected to `divergent`, characterized by `wvr-ruc-completed-hooks-not-rerun`, and the pre-existing `case-run-user-commands-hook-failure` given the same tolerance
- [X] T107 [P] [US4] Add teardown error-path cases to `conformance/registry/cases/down.json` — **1 case**, `case-errorpath-down-repeat-teardown`: `up`, `down --remove`, `down --remove` AGAIN against resources already removed, then an `exec` that must fail. The second teardown is the subject — `down` is used as a cleanup step whose whole value is that it can be run when the state is unknown, so encountering a container that is no longer there is an error path it must absorb. The recorded failure is the exec's, both because a `down` exit code cannot distinguish removing something from removing nothing, and because the closed failure-phase set names no teardown phase. `spec-expectation`: the pinned reference has no `down` command at all
- [X] T108 [US4] Add direction-pinning companion cases for every error-path case whose sides disagree, so the differential is never the only evidence (FR-043, the 023 T074 lesson) — **3 twins**, one per disagreeing lifecycle hook (`postCreate`, `updateContent`, `postStart`), each a `spec-expectation` asserting deacon's side outright: the preceding `up` SUCCEEDS (so a failure cannot be blamed on a container that never came up) and the hook run FAILS. That is the side the spec's account of lifecycle-command failure supports and the side that keeps `run-user-commands` usable as an explicit re-run, its `--skip-*` flags being the opt-out. The differentials alone would stay green if BOTH sides started reporting success — the difference simply stops reproducing, and the record that was supposed to say which side is right says nothing. The other six error-path cases AGREE on both sides and need no twin
- [X] T109 [US4] Verify V18 rejects any unpinned image among the new fixtures, and pin any that fail — every new fixture pins `alpine:3.19` (declared `image`) or `FROM alpine:3.19` (Dockerfile base), so `validate` reports no V18. Verified as a NEGATIVE control rather than by inspection: re-pointing one new fixture at `alpine:latest` and then at bare `alpine` each produced the expected V18 naming the case and the image, and reverting cleared it — so the rule is demonstrably live on this tier's fixtures, not merely silent. Noted while checking: V18 reads only the config's top-level `image`, so a Dockerfile `FROM` is outside its reach; the two Dockerfile fixtures pin their base regardless

**Checkpoint**: Parity no longer stops where the reference is most lenient.

---

## Phase 7: User Story 5 - Fields that broad normalization used to hide (Priority: P2)

**Goal**: The twelve named fields are compared, not merely captured.

**Independent Test**: For each named field, confirm a case observes it and that changing only
that field makes the case fail.

**Depends on**: Phase 2 Block B (T015–T022).

### Tests for User Story 5

- [X] T110 [P] [US5] Test in `crates/conformance/tests/denormalized_coverage.rs`: every named field appears in `denormalizedFields` with ≥1 covering case (scenario 1, SC-008) — three tests, because the report's substring match is forgeable on its own: `labels.devcontainer.source` contains "source", so a case comparing only a LABEL would be credited with covering `mount-source`. So coverage is asserted, then re-asserted against a case whose id NAMES the field, and then the US5 cases are required to carry a real assertion on every channel they declare (a declared channel with no assertion is capture, not comparison)
- [X] T111 [P] [US5] Test in `crates/parity-harness/tests/normalize_consistency.rs`: null, empty, and omitted produce three distinguishable observations (scenario 2, FR-055) — asserted on the REFERENCE side (the side that has the information: its `configuration` is an echo of the authored document), then on the resulting VERDICTS (two divergences differing in what the reference held, one agreement). deacon's collapse is pinned in the same test, deliberately: it is the characterized defect, and pinning it means a future widening of the drop rule cannot quietly re-hide it
- [X] T112 [P] [US5] Test in `crates/deacon/tests/parity_conformance_docker.rs`: array-form and object-form lifecycle hooks are both observed (scenario 3) — the two cases are driven in ONE test, because a form observed in isolation cannot show it was distinguished. Live agreement proves each matched its own declaration; the structural half proves the declarations describe DIFFERENT observations, with disjoint marker files and both of the object form's named commands pinned (an implementation that stopped after the first would pass every exit-code assertion)
- [X] T113 [P] [US5] Test in `crates/parity-harness/tests/normalize_consistency.rs`: an unscoped normalization rule is rejected (scenario 4, V24, FR-056) — the shipped rule set is clean AND the check is demonstrably live: a well-formed rule must NOT be reported, and each of the five ways a rule can be unscoped (no scope, `all`, an unqualified scope, an open-ended `removes`, a drop with no justification) is fed to it as a negative control. A companion test pins that every rule the audit touched is registered with the right action — `compose_project_prefix` and `user_default_root` were applied for two stories without appearing in the registry at all, which is the one failure V24 cannot catch on its own
- [X] T114 [P] [US5] Test in `crates/conformance/tests/denormalized_coverage.rs`: an ambiguous Feature install order is either pinned or dispositioned (scenario 5) — checked at the FIXTURE (`overrideFeatureInstallOrder`, or `dependsOn`/`installsAfter` between the Features), never in prose, and scoped to the operations that actually INSTALL: `read-configuration` resolves an order without running anything and its cases compare the whole document, so there is no second place for chance to enter. A remote Feature's own `installsAfter` is invisible to the tree scan and is reported anyway — the safe direction. Two further tests require the install-order behaviors to be case-backed and the chain case to pin an EXACT ordered content

### Implementation for User Story 5

- [X] T115 [P] [US5] Add lifecycle array-vs-object and command/entrypoint cases to `conformance/registry/cases/up.json`, including entrypoints chained by multiple Features (FR-047, FR-048) — **5 cases** + `bhv-up-lifecycle-command-forms` / `bhv-up-feature-entrypoint-chain`. Both observed through their EFFECT: marker files the hooks and entrypoints write into the bind-mounted workspace, read on the host via `chan-file-content`. The raw entrypoint cannot be compared — deacon mounts a generated `/devcontainer/entrypoint-wrapper.sh`, the reference inlines the scripts into the container command — so comparing the mechanism would report a difference that says nothing about whether the entrypoints ran. The object form's two commands write SEPARATE files (the spec does not order named commands). `overrideFeatureInstallOrder` pins the otherwise-ambiguous order. `command`/`entrypoint` are additionally pinned on `chan-container-state` by the label/network case
- [X] T116 [P] [US5] Add environment merge-precedence and PATH-construction cases to `conformance/registry/cases/up.json` (FR-049, FR-050) — **3 cases**, and they found a REAL defect: a variable declared by BOTH the configuration and a Feature took the FEATURE's value in deacon (`container.rs` merged the feature-built image's env over the configuration's with `extend`), where oracle 0.87.0 gives the configuration's. Fixed in its own commit; `bhv-up-container-env-merge-precedence` records the state after the fix. The fixture carries a config-only and a feature-only variable alongside the conflicting one deliberately — without them a case cannot tell "the configuration wins the conflict" from "the Feature layer was dropped". PATH is compared SEGMENT-wise, with two base-image segments asserted alongside the Feature's, so a PATH that was REPLACED rather than prepended fails
- [X] T117 [P] [US5] Add user and UID/GID cases, including a non-root user and a Feature-created user, to `conformance/registry/cases/up.json` (FR-051) — **2 cases** + `bhv-up-effective-user-uid-gid`, each asserting BOTH halves: the declaration (`userSpec`, parsed from `Config.User`) and the effect (`id -u`/`id -g`/`id -un` run inside the container). The declaration alone would pass for a user that does not exist; the ids alone would not say which user was asked for. Distinct uids (4242 image-created, 4343 Feature-created) so a fixture mix-up cannot pass either case
- [X] T118 [P] [US5] Add metadata-label namespace cases, including labels one side emits and the other does not, to `conformance/registry/cases/up.json` (FR-052) — **1 case** pinning the derived `labelNamespaces` grouping, which is what makes "one side populates a namespace the other leaves empty" an ordinary comparison instead of an enumeration of every key either CLI might ever emit. The one-sided half is carried by the differentials, which now diverge at `labelNamespaces.devcontainer` and declare it — the same five identity labels the per-key tolerances name, reported a second way
- [X] T119 [P] [US5] Add mount and mount-source cases distinguishing a differing source path from a differing mount shape to `conformance/registry/cases/up.json` (FR-053) — **2 cases**, with the two axes separated by CONSTRUCTION rather than by prose: `/mnt/ro` and `/mnt/rw` share a shape and differ in source, `/mnt/rw` and `/mnt/vol` share a read-only flag and differ in type. Swapping the bind sources moves only `mountSources`; swapping the read-only flag moves only `mounts`. Before this the source axis was unobservable — the snapshot recorded only the bind's leaf component
- [X] T120 [P] [US5] Add network and Compose project-resource cases to `conformance/registry/cases/up.json` (FR-054) — **3 cases** (network folded into the label case, two Compose). The Compose differential surfaced a new characterized divergence: deacon delivers its generated Compose override on stdin (recorded as `-`), the reference writes a temp file and names it, so `project.config_files` and the `config-hash` derived from it differ. Recorded as `bhv-compose-project-file-set`, kept DISTINCT from `bhv-compose-project-name-robust` because an implementation could adopt either CLI's project naming and still deliver its override the other way
- [X] T121 [P] [US5] Add null/empty/omitted cases to `conformance/registry/cases/read-configuration.json` (FR-055) — **2 cases**. The OMITTED state needed a way to name a property that is not in the document, so the fixture declares it in a `.conformance-observed-properties` sidecar — deliberately not a config key, since a key added to steer an observation would change the document under observation. The differential is the case the normalizer narrowing exists to make possible: while `drop_absent_optional` ran on both sides all three states normalized to "absent on both sides" and this comparison was green while proving nothing. Four tolerances, all backed by `wvr-readconfig-authored-empty-omitted`
- [X] T122 [US5] Add the derived observer fields these comparisons need to `crates/parity-harness/src/observe/`, never a predicate or query in the assertion language (the 023 hard line) — new `observe/derived.rs`: `envMap`, `pathSegments`, `mountSources`, `labelNamespaces`, `userSpec`, `composeProjectResources` on `chan-container-state`, and `nullEmptyOmitted` on `chan-structured-output`. Each replaces a search, a grouping or a type test that would otherwise have to live in `expected`. All ADDITIVE — no raw field is removed or replaced, because a derived summary that replaced its source would be a collapse wearing a different name. `nullEmptyOmitted` classifies only the AUTHORED properties (plus any the fixture's sidecar names): classifying every modeled optional would restate one characterized defect as ~40 differences on every configuration case, and it is attached only to a document carrying a `configuration`/`mergedConfiguration` block, so `upgrade`'s lockfile result and the two exact `jsonEquals` assertions on it are untouched
- [X] T123 [US5] Audit `crates/parity-harness/src/normalize.rs` for rules that remove or collapse observable content; narrow or retire each, and bump `NORMALIZER_VERSION` in `crates/conformance/src/snapshot.rs` — **four findings**. (1) `drop_noise_env` claimed the scope `channel:chan-container-state` but ran at CAPTURE, inside `container_state` — which the declarative observer DELEGATES to — so it stripped `PATH` from the very channel FR-050 needs it on; moved to the legacy `diff_states` comparison, whose verdicts are unchanged. (2) The one variable two containers can never agree on, `HOSTNAME` (Docker's default is the container's short id), is now REWRITTEN by the new named `container_hostname_token` instead of being deleted alongside `PATH` and `HOME`; a configured hostname is not 12 hex characters and is compared. (3) `drop_absent_optional` ran on BOTH sides, which is what made an authored null, an authored empty and an omission ONE observation; narrowed to the side whose serializer defect it compensates — deacon's `configuration` block — while still applying to `mergedConfiguration` on both, where the pinned reference emits computed empties of its own. (4) `compose_project_prefix` and `user_default_root` were applied but had never been REGISTERED, which is the failure V24 cannot catch on its own: it validates the rules it is given. `NORMALIZER_VERSION` 5 → 6
- [X] T124 [US5] Refresh the committed snapshots invalidated by the normalizer bump via `cargo run -p parity-harness --bin conformance-snapshot refresh`, reviewing the resulting git diff as the review surface — **1 snapshot** (`case-readconfig-snapshot`), reported stale on `normalizerVersion` "5" != "6", which is the mechanism working. The review diff is two lines, `normalizerVersion` and `capturedAt`: `raw.json` and `normalized.json` are byte-identical, and that is the EXPECTED outcome rather than a suspicious one — the case observes `chan-exit-code` only, which no US5 rule touches and to which no derived field was added. A snapshot whose evidence had changed would have shown it in the same diff
- [X] T125 [US5] Characterize any divergence the de-suppression surfaces: record the behavior with all three axes, add a scoped `wvr-` or an `ext-` record, and file a `parity-drift` issue cross-linked from the behavior's `notes` for the fix-flavored ones (spec Assumption 2) — **four divergences, all measured against oracle 0.87.0**. `containerEnv` precedence: deacon was wrong and is FIXED, so its behavior records the state after the fix. `devcontainer.metadata` content: deacon omits the per-Feature `{"id": …}` entries the reference records AND writes substituted absolute host paths where the reference keeps the author's `${localWorkspaceFolder}`, baking the building machine's layout into an image label (`bhv-container-metadata-label-content` + waiver). Authored-empty vs omitted in `read-configuration` (`bhv-readconfig-authored-empty-omitted-collapsed` + waiver, tracked as 023 T111). Compose override delivery (`bhv-compose-project-file-set` + waiver). **Two record `spec: nonconformant`, which nothing in the registry had before** — it is the honest axis value and R1–R8 admit it, but note the vocabulary gap: the decision enum has no "known defect, fix queued" value and R5/R6 forbid `follow-spec`/`align-with-reference` while the reference is divergent, so `intentional-divergence` is the only VALID decision and means, for these, recorded and not yet fixed. **The `parity-drift` GitHub issues are NOT filed** — an automated agent should not open them; each behavior's `notes` and each waiver's `rationale` say so in capitals, and the two needing one are `bhv-container-metadata-label-content` and `bhv-readconfig-authored-empty-omitted-collapsed`

**Checkpoint**: The fields where false equivalence survives longest are now compared.

---

## Phase 8: User Story 6 - Prove the suite can fail (Priority: P3)

**Goal**: Each observable channel is demonstrably live.

**Independent Test**: Run the injected-regression harness and confirm every channel has ≥1
detected regression and that `inertCount` is zero.

**Depends on**: Phase 2 Block B, and on US3–US5 for the cases that do the detecting.

### Tests for User Story 6

- [X] T126 [P] [US6] Test in `crates/parity-harness/tests/injection_faults.rs`: an injected regression makes the suite fail with the failure naming the channel (scenario 1)
- [X] T127 [P] [US6] Test in `crates/parity-harness/tests/injection_faults.rs`: a channel with no detecting regression is reported inert and fails the run (scenario 2, FR-067)
- [X] T128 [P] [US6] Test in `crates/parity-harness/tests/injection_faults.rs`: the tree is unmodified after a run, including after an unwind (scenario 3, FR-066)
- [X] T129 [P] [US6] Test in `crates/parity-harness/tests/injection_faults.rs`: the detected/inert classification is identical across two runs (scenario 4, FR-069)
- [X] T130 [P] [US6] Test in `crates/parity-harness/tests/injection_faults.rs`: a **dead observer** — one returning a constant regardless of input — is correctly reported inert, proving the injection point is upstream of the observer (research Decision 5, FR-065b)
- [X] T131 [P] [US6] Test in `crates/parity-harness/tests/injection_faults.rs`: an ordinary run of either conformance driver cannot apply a regression — the injector is unreachable outside the `coverage-regressions` bin, asserted structurally rather than by convention (FR-070) — asserted as a **source-level reachability scan** plus a runtime no-op check, not by comment: enabling injection requires `RegressionHarness::declare()`, and the scan asserts that call exists in exactly one program (`coverage-regressions`, plus this guard). The drivers and the harness modules they go through are additionally asserted not to name `activate` / `perturb_source` at all, so the single contact point an ordinary run has is `inject::intercept`, which is inert with nothing armed

### Implementation for User Story 6

- [X] T132 [US6] Implement the `reg-` record model in `crates/conformance/src/regression.rs` with the closed perturbation-kind set from `contracts/regression-harness.md`
- [X] T133 [US6] Implement V30 (declared channel with no record; record targeting a channel with no observer) in `crates/conformance/src/validate.rs`
- [X] T134 [US6] Implement evidence-source-boundary injection in `crates/parity-harness/src/inject.rs`, applying perturbations to the raw captured artifact before any observer runs
- [X] T135 [US6] Add a compile-time or runtime guard in `crates/parity-harness/src/inject.rs` preventing injection into an observer's returned `RawChannelEvidence` (FR-065b) — a **type-level** guard, which is the stronger of the two options offered: every perturbation entry point is generic over a SEALED `EvidenceSource` trait whose supertrait is private, so `RawChannelEvidence` / `NormalizedChannelEvidence` cannot implement it in this crate or any other. Injecting downstream of an observer does not compile. Backed by a second, independent closure: `EvidenceTarget` names only raw artifacts, so a record cannot even ask for the forbidden point
- [X] T136 [US6] Implement apply/revert with unwind safety in `crates/parity-harness/src/inject.rs` using an RAII guard, mirroring the existing workspace cleanup guard
- [X] T137 [US6] Implement the `coverage-regressions` bin in `crates/parity-harness/src/bin/coverage-regressions.rs`, writing the byte-stable `target/conformance/regressions.json` and exiting non-zero on any inert channel. Baselines are cached **per case**, not per record — one `up` exercises four channels, and re-running it per record would quadruple the Docker cost for no extra evidence. Detection requires the baseline to be CLEAN on the channel and the perturbed run not to be; an already-failing case is reported as a note and does not count (its failure is not evidence the injection caused anything). A perturbation that never landed is a fail-loud `InjectionInapplicable`, deliberately NOT `inert`: `inert` is a claim about the channel, and a mis-authored record must never be able to masquerade as a dead one. A FILTERED run refuses to write the canonical report, so a partial document can never read as a complete run
- [X] T138 [US6] Author `conformance/registry/regressions.json` with ≥1 record per declared channel, covering all eleven (FR-065) — **11 records, `inertCount: 0` verified against live Docker + the pinned 0.87.0 oracle**. Two channels came back INERT on the first run and the fault was in the CASES, not the perturbations, which is the finding this whole story exists to produce: `chan-process-graph` carried `jsonSubset: {}` on `case-up-docker-channels` — an empty subset matches ANY graph, so the channel was declared, observed, and incapable of failing — and `chan-stderr` was asserted only with `contains`, which cannot see appended output by construction. Both were strengthened (the graph now pins the workspace bind + network; the stderr assertion is anchored at end-of-text, i.e. the parse diagnostic must be the LAST thing deacon writes). A third, `chan-stdout`, needed a different case: its natural differential candidate has a PRE-EXISTING baseline divergence (tracked separately), and an already-failing case cannot attribute anything
- [X] T139 [US6] Add `make test-parity-regressions` to the `Makefile` and wire the run into `.github/workflows/parity.yml` — wired WITHOUT `continue-on-error` and positioned after the comparison + aggregator, so a real divergence is reported on its own terms first and an inert channel still reddens the lane. The report is uploaded as its own artifact

**Checkpoint**: Every channel is proven able to fail. The green results of US1–US5 now mean
something.

---

## Phase 9: Polish & Cross-Cutting Concerns

- [ ] T140 [P] Update the "Conformance Registry" and "Parity & Conformance" sections of `CLAUDE.md` with the scenario model, obligation kinds, and V26–V30
- [ ] T141 [P] Add the coverage workflows to `conformance/RULES.md`: the drift workflow, the five reporting buckets, and the reporting-never-gates rule
- [ ] T142 [P] Verify SC-005 by confirming `channelsBelowFloor` is zero in `target/conformance/coverage-observables.json`
- [ ] T143 [P] Verify SC-006 by confirming `inertCount` is zero in `target/conformance/regressions.json` and that every one of the eleven declared channels appears with verdict `detected`
- [ ] T144 [P] Verify SC-004 by confirming `missingConfigSources` is empty for all ten operations
- [ ] T145 Verify SC-011 by running the hermetic set ten consecutive times and the live set three consecutive times, confirming identical verdicts and zero flakes
- [ ] T146 Verify SC-002 and SC-001 by confirming `summary.undispositioned` is zero in `target/conformance/coverage-pairwise.json`
- [ ] T147 Run the full quality gate: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `make test-nextest`, `validate`, `certify`
- [ ] T148 Walk `specs/024-deterministic-conformance-coverage/quickstart.md` end to end and correct any drift between it and the shipped commands

---

## Deferred Work

Per the constitution's Deferral Tracking rule, every deferral recorded in `research.md` or
`spec.md` gets an entry here with acceptance criteria. A specification is **not** complete
while deferred tasks remain unresolved.

- [ ] T149 [Deferral] Activate a second environment profile (alternative runtime and/or non-Linux platform) per spec Assumption 10
  - **Decision**: spec FR-004a/FR-004b, clarification session Q1 — model now, activate later
  - **Rationale**: activating a runtime lane multiplies live cost; the model keeps the backlog visible in the meantime via the `inactive-environment` bucket
  - **Acceptance**: marking the profile `active` in `conformance/registry/profiles.json` re-buckets its obligations with **zero** changes to `scenario.json`, `applicability.json`, or any case (SC-015 already tests this against a fixture; this task does it for real)
- [X] T150 [Deferral] Re-review the remaining `non-testable` clause classifications not covered by T095
  - **Decision**: research Decision 9 — the 156 `non-testable` clauses are the real reservoir of new behaviors; only those blocked by the absent container tier are in scope for T095
  - **Acceptance**: every remaining `non-testable` classification either keeps its ground with a restated rationale, or becomes `behavior-mapped` with a behavior and dispositioned obligations
  - **Outcome**: all 148 reviewed individually against the clause's own excerpt in
    `conformance/inventory/clauses.json`, one document at a time. Result: **148 → 45**
    `non-testable`, **+91** `behavior-mapped` (59 → 150), **+12** `not-applicable` (12 → 24).
    - The starting state was the real finding: all 148 carried one of exactly **two**
      boilerplate rationales, which is the filler-rationale failure V23/V29 reject
      everywhere else in this registry. Every one of the 148 now names a specific ground.
    - **90 of the 91 flips reuse an EXISTING behavior**, so the coverage denominator did
      not move and no flip rests on evidence that does not run. The mapping rule is the
      one 020 already established for the schema surface: a prose clause stating a
      property's shape maps where its `cst-` twin maps (`bhv-readconfig-basic-parse`), a
      clause stating a runtime consequence maps to the behavior that observes it. 024's
      own US5 behaviors did most of the unblocking — "container mounts, environment
      variables, and user configuration should be applied at [creation]" and "per
      variable, last value wins" had no observer before this feature and have three now.
    - **1 flip minted a behavior**, `bhv-up-lifecycle-command-cwd`, for the clause
      inventory's only carrier of the lifecycle-hook cwd rule. It arrives with
      `case-up-lifecycle-cwd` + `fx-lifecycle-cwd` (obligation dispositioned `case`, not
      `gap`) and both axes measured against the pinned oracle first, per Decision 11.
      Verified live under `--profile parity`.
    - **12 re-dispositioned `not-applicable`** where the ground is scope, not
      testability: the whole `features-legacy-ids` document (its `legacyIds`/`deprecated`
      schema twins are already `not-applicable`), `supporting-tools`' editor- and
      Codespaces-owned entries, and the publish-side OCI annotation clause.
    - **Zero left open.**
  - **Two findings worth their own follow-ups** (neither is a T150 deliverable):
    - `optionalPaths` (templates) is not implemented at all — no occurrence anywhere in
      `crates/`. Its three clauses are dispositioned on the prompt-is-not-observable
      ground, which is honest for a non-interactive CLI but is not the same as saying the
      property is out of scope.
    - The hypothesis that `authoring.json` held per-clause records that the document-scope
      `not-applicable` default already covers is **false**: all 36 of its `non-testable`
      records are CONSUMER obligations living inside authoring documents, which is exactly
      why they were given per-clause records. No cleanup was needed there.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: no dependencies
- **Foundational Block A (T007–T014)**: blocks **all** user stories
- **Foundational Block B (T015–T022)**: blocks **US3–US6 only** — not required for the MVP
- **US1 (Phase 3)**: after Block A
- **US2 (Phase 4)**: after Block A; independent of US1 in principle, but its fixtures are far
  easier to author once US1's generator exists
- **US3, US4, US5 (Phases 5–7)**: after Block A + Block B; mutually independent
- **US6 (Phase 8)**: after Block B, and needs US3–US5's cases to have something to detect with
- **Polish (Phase 9)**: after all desired stories

### Critical Path

```text
T007 → T008 → T010 → T012 → [US1: T031 → T037 → T039 → T044 → T048] → [US2: T061 → T064 → T066]
                          └→ T015 → T017 → T020 → [US3/US4/US5 cases] → [US6: T134 → T137]
```

### Ordering Rules That Are Not Negotiable

1. **T007 lands alone.** The `cases.json` split is a mechanical migration that can silently
   lose records; T009's count assertion is the guard, and folding a content change into the
   same commit hides it.
2. **T064/T065 (V28/V29) land before T094.** The guard against diluting the denominator must
   exist before behaviors are added to it, or the first bulk addition slips through
   undispositioned.
3. **T123 before T124.** Refreshing snapshots before narrowing the normalizer records the
   suppressed values as if they were reviewed.
4. **T108 before US4 is considered complete.** A differential alone proves disagreement, not
   direction.

### Parallel Opportunities

- Phase 1: T002–T006 all parallel
- US1 tests: T023–T030 all parallel
- US1 renderers: T044, T045, T046 parallel (distinct functions, one file — serialize the
  final commit)
- US2 tests: T052–T060 all parallel
- US3 case authoring: T082–T093 all parallel (distinct `cases/<area>.json` files, which is
  precisely what research Decision 7's split buys)
- US4 case authoring: T103–T107 all parallel
- US5 case authoring: T115–T121 all parallel
- US3, US4, US5 can proceed concurrently by different people once Block B lands

---

## Parallel Example: User Story 3 case authoring

```bash
# Distinct files, no shared state — the payoff from splitting cases.json by area:
Task: "Add Feature install-order cases to conformance/registry/cases/up.json"
Task: "Add build cases to conformance/registry/cases/build.json"
Task: "Add lifecycle cases to conformance/registry/cases/run-user-commands.json"
Task: "Add teardown cases to conformance/registry/cases/down.json"
Task: "Add templates apply cases to conformance/registry/cases/templates.json"
```

---

## Implementation Strategy

### MVP (US1 only)

1. Phase 1 Setup
2. Phase 2 **Block A only** — skip Block B entirely
3. Phase 3 US1
4. **STOP and VALIDATE**: `coverage report` against the unchanged 88-case set enumerates the
   valid space and lists the remainder

The MVP delivers the spec's stated highest-value increment — a measured, reviewable backlog —
with zero new conformance cases and no Docker.

### Incremental Delivery

1. Setup + Block A → hermetic foundation
2. US1 → the hole is visible (MVP)
3. US2 → the hole blocks the release
4. Block B → live infrastructure can scale
5. US3 → the workflow is covered
6. US4 → parity survives past configuration read
7. US5 → suppressed fields are compared
8. US6 → the suite is proven able to fail

Each step is its own small, CI-gated PR with a Conventional-Commit title
(`feat`/`fix`/`chore` — never `test`/`style`, which the PR-title check rejects).

---

## Notes

- **Every task is a data or code edit in a named file.** Adding a case, assertion, fixture,
  dimension value, or applicability rule requires **zero** new test functions (SC-013) — the
  only new test functions in this list are the acceptance tests FR-071 mandates and the
  per-resource-group drivers of research Decision 4.
- **No `#[ignore]`, no env-var opt-in, no silent skip** anywhere (FR-075). A missing
  prerequisite fails loudly.
- Live tasks run only under `--profile parity`; hermetic tasks run on every PR.
- Commit after each task or logical group; stop at any checkpoint to validate independently.
