# Features Plan — Empty Example

What this demonstrates

- Running `deacon features plan` when no features are declared.

How to run

```sh
# From this directory
deacon features plan --config devcontainer.json --json
```

Expected output

```json
{ "order": [], "graph": {} }
```
