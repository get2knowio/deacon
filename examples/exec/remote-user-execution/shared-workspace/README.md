# Shared Workspace Directory

This directory is used to test file creation and permissions when executing commands as the configured `remoteUser`.

Files created here via `deacon exec` will be owned by the remoteUser specified in devcontainer.json.
