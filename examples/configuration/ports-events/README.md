# Ports Events Example

Starts a tiny HTTP server on port 8080. Use port events to observe forwarding behavior.

Run (from this directory):
```sh
deacon config validate .
# Then bring it up with port events enabled
# (requires Docker running)
# deacon up . --ports-events
```
