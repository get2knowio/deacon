# Dockerfile-based Configuration

Demonstrates using `dockerFile` and `build` options with build args to construct the dev container image.

Run (from this directory):
```sh
deacon config validate .
```